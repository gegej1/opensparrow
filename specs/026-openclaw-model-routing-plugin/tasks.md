# F-026 Tasks — OpenClaw Model Routing Plugin Integration

- [x] 准备兼容 `@blockrun/clawrouter` 的 OpenClaw runtime（目标 `2026.4.14`）。
- [x] 固化 runtime 安装时的 `OPENCLAW_DISABLE_BUNDLED_PLUGIN_POSTINSTALL=1`。
- [x] 新增 plugin 管理脚本，封装 install / enable / disable / status / test。
- [x] 新增 mac 点击入口，覆盖开启 / 关闭 / 状态 / 验证。
- [x] 用 CLI 验证 plugin 安装、加载、默认模型切换。
- [x] 更新 runbook、`feature_list.json`、`claude-progress.txt`。
