# Progress Log (opensparrow-unified)

## Session 1 (Root Unification Bootstrap)
- Date: 2026-03-23
- Goal: 把统一真源工作根正式切换到 `opensparrow/`，并完成第一阶段上下文迁移、规则合并与结构收口。
- Workspace path: `longrun/workspaces/opensparrow-unified`
- Project root: `/Users/eduardogan/Desktop/GHJProject/opensparrow`
- Imported contexts:
  - `openclawNative` → `vendor/`、`platforms/*/companion/`、`.codex/`、`.specify/`、`longrun/`、`docs/`
  - `opensparrow_win/feishu-source` → `specs/`、`scripts/openclaw-usb/`、`skills/`、`research/`、`longrun/workspaces/openclaw-usb-portable/`
  - `opensparrow_win/usb-pack` → `ui/`、`docs/usb-pack/`、`docs/runbooks/`、`platforms/*/wrappers/`
- What was created or updated:
  - 根级 `AGENTS.md`
  - 根级 `.gitattributes`
  - 根级 `.gitignore`
  - 根级 `README.md`
  - 根级 `docs/项目持久化说明.md`
  - 根级 `docs/多平台统一仓方案-20260323.md`
  - 根级 `.specify/memory/constitution.md`
  - `specs/006-opensparrow-root-unification/`
  - `longrun/workspaces/opensparrow-unified/`
  - legacy workspace 路径适配完成，并已通过 init 验证
- Confirmed planning conclusions:
  1) 统一真源仓继续成立，但工作根改为 `opensparrow/`；
  2) 统一仓不要求统一入口脚本；
  3) 应冻结 legacy 目录，而不是继续在其上手改；
  4) 容器化仍是后续 Phase，不在本次抢跑实现。
- Verification evidence:
  - `git init -b main` 已在根目录执行
  - `./longrun/workspaces/opensparrow-unified/init.sh` 通过
  - `./longrun/workspaces/openclaw-native/init.sh` 通过
  - `./longrun/workspaces/openclaw-usb-portable/init.sh` 通过
  - `bash -n scripts/openclaw-usb/*.sh`、`bash -n platforms/linux/companion/*.sh`、`bash -n platforms/mac/companion/*`、`bash -n platforms/mac/wrappers/*.command` 通过
- Progress snapshot: 6/7 passing（legacy 归档与 Docker baseline pending）
- Next feature candidate: `F-007`

## Session 2 (F-007 Legacy Archive + Docker Baseline)
- Date: 2026-03-23
- Goal: 把 F-007 从“已规划”推进到“已执行第一轮”：完成 legacy 逻辑归档冻结，并在 `deploy/docker/` 落地最小可运行容器基线。
- Selected feature id: `F-007`
- Changes made:
  - 新增 `specs/007-legacy-archive-docker-baseline/`（spec / plan / tasks）
  - 新增 `docs/legacy-archive-freeze-20260323.md` 与 `scripts/verify-legacy-freeze.sh`
  - 新增 `deploy/docker/Dockerfile`、`deploy/docker/docker-compose.yml`、`deploy/docker/.env.example` 与 `deploy/docker/bin/*.sh`
  - `ui/server.mjs` 已支持 `USB_RUNTIME_ROOT`、`OPENSPARROW_UI_PORT`、`OPENCLAW_GATEWAY_PORT`
  - `scripts/openclaw-usb/install-local-feishu.sh` 已支持 `prepare-only` 模式、`OPENCLAW_HOME` 与 `OPENCLAW_GATEWAY_BIND`
  - unified / portable workspace 的 F-007 feature item、runbook 与 handoff 已同步
- Regression checks:
  - `./longrun/workspaces/opensparrow-unified/init.sh`
  - `bash scripts/verify-legacy-freeze.sh`
  - `bash -n deploy/docker/bin/*.sh`
  - `bash -n scripts/openclaw-usb/*.sh`
  - `node --check ui/server.mjs`
  - `docker compose -f deploy/docker/docker-compose.yml config`
  - `docker compose -f deploy/docker/docker-compose.yml up -d --build opensparrow-core`（已尝试，但本机 Docker daemon 未启动）
  - `OPENSPARROW_APP_ROOT=$PWD ... bash deploy/docker/bin/run-core.sh`
  - `OPENSPARROW_APP_ROOT=$PWD ... bash deploy/docker/bin/bootstrap-profile.sh`
  - `curl http://127.0.0.1:19001/api/status`（bootstrap 前 `installed:false`，bootstrap 后 `installed:true`）
- End-to-end verification evidence:
  - legacy freeze contract 已由脚本校验
  - compose 已可渲染
  - 在本机 Docker daemon 不可用的情况下，已通过 `run-core.sh` + `bootstrap-profile.sh` 做等价验证，`/api/status` 在 bootstrap 前后分别返回 `installed:false` / `installed:true`
- Known risks:
  - bootstrap 之后的 `channels status --probe` 与 `agent smoke` 仍需要真实 Feishu/OpenAI 凭据
  - native wrapper / build-export 收口仍在下一阶段
- Open questions:
  - 后续是先收口 build/export，还是先把 native wrapper 再进一步薄化
- Next feature candidate:
  - build/export 与 native wrapper 收口
- Progress snapshot: 7/7 passing

## Session 3 (F-008 / W-002 Build-Export Dist Closure)
- Date: 2026-03-23
- Goal: 把 USB build/export 导出链从历史 workspace 落点收口到根级 `dist/`，并确保组包与 runtime 只从 canonical 根路径取源。
- Selected feature id: `F-008` / W-002
- Changes made:
  - 新增 `specs/008-build-export-dist-closure/`（spec / plan / tasks）
  - `build-delivery-pack.sh` 已改为从 `docs/usb-pack/`、`docs/runbooks/`、`platforms/*/wrappers/`、`scripts/openclaw-usb/` 组包，并输出到 `dist/usb-pack/openclaw-usb-pack/`
  - `create-mac-handoff-copy.sh` 与 `create-windows-handoff-copy.sh` 已改为输出到 `dist/handoff/`
  - `execution/scripts/lib/export-common.sh` 已改为使用 `vendor/mac-openclaw/` 与 `vendor/windows-openclaw/` 作为 bundled runtime 来源，不再依赖 `npm-global snapshot` 或在线下载 Node
  - 新增 canonical Windows 包内桥接 wrapper：`platforms/windows/wrappers/install-local-feishu.ps1`、`platforms/windows/wrappers/harden-local-feishu.ps1`
  - USB pack / Windows delivery / F-003 runbook / portable execution README 均已同步到新路径和 canonical 取源约束
- Regression checks:
  - `bash -n longrun/workspaces/openclaw-usb-portable/execution/scripts/*.sh`
  - `bash -n longrun/workspaces/openclaw-usb-portable/execution/scripts/lib/*.sh`
  - `bash longrun/workspaces/openclaw-usb-portable/execution/scripts/build-delivery-pack.sh`
  - `bash longrun/workspaces/openclaw-usb-portable/execution/scripts/create-mac-handoff-copy.sh`
  - `bash longrun/workspaces/openclaw-usb-portable/execution/scripts/create-windows-handoff-copy.sh`
  - `./longrun/workspaces/opensparrow-unified/init.sh`
  - `./longrun/workspaces/openclaw-usb-portable/init.sh`
- End-to-end verification evidence:
  - staging 包已生成在 `dist/usb-pack/openclaw-usb-pack/`
  - build/export 脚本不再引用不存在的 `execution/delivery-pack/` 模板目录
  - Mac handoff 已生成在 `dist/handoff/mac-feishu-usb-copy-20260323-130547/`，归档为同名 `.tar.gz`
  - Windows handoff 已生成在 `dist/handoff/windows-feishu-usb-copy-20260323-130642/`，归档为同名 `.zip`
  - bundled runtime 来源说明已收口为 `vendor snapshot`
- Known risks:
  - Windows handoff copy 仍建议补一次真实 Windows 主机验证
  - `F-006`（DingTalk Win/Mac parity）仍未执行完成
- Next feature candidate:
  - 回到 `F-006`
  - 或继续做 native wrapper 的进一步薄化与 release/archive 策略
- Progress snapshot: 8/8 passing

## Handoff quickstart
- Init command: `./longrun/workspaces/opensparrow-unified/init.sh`
- Session start: `./longrun/scripts/session_start.sh longrun/workspaces/opensparrow-unified`
- USB install (macOS/Linux): `bash scripts/openclaw-usb/install-local-feishu.sh --profile usb-portable --port 18889`
- USB install (Windows): `powershell -ExecutionPolicy Bypass -File scripts/openclaw-usb/install-local-feishu.ps1 -Profile usb-portable -Port 18889`
- Blockers (if any): 当前无根级结构阻断；剩余工作集中在 build/export 与 native wrapper 收口
- Notes for next session:
  - 如需导出交付件，优先查看 `dist/usb-pack/` 与 `dist/handoff/`
  - 继续推进 build/export 收口
  - 把 native wrapper 再进一步薄化
  - 再决定 legacy 目录的物理 archive 时机

## Session 4 (F-009 / W-005 UI Reset & Hardening Closure)
- Date: 2026-03-23
- Goal: 收口 UI-first 安装态/重置链路，消除 `/api/status` 假阳性，并补回 canonical macOS 双击入口。
- Selected feature id: `F-009` / W-005
- Changes made:
  - `ui/server.mjs` 新增统一 runtime 探测 helper，区分 `daemon` 真状态、`gateway-fallback` 健康态、以及“仅端口占用”场景，不再把 busy port 伪装成 `daemon=running`
  - `/api/status` 现返回 `configExists`、`profileDirExists`、`gatewayHealthy`、`gatewayPortBusy`，且 `installed` 仅在 `configExists && daemon === 'running'` 时为 `true`
  - `ui/public/dashboard.html` 增加 reset-aware 路由保护：`/dashboard?force=1` 或 reset 后 `configExists=false` 时自动回 `/setup?force=1`
  - 新增 canonical mac launcher：`platforms/mac/wrappers/01-开始部署.command`，直接启动 `ui/server.mjs`，不再触发 `FEISHU_APP_ID` / `OPENAI_API_KEY` CLI 提示
  - 新增 runbook：`docs/runbooks/F-005-ui-install-reset.md`
- Regression checks:
  - `node --check ui/server.mjs`
  - `bash -n platforms/mac/wrappers/01-开始部署.command`
  - `bash -n platforms/mac/wrappers/run-openclaw-usb.command`
  - `./longrun/workspaces/opensparrow-unified/init.sh`
  - `OPENCLAW_HOME=$(mktemp -d) OPENSPARROW_AUTO_OPEN=0 OPENSPARROW_UI_PORT=19051 node ui/server.mjs`
  - `curl http://127.0.0.1:19051/api/status`（空 profile / 伪残留 config 两轮断言）
- End-to-end verification evidence:
  - `/api/status` 不再用“18889 端口占用”推导 `daemon=running`
  - 临时 profile smoke 下：空 profile 时 `installed=false`，手动写入残留 `openclaw.json` 后仍为 `installed=false`
  - Dashboard 直接打开时，可基于 `configExists=false` 回到安装向导，而不是停留在旧 Dashboard 视图
  - macOS 顶层 UI 入口已恢复到 canonical 目录，且启动链只依赖 `ui/` + `vendor/mac-openclaw/`
- Known risks:
  - Windows `gateway-fallback` 仍会显示为非 daemon 安装态；这是刻意保留的真实状态，而不是回退到旧的假阳性逻辑
  - 尚未做浏览器级自动化验证，本轮以静态检查和代码路径验证为主
- Next feature candidate:
  - 回到 `F-006`（DingTalk Win/Mac parity）
  - 或补一轮 UI status/reset 的 API smoke test 脚本
- Progress snapshot: 9/9 passing

## Session 5 (F-007 / W-006 Docker SOP)
- Date: 2026-03-23
- Goal: 为已落地的 Docker baseline 补齐运维 SOP，并把 `docs/` 下的 Docker 相关文档口径统一到当前容器行为。
- Selected feature id: `F-007` / W-006
- Changes made:
  - 新增 `docs/docker-SOP.md`，覆盖前置条件、快速开始、配置说明、日常运维、健康检查、故障排查、备份恢复、升级流程。
  - 同步 `docs/runbooks/F-007-legacy-archive-docker-baseline.md`，补充对 `docs/docker-SOP.md` 的引用，并修正 `/api/status` 在容器 baseline 下的预期：以 `configExists=true`、`gatewayHealthy=true`、`runtimeMode=gateway-fallback` 为主，不再把 `installed=true` 当成唯一成功标志。
  - 更新 `longrun/workspaces/opensparrow-unified/feature_list.json` 的 F-007 文档证据描述，纳入 `docs/docker-SOP.md`。
- Validation notes:
  - 本轮未修改 `Dockerfile`、`docker-compose.yml` 或任何运行时代码，只对文档与 longrun 跟踪做同步。
  - SOP 中的端口、卷、环境变量、healthcheck 与 `deploy/docker/*` 当前定义逐项核对一致。
  - `/api/status` 的健康语义已按 `ui/server.mjs` 当前实现校对。
- Current status:
  - Docker baseline 的运维交接文档已补齐。
  - `docs/` 下现有 Docker runbook 与新 SOP 口径一致。
- Next feature candidate:
  - 继续做容器 baseline 的真实宿主验证补证；或回到 native wrapper / build-export 的下一轮收口。
- Progress snapshot: 9/9 passing（F-007 doc ops handoff complete）

## Session 6 (F-010 / spec-009 CI/CD Pipeline Baseline)
- Date: 2026-03-24
- Goal: 为统一真源仓补齐第一版 GitHub Actions CI，覆盖 shell 语法、Node 语法、Docker compose 渲染和仓库健康检查。
- Selected feature id: `F-010` / spec-009
- Changes made:
  - 新增 `specs/009-ci-cd-pipeline/`，包含 `spec.md`、`plan.md`、`tasks.md`。
  - 新增 `.github/workflows/ci.yml`，定义 `shell-lint`、`node-check`、`docker-validate`、`repo-health` 四个 job，触发条件为 `push` to `main` 与 `pull_request`。
  - 新增 `docs/runbooks/F-010-ci-cd-pipeline.md`，说明 CI 触发条件、job 职责、本地等价命令和故障排查。
  - 更新 `longrun/workspaces/opensparrow-unified/feature_list.json`，新增 `F-010`。
- Validation highlights:
  - 已执行 workflow 对应的本地等价命令：shell `bash -n`、`node --check ui/server.mjs`、`docker compose -f deploy/docker/docker-compose.yml config`、`chmod +x scripts/verify-legacy-freeze.sh && bash scripts/verify-legacy-freeze.sh`、`python3 -m json.tool ...`。
  - 若本机存在 PyYAML，则执行 `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` 做 YAML 解析校验。
- Current status:
  - 当前 main 基线已有最小 CI 门槛。
  - 本轮未修改任何既有运行时代码，仅新增 spec/workflow/runbook 并同步 longrun 记录。
- Next feature candidate:
  - 后续可继续扩展 PowerShell、build/export、Docker build 或 smoke test CI，但需先补 spec/task。
- Progress snapshot: 10/10 passing


## Session 6 (F-011 Vendor Source Verification)
- Date: 2026-03-24
- Goal: 为 `vendor/` 三个平台运行时补齐来源清单、版本/架构探测证据，以及可重复执行的校验脚本。
- Selected feature id: `F-011`
- Changes made:
  - 新增 `docs/vendor-source-inventory.md`，记录 macOS / Linux / Windows vendor runtime 的 Node 版本、架构、官方来源 URL、上游归档 SHA256、以及本地二进制 SHA256
  - 新增 `scripts/verify-vendor.sh`，检查 vendor 目录存在性、关键文件、版本一致性、架构信息，并在存在 `vendor/checksums.sha256` 时自动做 checksum 校验
  - `longrun/workspaces/opensparrow-unified/feature_list.json` 已新增 F-011 条目
- Probe evidence:
  - `vendor/mac-openclaw/bin/node --version` → `v24.14.0`
  - `strings vendor/linux-openclaw/bin/node` → `v24.14.0`
  - `strings vendor/windows-openclaw/node.exe` → `v24.14.0`
  - `file vendor/mac-openclaw/bin/node` → `Mach-O 64-bit executable x86_64`
  - `file vendor/linux-openclaw/bin/node` → `ELF 64-bit ... x86-64`
  - `file vendor/windows-openclaw/node.exe` → `PE32+ ... x86-64`
- Upstream checksum evidence:
  - `node-v24.14.0-darwin-x64.tar.gz` → `f2879eb810e25993a0578e5d878930266fd2eafcffe9f2839b3d8db354d4879e`
  - `node-v24.14.0-linux-x64.tar.xz` → `41cd79bb7877c81605a9e68ec4c91547774f46a40c67a17e34d7179ef11729df`
  - `node-v24.14.0-win-x64.zip` → `313fa40c0d7b18575821de8cb17483031fe07d95de5994f6f435f3b345f85c66`
- Regression checks:
  - `bash -n scripts/verify-vendor.sh`
  - `bash scripts/verify-vendor.sh` → `PASS=22 WARN=1 FAIL=0`
  - warning only: `vendor/checksums.sha256 not found; checksum manifest verification skipped`
- Known risks:
  - 当前仓内仍未保留原始 vendor 归档文件，也未保存 `vendor/checksums.sha256`
  - 文档中的上游 URL / SHA256 是基于本地版本与架构对应到 Node.js 官方发布归档，不是仓内本地压缩包摘要
- Next follow-up:
  - 后续可补 `vendor/checksums.sha256`，把当前仓内实际保存的关键二进制摘要纳入校验流程

## Session 7 (F-012 OpenClaw Runtime Upgrade)
- Date: 2026-03-25
- Goal: 升级 vendor 内 OpenClaw 运行时至 v2026.3.23，适配 gateway.auth.mode 和 DEFAULT_MODEL 破坏性变更
- Selected feature id: `F-012`
- Changes made:
  - 升级 `vendor/mac-openclaw/` openclaw 至 2026.3.23
  - 升级 `vendor/linux-openclaw/` openclaw 至 2026.3.23
  - `ui/server.mjs`: 新增 DEFAULT_MODEL 常量（line 72），替换 4 处硬编码 gpt-4o-mini
  - `ui/server.mjs`: 新增 gateway.auth.mode 显式配置（line 1656-1663）
  - `platforms/windows/wrappers/one-click-deploy.ps1`: 提取 DEFAULT_MODEL 为环境变量（line 355）
  - 新增 `docs/runbooks/F-012-openclaw-runtime-upgrade.md`
  - `longrun/workspaces/opensparrow-unified/feature_list.json` 新增 F-012 条目
- Validation:
  - `node --check ui/server.mjs` → PASS
  - `bash -n scripts/openclaw-usb/*.sh` → PASS
  - `bash -n deploy/docker/bin/*.sh` → PASS
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
  - Mac vendor: v2026.3.23
  - Linux vendor: v2026.3.23
- Pending:
  - Windows vendor 升级需要 Windows 环境（标记为待办）
  - ClawHub-first 插件验证需要升级后的 openclaw CLI
- Risk: high (破坏性变更 + 安全修复)

## Session 8 (F-015 Skill Store source/dist sync)
- Date: 2026-03-31
- Goal: 修复 Skill Store“源码已修但打包页面仍空白”的收口问题，并校准已安装统计与分类展示。
- Selected feature id: `F-015`
- Root cause:
  - 仓库根目录的 `ui/server.mjs` 已经修到支持 `skills/My_Skills/<category>/<skill>/SKILL.md` 三层结构，但实际在浏览器里跑的是旧版 `dist/usb-pack/opensparrow-0.1.0-alpha/ui/server.mjs`，它仍按“分类目录下直接是文件”的旧逻辑扫描，所以 `/api/skills/list` 返回 `total=0`。
  - 浏览器硬刷新只能刷新静态页面，不能把旧打包产物中的服务端代码替换成新逻辑；因此必须重建 `dist`。
  - Dashboard 同时还把后端返回的 `installedCount` 误读成 `installed`，导致“已安装”统计在刷新后永远不准。
- Changes made:
  - `ui/server.mjs`：为 Skill Store 分类返回补齐 `label`/`description` 元数据，并把 `installedCount` 修正为“按已安装 skill 目录计数”而不是按分类目录下散文件计数。
  - `ui/public/dashboard.html`：改为读取 `installedCount`；分类下拉改为根据 `/api/skills/list` 返回的分类动态构建，并在分类失效时自动回退到“全部分类”。
  - `docs/runbooks/release-process.md`：补充 Skill Store 变更后的发布提醒——修完源码后必须重建 `dist`，并实际探测组包后的 `/api/skills/list`。
  - 重新执行 `bash scripts/build-usb-pack.sh --platform all`，重建 `dist/usb-pack/opensparrow-0.1.0-alpha/`。
  - `longrun/workspaces/opensparrow-unified/feature_list.json`：新增 `F-015` 条目。
- Validation:
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
  - `node --check ui/server.mjs` → PASS
  - 从 `ui/public/dashboard.html` 抽取内联 `<script>` 后执行 `node --check` → PASS
  - 源码服务：`OPENSPARROW_AUTO_OPEN=0 node ui/server.mjs`，随后请求 `/api/skills/list?page=1&pageSize=3` → `total=60300`
  - 组包服务：`cd dist/usb-pack/opensparrow-0.1.0-alpha && OPENSPARROW_AUTO_OPEN=0 node ui/server.mjs`，随后请求 `/api/skills/list?page=1&pageSize=3` → `total=60300`
  - 隔离 HOME 冒烟：`HOME=/tmp/opensparrow-skilltest-home OPENSPARROW_AUTO_OPEN=0 node ui/server.mjs` 后，安装/卸载 `Business/2d-games` 验证 `installedCount` 从 `0 → 1 → 0`，`installed` 从 `false → true → false`
- Current status:
  - Skill Store 在源码服务和重建后的 `dist` 包里都能返回完整技能列表。
  - “已安装”统计和分类标签现在与后端返回一致。
- Next follow-up:
  - 如需浏览器级 UI 证据，可补一轮 Playwright 截图或冒烟脚本（当前机器尚未安装 Playwright 浏览器运行时）。
- Progress snapshot: 14/15 passing

## Session 9 (F-016 Windows home path portability)
- Date: 2026-03-31
- Goal: 去掉 Windows one-click 部署对 `C:\Users` 的根路径假设，避免 VM / 域用户 / 重定向用户目录环境下历史 profile 发现失效。
- Selected feature id: `F-016`
- Root cause:
  - `platforms/windows/wrappers/one-click-deploy.ps1` 原先在 `Resolve-OpenClawHome()` 中直接扫描 `C:\Users` 查找 `.openclaw-<profile>\openclaw.json`。
  - 这虽然不是绑定到某个具体用户名，但仍然假设了“所有用户目录都挂在 `C:\Users`”，在企业域、用户目录迁移、非标准系统盘或某些虚拟机镜像中都有风险。
- Changes made:
  - `platforms/windows/wrappers/one-click-deploy.ps1`：新增 `Get-UserHomeCandidates()`，优先从 `HOME`、`USERPROFILE`、`HOMEDRIVE` + `HOMEPATH` 生成候选目录，并使用 `Win32_UserProfile` 枚举可用用户目录去重补充。
  - `Resolve-OpenClawHome()` 改为遍历候选 home 目录寻找历史 profile，不再写死 `C:\Users`。
  - `docs/usb-pack/windows-native-delivery.md`：补充路径兼容性说明，明确 one-click 已改为环境变量 + Windows 用户配置 API 发现历史 profile。
  - `longrun/workspaces/opensparrow-unified/feature_list.json`：新增 `F-016` 条目。
- Validation:
  - `grep -F 'C:\Users' platforms/windows/wrappers/one-click-deploy.ps1` → 无结果
  - 本机无 `pwsh` / `powershell` 可执行文件，因此本轮无法在 macOS 上做 PowerShell 语法解析；已改为最小范围静态审查 + 差异核对，并保留后续在 Windows VM 上补 `pwsh` 解析 / 实机执行。
  - `./longrun/workspaces/opensparrow-unified/init.sh` 仍通过（未影响 shell / node 验证链）。
- Current status:
  - Windows one-click 历史配置发现逻辑已不依赖固定的 `C:\Users` 根目录。
  - 当前剩余建议是在 Windows VM 上补一轮 `one-click-deploy.ps1` 实机验证，确认提权/非标准 home 环境下仍能正确命中历史 profile。
- Next follow-up:
  - 在 Windows VM 中执行 `platforms\windows\wrappers\one-click-deploy.ps1`，分别验证当前用户 HOME、提权启动、以及历史 profile 自动发现三种场景。
- Progress snapshot: 15/16 passing

## Session 10 (F-017 Dashboard logo static asset hardening)
- Date: 2026-04-03
- Goal: 修复 dashboard 顶部 logo 在 HTTP 场景下空白的问题，并把 UI 公共静态资源服务能力收口到统一实现。
- Selected feature id: `F-017`
- Root cause:
  - `ui/public/dashboard.html` 原先把 logo 以内联 `data:image/png;base64,...` 写在单行属性里，浏览器侧存在 URL 解析失败风险。
  - 当 logo 改成相对路径 `logo.png` 后，`ui/server.mjs` 的静态路由仍只放行 `.css/.js/.mjs`，导致 `GET /logo.png` 无法返回。
  - 静态路由白名单与 `sendFile()` 的 MIME 支持分叉，后续新增 `.png/.svg/.ico/.json` 或子目录资源时容易重复出错。
- Changes made:
  - `ui/public/logo.png`：从 dashboard 原始 base64 数据提取为独立 PNG 文件。
  - `ui/public/dashboard.html`：仅将顶部 logo 的 `src` 改为 `logo.png`，其余属性保持不变。
  - `ui/server.mjs`：新增 `PUBLIC_FILE_MIME_MAP` / `PUBLIC_ASSET_EXTENSIONS`，把静态扩展名与 MIME 映射统一到同一份定义。
  - `ui/server.mjs`：新增 `resolvePublicAssetPath()` 与 `isPathInside()`，支持 `ui/public/` 下安全的相对路径解析、真实路径校验与路径穿越防护。
  - `ui/server.mjs`：静态资源请求从“仅 basename + css/js/mjs”升级为统一的 `GET/HEAD` 公共资源服务。
  - `longrun/workspaces/opensparrow-unified/feature_list.json`：新增 `F-017` 条目。
- Validation:
  - `node --check ui/server.mjs` → PASS
  - `file ui/public/logo.png` → `PNG image data, 115 x 120, 8-bit/color RGBA, non-interlaced`
  - `curl -I http://127.0.0.1:19111/logo.png` → `200 OK`, `Content-Type: image/png`
  - `curl -I http://127.0.0.1:19111/dashboard` → `200 OK`
  - `curl http://127.0.0.1:19111/dashboard` 命中 `<img src="logo.png" ... alt="Open Sparrow">`
  - `curl http://127.0.0.1:19111/../package.json` → `404`（路径穿越拦截）
  - Headless Chrome 直接打开 `file:///Users/eduardogan/Desktop/GHJProject/opensparrow/ui/public/dashboard.html`：`currentSrc=file:///.../ui/public/logo.png`，`naturalWidth=115`，`naturalHeight=120`
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
- Current status:
  - dashboard logo 在源码直开与 HTTP 静态服务两条路径上都能被正确解析。
  - UI 公共静态资源服务已改为统一收口实现，后续新增图片、图标、JSON 或子目录资源不需要再单独补路由。
- Next follow-up:
  - 若后续需要分发新版交付包，应同步重建 `dist`，避免源码与打包产物的静态资源能力再次漂移。
- Progress snapshot: 16/17 passing

## Session 11 (Bug investigation: custom OpenAI endpoint compatibility + model drift)
- Date: 2026-04-08
- Goal: 只读定位“自定义 OpenAI 兼容地址 + Qwen3-32B”链路中的 404/500 报错，并把模型错位问题落盘为 bug 记录。
- Scope: 调查与文档落盘；本轮未修改运行时代码。
- Findings:
  - `ui/server.mjs` 的 `normalizeOpenAIBaseUrl()` 对 `https://llm.wtsht.cn/mosuanguichao/qwen3-32b-api` 不会自动补 `/v1`。
  - 实测同一 provider 仅支持 `/v1/chat/completions`、`/v1/completions` 与 `/v1/chat/completions?stream=true`，不支持裸路径 `chat/completions`，也不完整支持 `/v1/responses`。
  - 真实会话日志同时出现过 `openai-completions -> 404 status code (no body)` 与 `openai-responses -> 500 not implemented`。
  - Dashboard 保存 API 的后端路径只更新 `models.providers.openai`，不会同步执行 `models set`，导致 `/api/config` 表单配置与 `openclaw models list` 当前激活模型发生漂移。
- Evidence recorded:
  - 新增 `specs/010-openclaw-runtime-upgrade/BUG-001-custom-openai-endpoint-and-model-drift.md`
- Current status:
  - 该问题已形成可复现 bug 记录，尚未进入修复实现。
- Suggested next step:
  - 如需修复，优先做：`/api/config/api` 同步 `models set`，以及 install/save 前的 provider 兼容性预检。

## Session 12 (F-018 OpenAI-compatible provider hardening)
- Date: 2026-04-08
- Goal: 以最小改动修复自定义 OpenAI-compatible provider 的 `/v1` 路由兼容、provider API 默认值和 Dashboard 模型漂移问题。
- Selected feature id: `F-018`
- Root cause:
  - OpenClaw runtime 在 provider 级 `api` 缺失时会回退到 `openai-responses`，而当前安装/保存逻辑只写了 model 级 `api`。
  - Dashboard `POST /api/config/api` 只更新 `models.providers.openai`，不会同步执行 `models set`，导致表单配置与实际激活模型漂移。
  - 对自定义非根路径 provider，当前 UI 归一化逻辑不会在需要时自动收敛到带 `/v1` 的可用 base URL。
- Changes made:
  - 新增 `specs/012-custom-openai-provider-hardening/{spec,plan,tasks}.md`，补齐本轮 bugfix 的 spec 流程。
  - 新增 `ui/lib/openai-provider.mjs` 与 `ui/lib/openai-provider.test.mjs`，抽出 OpenAI base URL 归一化、`/v1` 候选生成、provider config builder，并补了 5 个聚焦测试。
  - `ui/server.mjs`：安装与 Dashboard API 保存统一走 OpenAI provider helper，写入 provider 级 `api: "openai-completions"`，并在保存后同步 `models set`。
  - `ui/server.mjs`：新增保守型 provider probe；当原始自定义路径的 `chat/completions` 不可达而 `/v1` 候选可达时，自动切换到带 `/v1` 的 base URL，并把兼容性告警透传给前端。
  - `ui/server.mjs`：Dashboard 保存 API 时，空 `apiKey` 不再覆盖已有 auth profile；仅在用户明确填写新 key 时才重写 `auth-profiles.json`。
  - `ui/public/dashboard.html`：保存 API 成功 toast 现在会拼接后端返回的 warnings。
  - `scripts/openclaw-usb/install-local-feishu.sh` 与 `scripts/openclaw-usb/install-local-feishu.ps1`：同步写入 provider 级 `api: "openai-completions"`。
- Validation:
  - `node --test ui/lib/openai-provider.test.mjs` → PASS (5/5)
  - `node --check ui/server.mjs` → PASS
  - `bash -n scripts/openclaw-usb/install-local-feishu.sh` → PASS
  - 隔离 smoke：`OPENCLAW_HOME=$(mktemp -d) ... POST /api/config/api` 后，`openclaw.json` 已包含 provider 顶层 `api: "openai-completions"`，且 `openclaw models list` 显示 `openai/Qwen3-32B ... default,configured`
  - `pwsh` 当前环境不可用，因此 `install-local-feishu.ps1` 仅完成静态差异审查，待 Windows / pwsh 环境补语法解析。
- Current status:
  - 安装与 Dashboard API 保存路径都已固定 provider 级 `openai-completions`，并在保存后同步当前激活模型。
  - 自定义 OpenAI-compatible base URL 现在具备保守型 `/v1` 探测与 warnings 透传能力。
- Next follow-up:
  - 用真实第三方 provider 再做一次端到端 smoke，确认 `https://llm.wtsht.cn/mosuanguichao/qwen3-32b-api` 在 UI 保存后最终落为 `.../v1`，并验证聊天链路恢复。
- Progress snapshot: 18/18 passing

## Session 13 (F-019 Commander orchestration governance)
- Date: 2026-04-09
- Goal: 把“总司令 + 固定 5 子 Agent + 可临时扩编”的长期协作制度正式落盘到 unified repo，作为后续会话默认执行基线。
- Selected feature id: `F-019`
- Scope:
  - 本轮只做治理文档、runbook 与 longrun 记录同步。
  - 本轮未修改 `ui/`、`platforms/`、`scripts/`、`vendor/` 等运行时代码。
- Changes made:
  - 新增 `specs/013-commander-orchestration-governance/{spec,plan,tasks}.md`，明确固定 5 人编制、总司令职责边界、默认派工模式、扩编规则与冲突规避要求。
  - 新增 `docs/runbooks/F-019-commander-orchestration-governance.md`，把角色职责卡、派单模板、总司令 SOP、收口动作与升级路径写成可执行手册。
  - 更新 `longrun/workspaces/opensparrow-unified/feature_list.json`，新增 `F-019` 条目。
  - 补充“总司令是会话级角色，不绑定历史特定 Agent 身份；若用户重新指定，以当前用户指令为准”的口径，避免旧上下文对新会话造成误导。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS
- Current status:
  - unified repo 已拥有一套长期固定的 Commander 调度制度，可直接指导后续 feature 的拆解、派工、审阅与收口。
  - 后续新任务默认沿用“固定 5 人 + 按需临时扩编”的工作方式，除非用户另行指定。
- Next follow-up:
  - 下一轮真实 feature 开工时，按本 runbook 执行一次完整派工，验证这套制度在实战中的节奏与边界是否仍需微调。
- Progress snapshot: 18/19 passing

## Session 14 (F-020 Mac arm64 installer hardening)
- Date: 2026-04-14
- Goal: 修复 macOS arm64 UI 包首次安装时的 skill 复制失败与 DingTalk/WeCom 插件安装级联报错，并重新导出唯一保留的最新好包。
- Selected feature id: `F-020`
- Root cause:
  - `ui/server.mjs` 的 `installSkills()` 把 `skills/superpowers/` 顶层条目一律当普通文件复制；当遇到 `agents/`、`commands/` 等目录时会触发 `copyfile ENOTSUP`，导致 `~/.claude/skills/superpowers` / `~/.codex/skills/superpowers` 安装中断。
  - UI 安装流程对 DingTalk / WeCom 仍调用 bare npm spec，OpenClaw 2026.3.23 会先尝试 ClawHub；当 ClawHub 返回 `429` 时不会回退 npm，因此插件安装失败。
  - 即使插件安装失败，旧逻辑仍继续写 `channels.dingtalk/*` / `channels.wecom/*`，于是又触发 `unknown channel id` 二次报错。
  - `ui/server.mjs` 的 OpenClaw entry 解析顺序优先落到 `vendor/mac-openclaw/bin/node_modules/openclaw`（2026.3.12），而不是 `lib/node_modules/openclaw`（2026.3.23），存在继续跑旧版 runtime 代码的风险。
- Changes made:
  - 新增 `specs/014-mac-arm64-installer-hardening/{spec,plan,tasks}.md`，补齐这轮 bugfix 的 spec 流程。
  - 新增 `ui/install-helpers.mjs` 与 `ui/tests/install-helpers.test.mjs`，覆盖递归 skill 复制与 bundled plugin archive 发现逻辑。
  - `ui/server.mjs`：
    - `installSkills()` 改为递归复制 `skills/superpowers/`；
    - 优先从包内 `plugins/*.tgz` 安装 DingTalk / WeCom，而非直接走 ClawHub-first 解析；
    - 当 DingTalk / WeCom 插件未就绪时，跳过对应 `channels.*` 配置写入，避免 `unknown channel id` 级联报错；
    - `handleUpdateChannel()` 同步复用插件就绪检查；
    - `resolveOpenClawEntry()` 调整为优先选择 `lib/node_modules/openclaw/openclaw.mjs`；
    - `OC_TIMEOUT.PLUGIN_INSTALL` 从 `180000` 提升到 `300000`，覆盖企微本地归档安装耗时。
  - `scripts/build-usb-pack.sh`：
    - 补齐 `superpowers/ -> skills/superpowers/` 的 canonical 复制；
    - 新增 `bundle_plugin_archives()`，把 `@openclaw-china/channels` 与 `@sunnoy/wecom@3.0.0` 打成 `.tgz` 放入包内 `plugins/`。
  - 更新 `docs/runbooks/release-process.md` 与 `docs/release-checklist.md`，把本地 `.tgz` 插件归档与“不得出现 unknown channel id 级联报错”写入发布核对项。
  - 重建并收口最终交付物：仅保留 `dist/handoff/opensparrow-mac-ui-full-arm64-20260414-162747/` 与 `dist/handoff/opensparrow-mac-ui-full-arm64-20260414-162747.zip`。
- Validation:
  - `vendor/mac-openclaw/bin/node --test ui/tests/install-helpers.test.mjs` → PASS (2/2)
  - `vendor/mac-openclaw/bin/node --check ui/server.mjs` → PASS
  - `bash -n scripts/build-usb-pack.sh` → PASS
  - DingTalk local archive smoke：本地 `openclaw-china-channels-2026.3.29.tgz` 安装成功，随后 `config set channels.dingtalk.enabled true --strict-json` → PASS
  - WeCom local archive smoke：本地 `sunnoy-wecom-3.0.0.tgz` 安装成功，随后 `config set channels.wecom.enabled true --strict-json` → PASS
  - Final package checks：
    - `file dist/handoff/opensparrow-mac-ui-full-arm64-20260414-162747/opensparrow-0.1.0-alpha-mac-ui-arm64/vendor/mac-openclaw/bin/node` → `Mach-O 64-bit executable arm64`
    - final package launch smoke on port `19128` → `FINAL_ENTRY_SMOKE=PASS`
- Current status:
  - 新 mac 上的两条核心失败链路都已被收口：superpowers 目录型 skill 可正常复制；DingTalk / WeCom 不再依赖 ClawHub 首次解析成功，也不会在插件失败时继续写无效 channel config。
  - 当前 `dist/handoff/` 下仅保留唯一最新的 mac arm64 UI 交付物，便于直接迁移。
- Next follow-up:
  - 在真实目标 Mac 上用最终 ZIP 再做一次带企微凭据的完整 UI 安装，观察企微插件安装实际耗时是否稳定低于 300 秒；若仍接近上限，可考虑把插件依赖进一步预热到离线缓存层。
- Progress snapshot: 19/20 passing

## Session 15 (F-021 codeSPEC template ingestion)
- Date: 2026-04-14
- Goal: 参考 `/Users/eduardogan/Desktop/GHJProject/codeSPEC`，把适合当前 unified repo 清洗/重整使用的模板资产安全迁入当前仓，同时明确 reference 层与本地 authority 层边界，且不修改 source 项目。
- Selected feature id: `F-021`
- Scope:
  - 本轮新增一个可重复执行的 `codeSPEC` 模板导入脚本与自动化测试。
  - 本轮建立 `docs/reference/codeSPEC-template/` 作为 reference 镜像层，仅镜像策展后的 `UnifiedFramework` 白名单文件。
  - 本轮把 `codeSPEC` 中更清晰的 `longrun` 边界说明吸收到当前仓活跃文档。
  - 本轮未修改 `codeSPEC` source 项目，也未改动 OpenSparrow 运行时代码行为。
- Changes made:
  - 新增 `specs/015-codespec-template-ingestion/{spec,plan,tasks}.md`，定义导入边界、白名单、跳过清单与验证链。
  - 新增 `scripts/tests/import-codespec-template.test.mjs`，以 TDD 方式覆盖“只同步白名单文件、且不覆盖本地 reference 说明文档”两项行为。
  - 新增 `scripts/import-codespec-template.sh`，默认从 `/Users/eduardogan/Desktop/GHJProject/codeSPEC` 读取 source，并重建 `docs/reference/codeSPEC-template/upstream/`。
  - 新增 `docs/reference/codeSPEC-template/README.md` 与 `docs/reference/codeSPEC-template/IMPORT_SCOPE.md`，说明 reference 定位、映射关系、跳过项与刷新命令。
  - 运行导入脚本后，生成 `docs/reference/codeSPEC-template/upstream/UnifiedFramework/` 与 `docs/reference/codeSPEC-template/upstream/IMPORT_MANIFEST.md`。
  - 新增 `docs/runbooks/F-021-codespec-template-ingestion.md`，记录刷新动作、验证命令与风险边界。
  - 更新 `README.md`，增加 `codeSPEC` 模板参考入口。
  - 更新 `longrun/README.md`、`longrun/CHECKLIST.md`、`longrun/METHOD.zh-CN.md`，明确 `longrun` 是项目记忆层而非 feature / execution authority。
  - 更新 `longrun/workspaces/opensparrow-unified/feature_list.json`，新增 `F-021` 条目并标记通过。
- Validation:
  - `node --test scripts/tests/import-codespec-template.test.mjs` → PASS (2/2)
  - `bash -n scripts/import-codespec-template.sh` → PASS
  - `./scripts/import-codespec-template.sh /Users/eduardogan/Desktop/GHJProject/codeSPEC` → PASS
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS (`20/21` passing)
- Current status:
  - 当前仓已经拥有独立的 `codeSPEC` 参考镜像层，可直接支持后续清洗 / 重整工作，而不会把 source 项目 authoring 面或实验残留混进统一仓权威面。
  - `longrun` 的边界在当前仓已被明确收紧：它只承接项目事实与 workspace contract，不再与 `specs/` 或 `superpowers` 争夺 authority。
- Next follow-up:
  - 如后续需要继续吸收 `codeSPEC/AgentTeam` 的 onboarding / dispatch 模板，建议单独新开 feature，在当前仓语境下本地化改写后再迁入，避免与 `F-019` 治理基线形成双轨。
- Progress snapshot: 20/21 passing

## Session 16 (F-022 codeSPEC framework full adaptation)
- Date: 2026-04-14
- Goal: 把 `codeSPEC` 从“上游参考模板”升级为 OpenSparrow 当前仓正在使用的底层开发框架来源，完成治理面、本地 authority wiring、longrun scaffold 与 reference import 的适配闭环。
- Selected feature id: `F-022`
- Scope:
  - 本轮新增当前仓活跃治理文档目录 `docs/governance/`。
  - 本轮把 `AgentTeam` 的 onboarding / dispatch / quick-reference 模板本地化改写为 OpenSparrow 语境。
  - 本轮继续扩展 `codeSPEC` reference import，让 `AgentTeam` 高价值模板与 `UnifiedFramework/12-Superpower-Execution-Bridge.md` 一并被镜像。
  - 本轮更新 `AGENTS.md`、constitution、`README.md`、`docs/项目持久化说明.md`、`longrun/templates/*prompt*`，统一 authority order。
  - 本轮未修改 `codeSPEC` source 项目，也未修改 OpenSparrow 运行时代码行为。
- Changes made:
  - 新增 `specs/016-codespec-framework-adaptation/{spec,plan,tasks}.md`，明确本轮是“底层框架适配完成”，而非仅做 reference 镜像。
  - 新增 `docs/governance/README.md`、`docs/governance/framework-stack.md`、`docs/governance/project-onboarding-sop.md`、`docs/governance/dispatch-templates.md`、`docs/governance/quick-reference.md`。
  - 新增 `docs/runbooks/F-022-codespec-framework-adaptation.md`，记录底层框架适配的使用方式与验证命令。
  - 更新 `AGENTS.md`，把治理入口加入必读清单，并显式声明当前底层框架五层结构。
  - 更新 `.specify/memory/constitution.md`，新增“框架栈与 Authority Order”章节。
  - 更新 `README.md` 与 `docs/项目持久化说明.md`，把治理入口挂入仓库首页与项目持久化说明。
  - 更新 `longrun/templates/initializer_prompt.template.md` 与 `longrun/templates/coding_prompt.template.md`，把它们收紧为 project-memory compatibility scaffold。
  - 扩展 `scripts/tests/import-codespec-template.test.mjs` 与 `scripts/import-codespec-template.sh`，新增 `AgentTeam/{README,02,03,04}` 和 `UnifiedFramework/12-Superpower-Execution-Bridge.md` 的白名单镜像。
  - 更新 `docs/reference/codeSPEC-template/README.md` 与 `IMPORT_SCOPE.md`，同步新的 reference 范围与治理映射关系。
  - 运行真实导入后，`docs/reference/codeSPEC-template/upstream/` 现已包含 `UnifiedFramework/` + `AgentTeam/` 两块白名单快照。
  - 清理了上一步卡住脚本误生成的空壳目录 `specs/017-codespec-framework-adaptation/`，避免污染 feature 序列。
  - 更新 `longrun/workspaces/opensparrow-unified/feature_list.json`，新增 `F-022` 条目并标记通过。
- Validation:
  - `node --test scripts/tests/import-codespec-template.test.mjs` → PASS (2/2)
  - `bash -n scripts/import-codespec-template.sh` → PASS
  - `./scripts/import-codespec-template.sh /Users/eduardogan/Desktop/GHJProject/codeSPEC` → PASS（13 个 curated files）
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `./longrun/workspaces/opensparrow-unified/init.sh` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`21/22` passing）
- Current status:
  - 当前仓已经具备可直接使用的底层开发框架：规则层、feature 交付层、项目记忆层、执行方法层、协作治理层已全部显式接线。
  - `codeSPEC` 现在在 OpenSparrow 中既有上游 reference 镜像，也有本地化后的活跃治理文档与 scaffold，对后续产品改造已经足够作为开发底座使用。
- Next follow-up:
  - 下一轮可以不再继续做框架迁移，直接基于这套底座选定一个产品 feature，开始“优雅化”改造当前项目；建议优先从 `F-014` 或一个新的 UI / 配置收敛 feature 开始。
- Progress snapshot: 21/22 passing

## Session 17 (F-014 WeCom channel integration hardening)
- Date: 2026-04-14
- Goal: 以当前已适配好的框架底座为起点，优先收口 unified repo 唯一未通过项 `F-014`，先把企业微信渠道从“半落地、难维护”推进到“有 spec、有测试、有 runbook、代码结构更优雅”的状态。
- Selected feature id: `F-014`
- Scope:
  - 本轮补齐 `specs/011-wecom-channel-integration/{spec,plan,tasks}.md`。
  - 本轮把企微纯逻辑从 `ui/server.mjs` 提取到独立 helper 模块并补 node:test。
  - 本轮新增企微 runbook，明确自动验证范围与真实凭据 E2E 待补边界。
  - 本轮不伪造真实企业微信凭据 E2E 通过证据。
- Changes made:
  - 进一步优雅化浏览器端企微逻辑：新增 `ui/public/wecom-helpers.js` 与 `ui/public/wecom-helpers.test.mjs`，让安装向导与 Dashboard 共用同一套企微字段校验规则。
  - 覆盖 `specs/011-wecom-channel-integration/spec.md`，并新增 `plan.md`、`tasks.md`，把 `F-014` 从空模板状态补成可执行 feature。
  - 新增 `ui/lib/wecom.mjs`，集中承载 `isLikelyWecomBotId`、`normalizeWecomCredentials`、`collectWecomInputErrors`、`enrichWecomChannelForUi`。
  - 新增 `ui/lib/wecom.test.mjs`，通过 TDD 覆盖 Bot ID 判断、字段归一化、输入校验和 UI 扁平化。
  - 更新 `ui/server.mjs`，改为导入 `ui/lib/wecom.mjs`，不再在超长 server 文件内内联企微纯逻辑。
  - 新增 `docs/runbooks/F-014-wecom-channel-integration.md`，区分“已自动验证链路”与“仍需真实企业微信凭据补证的 E2E”。
- Validation:
  - `node --test ui/lib/wecom.test.mjs` → PASS (4/4)
  - `node --test ui/public/wecom-helpers.test.mjs` → PASS (2/2)
  - `node --check ui/server.mjs` → PASS
  - 既有企微插件本地归档链路证据继续有效：`sunnoy-wecom-3.0.0.tgz` 安装成功且 `config set channels.wecom.enabled true --strict-json` 曾在 F-020 收口中验证通过
- Current status:
  - `F-014` 的代码结构与文档完整度已经明显提升，后续再做企微真机 / 真凭据 E2E 时，不需要继续在 `ui/server.mjs` 中盲改。
  - 当前仍缺少“带真实企业微信凭据的一次完整 UI 安装 + 消息/回调链路”证据，因此本轮不把 `F-014` 改为通过。
- Next follow-up:
  - 准备一套真实企业微信 Bot ID / Secret（以及如需回调的 CorpId / CorpSecret / AgentId / Callback 参数），按 `docs/runbooks/F-014-wecom-channel-integration.md` 进行人工补证；证据齐全后再把 `F-014` 写成 passing。
- Progress snapshot: 21/22 passing

## Session 18 (F-014 WeCom browser dedupe follow-up)
- Date: 2026-04-14
- Goal: 在不扩大回归面的前提下，继续把 `F-014` 浏览器端企微表单默认值与 helper 调用收敛得更优雅，减少安装向导 / Dashboard 间的薄重复。
- Selected feature id: `F-014`
- Scope:
  - 本轮只处理浏览器端企微默认字段工厂与 helper 调用去重。
  - 本轮不改 `F-014` 的验收口径，也不伪造真实企业微信 E2E 通过证据。
- Changes made:
  - 更新 `ui/public/wecom-helpers.js`，新增 `createEmptyWecomFields()`，统一产出企微表单默认字段结构。
  - 更新 `ui/public/index.html`，改为使用 `window.OpenSparrowWecom.createEmptyWecomFields()` 初始化 `credentials.wecom`，并直接调用共享 helper，不再保留页面内薄包装方法。
  - 更新 `ui/public/dashboard.html`，改为使用 `window.OpenSparrowWecom.createEmptyWecomFields()` 初始化企微卡片字段，并直接调用共享 helper，不再保留页面内薄包装方法。
  - 更新 `ui/public/wecom-helpers.test.mjs`，补充“默认字段工厂返回新对象”的测试，确保不会把 Alpine 状态变成共享引用。
  - 更新 `specs/011-wecom-channel-integration/tasks.md`，把当前 hardening 范围内已完成的任务与验证项勾选落盘。
- Validation:
  - `node --test ui/public/wecom-helpers.test.mjs` → PASS (3/3)
  - `node --test ui/lib/wecom.test.mjs` → PASS (4/4)
  - `node --test ui/tests/install-helpers.test.mjs` → PASS (2/2)
  - `node --check ui/server.mjs` → PASS
- Current status:
  - 浏览器端企微默认字段结构与校验入口已经进一步统一，安装向导和 Dashboard 继续共用同一套 helper 契约，后续再做 UI 收敛时不需要重复维护默认字段字面量。
  - `F-014` 仍然缺少“真实企业微信凭据的一次完整 UI 安装 + 消息/回调链路”证据，因此本轮仍不把它改为 passing。
- Next follow-up:
  - 若拿到真实企业微信 Bot ID / Secret（以及如需回调的 CorpId / CorpSecret / AgentId / Callback 参数），按 `docs/runbooks/F-014-wecom-channel-integration.md` 走一次人工补证；补证完成后再更新 `feature_list.json` 的 `passes`。
  - 如果短期内拿不到真实凭据，下一轮可基于这套底座继续选择另一个可本地闭环的 UI / 配置优雅化 feature。
- Progress snapshot: 21/22 passing

## Session 19 (F-023 Commander Mode hardening verified closeout)
- Date: 2026-04-14
- Goal: 基于已经完成的 review + verification 证据，把 Commander Mode hardening 正式写回统一项目记忆层，并完成最小必要 closeout。
- Selected feature id: `F-023`
- Scope:
  - 本轮只做 verified closeout 写回：更新 `feature_list.json`、`claude-progress.txt`，并仅在证据充分时同步勾选 `specs/017-commander-mode-hardening/tasks.md`。
  - 本轮不改 `docs/governance/*`、不改运行时代码、不重写 spec / plan，也不新增验证结论。
- Evidence basis:
  - 本轮 close 直接依赖既有 verified evidence，而不是基于 summary 推断：`Batch Reviewer: APPROVED`、`Batch Verifier: PASS`。
  - 已验证范围限定为 `PKT-017-A/B/C/D`；关键落点包括 `.specify/memory/constitution.md:96`、`.specify/templates/plan-template.md:38`、`.specify/templates/plan-template.md:77`、`.specify/templates/plan-template.md:85`、`longrun/templates/coding_prompt.template.md:9`、`longrun/templates/coding_prompt.template.md:14`、`longrun/templates/coding_prompt.template.md:47`、`longrun/templates/coding_prompt.template.md:84`。
- Packet closeout recorded:
  - `PKT-017-A`：`.specify/memory/constitution.md` 已明确 Commander Mode 激活条件、角色边界、Commander contract、ownership 串行化，以及 closeout / summary non-authoritative 边界。
  - `PKT-017-B`：`.specify/templates/plan-template.md` 已把 packet 结构固化为默认 dispatch scaffold，并模板化必填字段、Reviewer / Verifier 分离、Stop rule 与 integration packet 约束。
  - `PKT-017-C`：`longrun/templates/coding_prompt.template.md` 已明确 continuation scaffold 不能替代 `specs/` / `superpowers` / verified truth，Commander 默认不承担 implementation，`passes: true` 仍需 traceable acceptance evidence。
  - `PKT-017-D`：cross-surface wording integration 已有 review + verification 证据支撑，本轮无需继续 integration。
- Longrun updates:
  - 在 `feature_list.json` 新增 `F-023`，定位为对 `F-019` commander governance 的 hardening；`passes: true` 只因为已有 reviewer / verifier 通过证据支撑。
  - 在 `specs/017-commander-mode-hardening/tasks.md` 仅勾选被上述 review + verification 证据直接覆盖的实现 / review / verification / acceptance 项；未被证据覆盖的 scope freeze、assignment、integration-owner 过程项与 stop-rule 过程项保持未勾。
  - 明确保留 `F-014` 现状不变；本轮没有改写其状态。
- Why this round can close:
  - closeout 只是在 longrun 中索引“已被 spec / 文本落点 / reviewer / verifier evidence 支撑”的事实，不提升 authority，不覆盖 governance / spec / code / tests，也不把 longrun summary 升格为 truth。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS
  - `specs/017-commander-mode-hardening/tasks.md` 已做最小文本检查：close gate / checklist 结构保持完整
- Current status:
  - `Commander Mode hardening` 已完成 verified closeout 落账；当前结论来自既有 reviewer / verifier evidence，不来自 longrun summary 自我升级。
  - `F-014` 仍保持未通过；本轮未改动其验收状态。
- Next follow-up:
  - 若后续继续扩 Commander governance，应从 `specs/017-commander-mode-hardening/` 继续切 packet / spec 驱动推进，而不是把 longrun 当作 authority 面继续扩写。
- Progress snapshot: 22/23 passing


## Session 20 (F-024 unified channel contract baseline verified closeout)
- Date: 2026-04-15
- Goal: 基于已存在的 packet evidence 与 Commander acceptance gate，把 `F-024` 的 verified closeout 写回统一项目记忆层。
- Selected feature id: `F-024`
- Scope:
  - 本轮只做 longrun 写回：更新 `longrun/workspaces/opensparrow-unified/feature_list.json` 与 `longrun/workspaces/opensparrow-unified/claude-progress.txt`。
  - 本轮不改 `specs/024-*`、不改 `docs/*`、不改 `ui/*`，也不把 longrun 升格成 authority 或重写验收结论。
- Evidence basis:
  - 本轮 closeout 直接依赖既有 packet evidence 与 Commander acceptance gate，而不是 longrun 自我推断：`PKT-024-A` 已完成并通过、`PKT-024-B` 已通过、`PKT-024-C` 已通过、`PKT-024-D` 已通过、`PKT-024-E` 已通过，且 `Commander acceptance gate: PASS`。
  - 已确认的 closeout 事实限定为 baseline：truth table / mapping closure 已冻结，server canonical handling 已落地，shared frontend helper baseline 已落地，最小 docs / live-spec / tutorial / runbook wording sync 已完成，install / dashboard / read-back baseline closure 证据充分。
- Packet closeout recorded:
  - `PKT-024-A`：shared core / delta / alias truth table 与 install / dashboard / server / read-back mapping closure 已冻结，可作为 F-024 baseline 的可追溯 contract 依据。
  - `PKT-024-B`：server canonical handling 已作为 authoritative contract handling 落地，DingTalk `corpId` 保持 optional metadata，WeCom 保持 `botId + secret` core 与 nested persistence，Feishu 接入同层级 canonical boundary。
  - `PKT-024-C`：Install 与 Dashboard 已共用 shared frontend helper baseline，normalize / validate / payload build contract 收口到同一层，DingTalk `corpId` 不再被写成 browser install blocker。
  - `PKT-024-D`：最小直接受影响的 live-spec / tutorial / SOP / runbook wording 已同步到 baseline 口径，并明确当前 Dashboard 仍是三张渠道卡片 + modal 保存 + `enabled` toggle。
  - `PKT-024-E`：install / dashboard / read-back baseline closure 已有验证证据，Commander 已据此通过 acceptance gate；本包保持 verification-only closeout，不回灌新实现。
- Longrun updates:
  - 在 `feature_list.json` 新增 `F-024`，组件名为 `unified-channel-contract-baseline`，`passes: true` 只对应 baseline canonical contract / shared helper / docs wording / closure 已通过的事实。
  - 在 `claude-progress.txt` 追加本 session，明确 closeout 依据来自既有 packet evidence 与 Commander acceptance gate，而不是 longrun summary 自抬 authority。
  - 明确保留 `F-014` 现状不变；本轮没有改写它的 `passes`。
- Why this round can close:
  - `F-024` 本轮关的是 baseline，不是“统一收口全部完成”；longrun 这里只索引已经被 acceptance gate 通过的事实。
  - `F-024` 的 passing 不代表真实 E2E、Windows replay fidelity 或全 docs parity 已经完成。
- Explicit carryover to `F-025`:
  - 真实 E2E。
  - Windows replay fidelity。
  - 全 docs parity / broader docs parity。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`23/24 passing`、`Remaining: 1`、`Blocked by dependencies: 0`）
  - `tail -n 80 longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（文件尾部确认上一条为 `Session 19`，本轮按顺序追加为 `Session 20`）
  - `git diff -- longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（diff 仅覆盖本轮目标 longrun 文件）
- Current status:
  - `F-024` 已在 longrun 中完成 verified closeout 落账，但该结论仍从属于既有 spec / runbook / packet evidence / Commander acceptance gate。
  - `F-014` 仍保持未通过；下一战不是继续包装 F-024，而是按边界转入 `F-025`。
- Next follow-up:
  - 后续若推进 replay parity、真实 E2E 或 broader docs parity，应以 `F-025` 继续派工，而不是回头把 `F-024` 写成 full parity。
- Progress snapshot: 23/24 passing


## Session 21 (F-014 WeCom long-connection verified closeout)
- Date: 2026-04-15
- Goal: 基于既有自动验证证据与 Commander 已接受的 fresh operator evidence，把 `F-014` 正式写回 longrun，并完成 verified closeout。
- Selected feature id: `F-014`
- Scope:
  - 本轮只做 longrun 写回：`feature_list.json` + `claude-progress.txt`。
  - 本轮不改 code/spec/runbook，不新增实现，不重跑设计。
- Evidence basis:
  - 既有自动验证层：`node --test ui/lib/wecom.test.mjs`、`node --test ui/public/wecom-helpers.test.mjs`、`node --check ui/server.mjs` 已覆盖 bot-first、long-connection-first 冻结口径所需的 helper / server truth surface。
  - Commander 已接受的 fresh operator evidence（最小真实入口挂载）：当前运行态已挂载真实 `WeCom Bot ID + Secret`，`channels.wecom.enabled=true`，`botId_present=true`，`secret_present=true`，且 fresh `POST /api/wecom/probe` 显示 `status=ok`、`ready=true`、`daemon=running`，summary 显示 Enterprise WeChat `enabled, configured, running, works`。
  - Commander 已接受的 fresh operator evidence（真实消息往返）：企业微信私聊测试消息已被 OpenSparrow 实际收到，OpenSparrow 已实际回复，且企业微信客户端侧已实际看到回复；本轮证明的是 bot-first 主链真实消息闭环，不是 callback gate，也不是增强链路全量通过。
- Closeout recorded:
  - `F-014` 当前 bot-first 企业微信长连接主链已经具备 passing evidence，最小真实入口为 `Bot ID + Secret`。
  - UI / Dashboard / server / probe 已支持 bot-first 主链，而 fresh operator evidence 已证明真实入口挂载成功并完成至少一轮真实消息往返闭环。
  - callback / `CorpId` / `CorpSecret` / `AgentId` 仍属于增强链路 / 特定场景链路；本轮没有把它们写成默认 passing gate，也没有宣称它们全部通过。
- Why this round can close:
  - runbook 的两层 passing 条件现已齐备：既有自动验证层稳定 bot-first truth surface，Commander 接受的 fresh operator evidence 补齐最小真实入口与真实消息闭环。
  - longrun 这里只索引被 spec/runbook/evidence/Commander acceptance gate 支撑的事实，不把 longrun 升格为 authority。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`24/24 passing`、`Remaining: 0`、`Blocked by dependencies: 0`）
  - `tail -n 80 longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（文件尾部已出现新追加的 `Session 21`）
  - `git diff -- longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（diff 仅覆盖目标 pair）
- Current status:
  - `F-014` 已完成 verified closeout 写回。
  - 当前 unified workspace 进度已达到 `24/24 passing`。
- Next follow-up:
  - 如后续需要 callback / 自建应用增强链路或 broader parity，应另行派工；不要 retroactively 扩写本轮 closeout 边界。
- Progress snapshot: 24/24 passing

## Session 22 (F-025-A replay parity verified sub-battle closeout)
- Date: 2026-04-15
- Goal: 基于 A1 / A2 / A3 的 fresh evidence 与 Commander final gate，把 `F-025-A` 作为 replay parity 子战写入 progress log。
- Selected feature id: `F-025`
- Scope:
  - 本轮只做 `F-025-A` 子战 closeout 记录。
  - 本轮不改 `feature_list.json`，不改 `specs/*`、`docs/*`、`ui/*`。
  - 本轮不宣称整个 `F-025` 已完成，也不改 longrun authority order。
- Evidence basis:
  - `A1`：replay truth inventory 已冻结，且不反向改写 `F-024` baseline truth surface。
  - `A2`：server / read-back parity 已成立；DingTalk stale optional `ui-meta` 值已在 authoritative server / read-back surface 关闭，WeCom 继续 `bot-first + nested persist + flat read-back`，Feishu 无额外污染。
  - `A3`：browser replay parity 已成立；install 已能从 authoritative `/api/config` 预填，dashboard save / toggle 已具备 read-after-write，DingTalk UI wording 已切到 canonical `Client ID / Client Secret` 并保留 alias 提示。
  - Commander 独立复核命令与结果：
    - `node --test ui/lib/channel-canonical.test.mjs ui/lib/wecom.test.mjs` → PASS（`9 pass / 0 fail`）
    - `node --test ui/public/channel-helpers.test.mjs ui/public/wecom-helpers.test.mjs ui/public/replay-surfaces.test.mjs` → PASS（`15 pass / 0 fail`）
    - `node --check ui/server.mjs` → PASS
    - `git diff --check -- ...` → PASS
- Sub-battle closeout recorded:
  - install replay / dashboard replay / read-back replay 已形成 replay parity baseline。
  - directly affected UI wording 已对齐 canonical replay truth surface。
  - DingTalk stale optional `ui-meta` 问题已关闭。
- Why this round can close:
  - 这是 `F-025-A` 子战收口，不是 `F-025` feature closeout。
  - longrun 在这里仅记录被 A1 / A2 / A3 evidence 与 Commander final gate 支撑的子战事实，不提升 authority。
- Explicit carryover:
  - `F-025-B = Windows replay fidelity`
  - `F-025-C = broader docs parity`
  - callback / 自建应用增强链路、schema 全迁移、动态表单引擎、cleanup 继续排除在本轮完成结论之外。
- Validation:
  - `node --test ui/lib/channel-canonical.test.mjs ui/lib/wecom.test.mjs` → PASS（`9 pass / 0 fail`）
  - `node --test ui/public/channel-helpers.test.mjs ui/public/wecom-helpers.test.mjs ui/public/replay-surfaces.test.mjs` → PASS（`15 pass / 0 fail`）
  - `node --check ui/server.mjs` → PASS
  - `git diff --check -- ...` → PASS
- Current status:
  - `F-025-A` 已完成 verified sub-battle closeout。
  - 整个 `F-025` 仍未 closeout。
- Next follow-up:
  - 继续启动 `F-025-B` 或 `F-025-C`，但需另开 packet。
- Progress snapshot:
  - existing feature baseline remains `24/24 passing`; `F-025` remains in-progress.

## Session 23 (F-026 mac-first platform parity verified closeout writeback)
- Date: 2026-04-15
- Goal: 基于 `spec / code / fresh evidence` 已支撑的事实，把 `F-026` verified closeout 同步到 unified workspace longrun，并把直接受影响的 `F-005` runbook 做 facts-only sync。
- Selected feature id: `F-026`
- Scope:
  - 本轮只做 verified closeout writeback：更新 `longrun/workspaces/opensparrow-unified/feature_list.json`、`longrun/workspaces/opensparrow-unified/claude-progress.txt`、`docs/runbooks/F-005-ui-install-reset.md`。
  - 本轮不改 `specs/026-*`，不改实现面，不改 longrun authority order。
- Evidence basis:
  - close gate 只覆盖 `MAC-DIV-001`、`MAC-DIV-002`、`MAC-DIV-003`、`MAC-DIV-006`。
  - verified implementation facts：
    - `platforms/mac/wrappers/01-开始部署.command` 的 runtime-root precedence 已与 `ui/server.mjs` 对齐，先看 `runtime/`，再回退 `vendor/mac-openclaw/`。
    - `platforms/mac/wrappers/run-openclaw-usb.command` 不再收凭据/直跑 legacy installer，当前只做 UI-first handoff。
    - `platforms/mac/wrappers/harden-openclaw-usb.command` 不再直跑 legacy harden，当前只做 canonical UI/Dashboard handoff。
    - `ui/server.mjs` 的 `/api/status` 返回 authoritative `gatewayPort`。
    - `ui/public/dashboard.html` 不再把服务端口写死为 `18889`，而是显示 authoritative `gatewayPort`。
  - fresh macOS verification evidence：
    - `bash -n platforms/mac/wrappers/01-开始部署.command` → PASS
    - `bash -n platforms/mac/wrappers/run-openclaw-usb.command` → PASS
    - `bash -n platforms/mac/wrappers/harden-openclaw-usb.command` → PASS
    - fresh execution：`01-开始部署.command` 启动到 `http://localhost:19031`，runtime 日志显示 `vendor/mac-openclaw`，`/api/status.gatewayPort = 18897`
    - fresh execution：`run-openclaw-usb.command` 启动到 `http://localhost:19032`，先打印 UI-first handoff，不再出现 `FEISHU_APP_ID` / `OPENAI_API_KEY` 提示，`/api/status.gatewayPort = 18898`
    - fresh execution：`harden-openclaw-usb.command` 启动到 `http://localhost:19033`，先打印 canonical UI/Dashboard handoff，不再直跑 legacy harden，`/api/status.gatewayPort = 18899`
    - fresh browser evidence：Safari 打开 `http://localhost:19031/dashboard` 时，“基本信息”卡片中的“端口”显示 `18897`，与 `/api/status.gatewayPort = 18897` 一致。
  - negative invariants 保持成立：
    - 没有重开 `F-003`
    - 没有重开真正的 `F-014 = wecom-channel-integration`
    - 没有把 `specs/014-mac-arm64-installer-hardening/` 写成 authority 里的 `F-014`
    - 没有把 `F-025-B` 提前并入或关闭
    - 没有把 callback 写成默认 blocking gate
    - 没有宣称整个 mac 体系或整个 `F-025` 已完成
- Closeout recorded:
  - `F-026` 已有足够 fresh macOS evidence 支撑 close gate，但完成范围仅限 mac-first direct user-path parity。
  - 本轮 verified closeout 只覆盖 `MAC-DIV-001/002/003/006`。
  - `run-openclaw-usb.command` 与 `harden-openclaw-usb.command` 已分别稳定为 UI-first handoff 与 canonical UI/Dashboard handoff。
  - Dashboard 服务端口显示现已来自 authoritative `gatewayPort`，不再写死 `18889`。
- Out-of-scope / blockers retained:
  - `MAC-DIV-004` / `MAC-DIV-005` 本轮未写成已完成。
  - `MAC-DIV-007` 继续保留为 `F-026` 外的 packaging/runtime blocker。
  - `F-025-B` 继续保持 `blocked on Windows-specific evidence`。
- Why this round can close:
  - 本轮 closeout 只搬运已被 `spec / code / fresh evidence` 支撑的事实，不新增设计、不改写 truth、不提升 longrun authority。
  - `docs/runbooks/F-005-ui-install-reset.md` 可以承载这次直接受影响的 mac wrapper / Dashboard 事实同步，无需新建 runbook。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`25/25 passing`、`Remaining: 0`、`Blocked by dependencies: 0`）
  - `tail -n 120 longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（文件尾部已出现新追加的 `Session 23`）
  - `git diff -- longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt docs/runbooks/F-005-ui-install-reset.md` → PASS（本轮 writeback 目标集仅包含允许 write-set）
- Current status:
  - `F-026` 已完成 verified closeout writeback。
  - `F-025-B` 仍 blocked on Windows-specific evidence。
- Next follow-up:
  - 若要继续处理 companion `MAC-DIV-004/005`、packaging/runtime blocker `MAC-DIV-007`、或 broader docs / Windows fidelity，必须另开 packet，不能 retroactively 扩写本轮 closeout 边界。
- Progress snapshot: `25/25 passing`; `F-025-B` remains blocked outside this closeout.

## Session 24 (F-028 Mem0 project memory integration)
- Date: 2026-04-15
- Goal: 以 sidecar 方式把 Mem0 接入 OpenSparrow 的 Codex-first 工作流，整理 durable project memory，并在不碰 F-027 实现面的前提下完成最小可用验收。
- Selected feature id: `F-028`
- Scope:
  - 本轮只改 memory/gov/tooling 面：`specs/028-*`、`AGENTS.md`、`docs/memory-setup.md`、`memory/mem0_import.json`、本机 `~/.codex/config.toml`、本机 Mem0 CLI 配置、以及最小 longrun 记账。
  - 本轮不改 `ui/`、`platforms/`、`scripts/openclaw-usb/`、`vendor/`、Windows 线或 F-027 packaged Mac 实现写面。
- Changes made:
  - 新增 `specs/028-mem0-project-memory-integration/{spec,plan,tasks}.md`，冻结 sidecar scope、namespace contract、A/B/C 分类与验收要求。
  - 在 `AGENTS.md` 新增 `Memory Operating Rules`，明确检索先行、durable-only 写入、旧事实优先 update、全文禁灌库、重要短期上下文用 `session_state`。
  - 新增 `docs/memory-setup.md`，说明 Codex/Mem0 接入方式、A/B/C 映射、re-import、纠错与验证方法。
  - 新增 `memory/mem0_import.json`，整理 21 条 OpenSparrow durable memories，覆盖 authority、目录职责、冻结边界、F-014/F-024/F-025/F-026/F-027 结论与项目偏好。
  - 本机 `~/.codex/config.toml` 已新增 `mcp_servers.mem0` 与 `env_http_headers.Authorization = "MEM0_AUTH_HEADER"`。
  - 本机已安装官方 `@mem0/cli`，并设置 `defaults.user_id = opensparrow-memory`、`defaults.app_id = opensparrow`。
- Evidence basis:
  - `MEM0_API_KEY` 与 `MEM0_AUTH_HEADER` 均存在；未写入 repo。
  - `codex mcp get mem0` 显示 Mem0 HTTP MCP 已启用，且 `env_http_headers: Authorization=MEM0_AUTH_HEADER`。
  - `mem0 status -o json` 返回 `connected=true`、`backend=platform`。
  - 使用官方 API 同步写入一条 system test memory：`605e14ed-3b4d-41b3-8d8b-08e6d08cf2c2`。
  - `mem0 get 605e14ed-3b4d-41b3-8d8b-08e6d08cf2c2 -o json`、`mem0 search "verification fact updated" --user-id opensparrow-memory --app-id opensparrow --keyword --threshold 0 -o json` 已验证 system test memory 可读、可搜、可更新。
  - 使用官方 API 同步导入 `memory/mem0_import.json`，导入数 `21`；验收时 `mem0 list --user-id opensparrow-memory --app-id opensparrow -o json` 一度返回 `count = 22`（21 条项目记忆 + 1 条 system test memory），删除 system test marker 后当前稳定计数为 `21`。
  - `mem0 search "OpenSparrow authority order" --user-id opensparrow-memory --app-id opensparrow -o json` 已命中 authority-order memory：`e2620434-bc29-4034-afb1-3af190422288`。
  - 新 `codex exec` 会话日志 `/tmp/opensparrow-codex-exec.log` 显示 `mcp: mem0/search_memories (completed)` 与 `mcp: mem0/get_memories (completed)`，证明 agent-side 已能实际调用 Mem0 MCP 检索已导入记忆。
- Operational findings:
  - `mem0` CLI v0.2.3 的 `add/import` 默认走 async background event；对“必须立刻可验证”的写入，本轮改用官方 API `infer=false` + `async_mode=false`。
  - `mem0 list/search --user-id opensparrow-memory` 在 v0.2.3 下不会自动补默认 `app_id`；若要命中当前项目命名空间，需显式追加 `--app-id opensparrow`。该差异已记录到 `docs/memory-setup.md`。
- Validation:
  - `python3 -m json.tool memory/mem0_import.json >/dev/null` → PASS
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `mem0 status -o json` → PASS (`connected=true`, `backend=platform`)
  - `mem0 get 605e14ed-3b4d-41b3-8d8b-08e6d08cf2c2 -o json` → PASS
  - `mem0 delete 605e14ed-3b4d-41b3-8d8b-08e6d08cf2c2` → PASS
  - `mem0 list --user-id opensparrow-memory --app-id opensparrow -o json` → PASS (`count=21`)
  - `mem0 search "OpenSparrow authority order" --user-id opensparrow-memory --app-id opensparrow -o json` → PASS
  - `codex mcp get mem0` → PASS
- Current status:
  - OpenSparrow 已具备可工作的 Mem0 项目长期记忆。
  - 规则层和 durable memory 层已分开；本轮未扰动 F-027 主线实现。
- Next follow-up:
  - 后续 feature 完成后，按 `AGENTS.md` 的 `Memory Operating Rules` 只补 durable facts，并优先 update 旧记忆而不是新增冲突 truth。
  - 若未来 `mem0` CLI 补齐同步写入或显式 `app_id` 默认继承，可再回收这轮同步 API workaround。

## Session 25 (PKT-027-A4 facts-only closeout writeback)
- Date: 2026-04-16
- Goal: 只把 Commander 已接受的 `PKT-027-A3 = PASS` packaged facts 写回 unified workspace 的长期记忆层，不补实现、不补 verification、也不改写 authority。
- Selected feature id: `F-027`
- Scope:
  - 本轮只更新 `longrun/workspaces/opensparrow-unified/feature_list.json` 与 `longrun/workspaces/opensparrow-unified/claude-progress.txt`。
  - 本轮不补实现，不补 packaged docs parity，不改 `F-025-B`，不重开 `F-026`，也不把真正 `F-014` 改写成失败。
- Evidence basis:
  - Commander 已冻结并接受：`PKT-027-A3 verdict = PASS`。
  - A3 唯一验证 artifact：`dist/handoff/opensparrow-mac-ui-full-arm64-20260415-235340/opensparrow-0.1.0-alpha-mac-ui-arm64`。
  - 今晚 packaged 官方 first-click path：根目录 `01-开始部署.command`。
  - packaged 正式支持面：`Mac-only UI-first`，且今晚 outward support 只承诺 `Feishu / DingTalk`。
  - `mac/run-openclaw-usb.command` 与 `mac/harden-openclaw-usb.command` 只保留为 advanced compatibility / handoff，不再是主安装 / 主 hardening 路径。
  - packaged UI / Dashboard 已按 authoritative `/api/status.gatewayPort` 工作，dashboard 不再硬编码 `18889`。
  - packaged outward promise 已对 WeCom 做 tonight de-scope；这只是 packaged outward promise narrowing，不是真正 `F-014` 失败。
  - companion 不纳入今晚 packaged 正式支持面。
  - `F-025-B` 继续保持 `blocked on Windows-specific evidence`。
- Closeout recorded:
  - `F-027` 以 `packaged-mac-ui-first-release-readiness` 身份 close，且冻结语义只限今晚 `Mac UI-first packaged support surface`。
  - 本轮 PASS 的核心 packaged evidence 点是：唯一 artifact 已冻结、官方 first-click path 已冻结、secondary wrappers 已冻结为 handoff only、packaged UI / Dashboard 已冻结在 authoritative `gatewayPort` read-back、outward support 已缩到 `Feishu / DingTalk`。
  - WeCom 今晚是 packaged outward promise de-scope，不是 channel failure，也不回写真正 `F-014`。
- Residual notes preserved:
  - 包内仍有 residual Windows files，但它们不在今晚 outward first-click surface，也没有被写成已支持 Windows。
  - WeCom plugin/runtime mismatch 仍是已知 packaged fact，但在今晚 de-scope 前提下只保留为 residual note，不升级为 gate-failing blocker。
- Why this round can close:
  - `PKT-027-A4` 只搬运 A3 已证实的 packaged facts；本轮没有通过 closeout 自我发明新结论，也没有让 longrun 越权。
  - `F-027` 的 closeout 没有改写 `F-025-B`、没有重开 `F-026`、也没有把真正 `F-014` 写成失败。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`27/27 passing`、`Remaining: 0`、`Blocked by dependencies: 0`）
  - `tail -n 120 longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（文件尾部已出现新追加的 `Session 25`）
  - `git diff -- longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（本轮 writeback 仅覆盖允许 write-set）
- Current status:
  - `F-027` 已完成 facts-only closeout writeback。
  - `F-025-B` 仍 blocked on Windows-specific evidence。
  - `F-026` 维持 verified closeout，不因 packaged closeout 被重开。
- Next follow-up:
  - 若后续要处理 Windows、broader packaged docs parity、companion 正式支持、或 WeCom packaged runtime/plugin 线，必须另开 packet，不能 retroactively 扩写本轮 `F-027` closeout 边界。
- Progress snapshot: `27/27 passing`; `F-025-B` remains blocked outside `F-027`.

## Session 26 (F-030 packaged DingTalk / WeCom verified closeout)
- Date: 2026-04-22
- Goal: 基于 latest fresh packaged artifact、same-session DingTalk / WeCom live install evidence、以及 facts-only doc/longrun sync，把 `F-030` 正式写回长期事实层。
- Selected feature id: `F-030`
- Scope:
  - 本轮吸收已完成的 packaged implementation / verification 结果，并同步 `docs/current-status.md`、`specs/030-packaged-dingtalk-wecom-support-closure/*`、`docs/runbooks/F-014-wecom-channel-integration.md`、`feature_list.json`、`claude-progress.txt`。
  - 本轮不推进 Windows，不改写 `F-025-B = blocked on Windows-specific evidence`，不重开 `F-026` / `F-027`，也不把 true `F-014` 改写成 callback-first。
- Root cause chain closed in this round:
  - DingTalk packaged false-negative：plugin 安装虽成功，但旧探测链会在 daemon 刚恢复时读到 `daemon=unknown`，从而把 probe 写成 `gateway fallback runtime（daemon 未运行）` 一类假阳性 warning。
  - WeCom packaged old blocker：旧 packaged route 围绕 `@sunnoy/wecom` community archive 展开，曾出现 install-time scanner blocker 与 `unknown channel id: wecom` follow-on error；当前 authoritative packaged route 已迁到官方插件。
- Changes made:
  - `ui/server.mjs`
    - 新增 runtime-state stabilization retry，避免 DingTalk/WeCom probe 在 daemon 刚恢复时读到短暂 `unknown` 状态。
    - packaged WeCom authoritative route 改为官方插件：`@wecom/wecom-openclaw-plugin` / `wecom-openclaw-plugin` entry。
    - WeCom packaged config 现围绕 `plugins.entries.wecom-openclaw-plugin.enabled` 与 `channels.wecom.*` 的真实顺序收口。
  - `scripts/build-usb-pack.sh`
    - bundled WeCom archive 改为 `wecom-wecom-openclaw-plugin-2026.4.22.tgz`。
  - Tests:
    - 新增 `ui/tests/packaged-runtime-state-stability.test.mjs`
    - 新增 `ui/tests/packaged-wecom-official-plugin.test.mjs`
    - 更新 `ui/tests/install-helpers.test.mjs`
  - Docs / longrun:
    - `docs/current-status.md` 已改写为 latest packaged PASS truth，而不是旧 blocker 叙述。
    - `specs/030-packaged-dingtalk-wecom-support-closure/{spec,plan,tasks}.md` 已同步 `2026-04-22` completion snapshot。
    - `docs/runbooks/F-014-wecom-channel-integration.md` 已明确 `F-014` longrun 已 close，且当前 packaged authoritative plugin route 是官方插件。
- Fresh packaged evidence basis:
  - latest artifact lineage：`/tmp/p0-channel-recut-output-20260422-2/gtclaw-mac-release-arm64-20260422-150245/GTClaw-0.1.0-alpha-macOS-arm64`
  - preserved handoff bundle：`/Users/eduardogan/Desktop/GHJProject/opensparrow-gptpro-handoff-20260422-1518-dingtalk-wecom-pass.zip`
  - artifact 内已确认存在：
    - `plugins/openclaw-china-channels-2026.3.29.tgz`
    - `plugins/wecom-wecom-openclaw-plugin-2026.4.22.tgz`
    - `vendor/mac-openclaw/RUNTIME_TRUTH.json`
- DingTalk fresh verdict:
  - real `/api/install` → `HTTP 200`
  - `installStatus.status = completed`
  - `runtimeMode = daemon`
  - `channelProbes.dingtalk.status = ok`
  - `channelProbes.dingtalk.ready = true`
  - `channelProbes.dingtalk.daemon = running`
  - `/api/install`、`/api/install/status`、`install-state.json`、`diagnostic-bundle.json`、`/api/diagnostics`、`/api/diagnostics/export` 已对同一 DingTalk session 给出一致结论
  - 旧 `unknown channel id: dingtalk` 与 `daemon unknown / gateway fallback` 假阳性未复现
- WeCom fresh verdict:
  - real `/api/install` → `HTTP 200`
  - `installStatus.status = completed`
  - `runtimeMode = daemon`
  - `channelProbes.wecom.status = ok`
  - `channelProbes.wecom.ready = true`
  - `channelProbes.wecom.daemon = running`
  - checks / summary 已覆盖 plugin entry enabled、`plugins.allow` 显式包含 `wecom-openclaw-plugin`、bot-first credentials 写入、DM policy open、群聊处理与 requireMention 已启用
  - `/api/install`、`/api/install/status`、`install-state.json`、`diagnostic-bundle.json`、`/api/diagnostics`、`/api/diagnostics/export` 已对同一 WeCom session 给出一致结论
  - 旧 `@sunnoy/wecom` scanner blocker 与 `unknown channel id: wecom` follow-on error 未复现
- Closeout recorded:
  - `F-030` 已以 fresh packaged evidence 满足 feature-level PASS threshold。
  - DingTalk 与 WeCom 当前都不是 `DECISION REQUIRED / EVIDENCE GAP / BLOCKED`，而是各自基于 same-session packaged evidence 的 `PASS`。
  - 当前 packaged WeCom authoritative route 是官方插件；旧 `@sunnoy/wecom` packaged blocker 仅作为历史归因保留，不再是现行 truth。
  - 本轮 closeout 不会把 mac packaged PASS 倒灌到 Windows；`F-025-B` 继续保持 `blocked on Windows-specific evidence`。
- Validation:
  - `node --check ui/server.mjs` → PASS
  - `node --test ui/tests/install-helpers.test.mjs ui/tests/packaged-runtime-state-stability.test.mjs ui/tests/packaged-wecom-official-plugin.test.mjs ui/tests/packaged-plugin-profile-sync.test.mjs ui/tests/packaged-dingtalk-install-gate.test.mjs ui/tests/packaged-wecom-install-gate.test.mjs ui/tests/packaged-channel-probe-diagnostics.test.mjs` → PASS
  - `bash -n scripts/build-usb-pack.sh` → PASS
  - same-session saved evidence review confirms DingTalk / WeCom both satisfy `completed + daemon + channelProbes.*.status=ok + ready=true`
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS
- Current status:
  - latest packaged mac artifact 已完成 DingTalk / WeCom support closure。
  - 当前主要剩余工作是 docs / handoff / Windows 分线，不再是 mac packaged channel blocker。
- Next follow-up:
  - 若后续出现新的 packaged regression，必须基于新的 artifact lineage 与 fresh evidence 另开 packet；不要回退到旧 `@sunnoy/wecom` packaged blocker 叙事。
  - Windows 线继续单独推进，不得借 `F-030` 的 mac packaged PASS 自动 closeout。

## Session 27 (F-020 mac packaged runtime architecture truth hardening)
- Date: 2026-04-22
- Goal: 修复 latest Desktop mac release bundle 把 x86_64 bundled `node` 误标成 `arm64` 包的问题，补齐 build/export CPU 架构 fail-fast，并重切一份真实可启动的 arm64 packaged artifact。
- Selected feature id: `F-020`
- Root cause:
  - `create-mac-handoff-copy.sh` 只按宿主机 `uname -m` 给 artifact 命名为 `arm64 / x64`，但并没有校验 `vendor/mac-openclaw/bin/node` 的真实 CPU 架构。
  - 当前 local vendor runtime 的 `bin/node` 实际是 `Mach-O 64-bit executable x86_64`，因此会生成“目录名是 arm64、payload 却是 x86_64 node”的假 arm64 包。
  - 新的 Apple Silicon Mac 在没有 Rosetta 的情况下执行该 bundled `node`，会直接报 `Bad CPU type in executable`，随后 `01-开始部署.command` 因 `set -euo pipefail` 立即退出。
- Changes made:
  - 新增 `ui/tests/packaged-mac-runtime-arch.test.mjs`，把 bundled mac runtime `node` 必须带 `arm64` slice、build script 必须有 runtime arch guard、export script 必须在 ship 前校验 artifact runtime slice 锁成回归测试。
  - `scripts/build-usb-pack.sh`
    - 新增 `assert_mac_runtime_arch_truth()`，在 staging 前读取 `vendor/mac-openclaw/bin/node` 的 Mach-O 描述并校验当前宿主所需 slice。
    - `vendor/mac-openclaw/RUNTIME_TRUTH.json` 新增 `nodeBinaryArchitectures`，把 packaged runtime CPU 架构 truth 落入 artifact。
    - release-facing `README.txt` / `README.md` 已同步 latest packaged truth，不再继续写旧的 WeCom de-scope 口径。
  - `create-mac-handoff-copy.sh`
    - 新增 `assert_artifact_runtime_arch_truth()`，在最终 artifact 目录生成后再次校验 `vendor/mac-openclaw/bin/node` 是否包含与 artifact 命名一致的 CPU slice。
    - `README-FIRST.txt` 已同步 latest packaged truth。
  - `docs/usb-pack/{INSTALL,SOP}.md` 已同步 latest packaged truth：今晚正式支持渠道包含企业微信，且 authoritative packaged route 是 bundled official plugin archive。
  - local `vendor/mac-openclaw/bin/node` 已升级为 `v24.14.0` 的 universal binary（`x86_64 + arm64`），不再是假 arm64 包源。
- Fresh artifact / evidence:
  - latest artifact lineage：`dist/handoff/gtclaw-mac-release-arm64-20260422-174834/GTClaw-0.1.0-alpha-macOS-arm64`
  - latest archive：`dist/handoff/gtclaw-mac-release-arm64-20260422-174834.zip`
  - isolated launch smoke:
    - `OPENSPARROW_UI_PORT=19021`
    - `OPENCLAW_GATEWAY_PORT=18941`
    - `OPENSPARROW_ROUTER_PORT=18441`
    - `OPENCLAW_HOME=/tmp/gtclaw-arch-fix-smoke-20260422-1755/home`
    - `/api/status` returned `installed:false` with server running and no `Bad CPU type in executable`
- Validation:
  - `node --test ui/tests/packaged-mac-runtime-arch.test.mjs ui/tests/packaged-mac-runtime-deps.test.mjs ui/tests/packaged-mac-diagnostics-shell.test.mjs ui/tests/install-helpers.test.mjs ui/tests/packaged-wecom-official-plugin.test.mjs ui/tests/packaged-dingtalk-install-gate.test.mjs ui/tests/packaged-channel-probe-diagnostics.test.mjs` → PASS
  - `bash -n scripts/build-usb-pack.sh` → PASS
  - `bash -n longrun/workspaces/openclaw-usb-portable/execution/scripts/create-mac-handoff-copy.sh` → PASS
  - `bash -n platforms/mac/wrappers/01-开始部署.command` → PASS
  - `node --check ui/server.mjs` → PASS
  - `file vendor/mac-openclaw/bin/node` → `Mach-O universal binary ... [x86_64] [arm64]`
  - `file dist/handoff/gtclaw-mac-release-arm64-20260422-174834/.../vendor/mac-openclaw/bin/node` → `Mach-O universal binary ... [x86_64] [arm64]`
  - isolated `01-开始部署.command` launch smoke on `19021` → PASS
- Current status:
  - latest Desktop mac bundle failure 的真实根因已经被修掉：不再会把 x86_64 bundled `node` 误打成 arm64 artifact。
  - packaged build/export 现在会在 runtime CPU 架构不匹配时 fail fast，而不是把坏包继续切出来。
  - 下一步只剩重新导出 Desktop full package 并替换旧错误 bundle，避免用户继续拿到坏包。

## Session 28 (F-020 packaged install retry / timeout / Sparrow-state hygiene hardening)
- Date: 2026-04-22
- Goal: 修复 mac packaged release candidate 在新机上暴露的两类严重回归：package-local Sparrow/OpenClaw 状态导致 retry / reinstall 不幂等，以及安装页在 `/api/install` 超时后把后端失败伪装成“正在部署中…”。
- Selected feature id: `F-020`
- User-visible incident:
  - 新机使用 `opensparrow-mac-full-package-20260422-180702-skills100` 时，安装页停在“正在部署中…”约 20 分钟。
  - package-local 日志显示后端其实已经在约 `171s` 后结束，`install-state.json.status = error`，并记录 `plugin already exists` 级错误。
- Root cause:
  - release 交付包本身没有 shipping `.gtclaw-state`，但 package-local 状态一旦在用户机器上生成，后续 retry / reinstall 会命中旧插件目录。
  - 旧实现对 bundled plugin retry 处理不幂等，用户可见错误是 `plugin already exists: .../.openclaw/extensions/channels` 与 `plugin already exists: .../.openclaw/extensions/wecom-openclaw-plugin`。
  - 尝试手工删插件目录会进一步导致 `channels.dingtalk/wecom: unknown channel id`，说明正确修复应依赖 OpenClaw 原生替换语义，而不是手工清目录。
  - 前端 `POST /api/install` 只等 `120000ms`；一旦超时就误判“后台仍在继续”，继续等待 `installed=true`，从而把后端已失败的 authoritative verdict 伪装成“正在部署中…”。
- Changes made:
  - `ui/server.mjs`
    - `installPluginPackage(...)` 改为始终走 `openclaw plugins install ... --force` 的原生替换语义；
    - 不再手工删插件目录，避免 same-package retry 触发 `unknown channel id`。
  - `ui/public/index.html`
    - 新增 install timeout 后的 terminal-state 收口；
    - `AbortError` 不再无限等待 `installed=true`，而是轮询 `/api/install/status`，若 `status=error` 立即显示失败。
  - `scripts/build-usb-pack.sh`
    - `strip_runtime_state()` 现清理 `.gtclaw-state`、`.openclaw`、`.openclaw-*`，防止 package-local Sparrow/OpenClaw 状态进入 release artifact。
  - `longrun/workspaces/openclaw-usb-portable/execution/scripts/create-mac-handoff-copy.sh`
    - handoff export 后再次清理 `.gtclaw-state`、`.openclaw`、`.openclaw-*`，堵住二次打包把状态目录带回 release 的路径。
  - 新增回归测试：
    - `ui/tests/packaged-install-retry-guards.test.mjs`
      - 校验 `plugins install --force`
      - 校验 install timeout 后会收口到 `/api/install/status` terminal state
      - 校验 build/export 与 handoff export 会 strip package-local Sparrow/OpenClaw 状态目录
- Fresh artifact / evidence:
  - latest hardened artifact lineage：`dist/handoff/gtclaw-mac-release-arm64-20260422-193047/GTClaw-0.1.0-alpha-macOS-arm64`
  - paired zip：`dist/handoff/gtclaw-mac-release-arm64-20260422-193047.zip`
  - `unzip -tq dist/handoff/gtclaw-mac-release-arm64-20260422-193047.zip` → `No errors detected in compressed data`
  - `find dist/handoff/gtclaw-mac-release-arm64-20260422-193047 -type d \\( -name '.gtclaw-state' -o -name '.openclaw' -o -name '.openclaw-*' \\)` → no matches
  - same-package real install verification on the fresh artifact:
    - package-local server ports:
      - `OPENSPARROW_UI_PORT=19041`
      - `OPENCLAW_GATEWAY_PORT=18961`
      - `OPENSPARROW_ROUTER_PORT=18461`
    - first `/api/install` → `HTTP 200`, `installStatus.status = completed`
    - second `/api/install` on the exact same artifact/profile → `HTTP 200`, `installStatus.status = completed`
    - second install did **not** reproduce `plugin already exists`
    - second install did **not** reproduce `unknown channel id`
- Validation:
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs` → PASS
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs ui/tests/packaged-dingtalk-install-gate.test.mjs ui/tests/packaged-wecom-install-gate.test.mjs ui/tests/packaged-plugin-profile-sync.test.mjs ui/tests/packaged-channel-probe-diagnostics.test.mjs ui/tests/packaged-runtime-state-stability.test.mjs ui/tests/packaged-wecom-official-plugin.test.mjs ui/tests/install-helpers.test.mjs ui/tests/packaged-mac-runtime-arch.test.mjs` → `24/24 pass`
  - `node --check ui/server.mjs` → PASS
  - `bash -n scripts/build-usb-pack.sh` → PASS
  - `bash -n longrun/workspaces/openclaw-usb-portable/execution/scripts/create-mac-handoff-copy.sh` → PASS
- Current status:
  - `2026-04-22` 同日暴露的 packaged install retry / timeout / Sparrow-state hygiene regression 已在 fresh artifact `193047` 上完成 re-verification。
  - 事故记录必须继续保留在 `docs/current-status.md`、`docs/release-checklist.md`、`docs/runbooks/release-process.md`、`docs/usb-pack/{INSTALL,SOP}.md`、`docs/vendor-source-inventory.md` 中，作为长期 anti-pattern 与 release blocker 检查项。
  - mac packaged 当前可重新 cut release candidate；但对外再次分发前，仍建议补一轮独立新机 smoke，而不是把本地 fresh 验证直接表述成“永不出错”的正式版。

## Session 29 (packaged-mac machine-specific timeout drift diagnostics)
- Date: 2026-04-23
- Goal: 继续追查“`221144` 在本机 real install 成功，但新 Mac 安装页最终报 `/api/install/status` terminal-state timeout”的 machine-specific 行为分叉，并补齐可直接定位错实例 / 旧实例的诊断面。
- Root-cause findings:
  - 当前 `221144.zip` 仍是 clean shipping artifact；zip 内没有 release 运行后生成的 package-local `.gtclaw-state`。
  - 但桌面上的 `opensparrow-mac-delivery-20260422-221144` 展开目录在本机完成 real install / follow-up smoke 后，已经重新生成 package-local `.gtclaw-state`，不再等同于干净 release 目录。
  - 桌面还残留 `opensparrow-mac-delivery-20260422-213211` 这一额外展开目录，也不是干净 release 目录。
  - 代码层同时确认：前端原先只给 `POST /api/install` `120000ms`、timeout 后只给 `/api/install/status` `180000ms` terminal wait；而 packaged install 的慢路径存在 clean-machine / first-run / 双插件串行安装差异，5 分钟总预算不足以覆盖所有合法慢路径。
- Changes made:
  - `ui/public/index.html`
    - install request timeout 提升到 `300000ms`
    - timeout 后的 terminal-state wait 提升到 `600000ms`
    - install-complete wait 提升到 `600000ms`
    - 安装页文案从“通常需要 1-2 分钟”调整为“通常需要 3-5 分钟”
  - `ui/server.mjs`
    - 新增 packaged instance fingerprint：`uiPort`、`packRoot`、`runtimeRoot`、`openclawHome`、`profileDir`、`configPath`、`pid`、`serverStartedAt`
    - `/api/status`、`/api/install/status`、diagnostics bundle 及 package-local `install-state.json` / `diagnostic-bundle.json` 现统一暴露上述 non-secret instance fingerprint，便于定位是否连到了错实例 / 旧实例
  - `docs/packaged-mac-diagnostics.md`、`docs/current-status.md`
    - 已同步 clean zip vs dirty expanded directory 的分发纪律
    - 已同步新的 instance fingerprint 调试入口
- Validation:
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs ui/tests/packaged-instance-fingerprint.test.mjs ui/tests/dashboard-status-shell.test.mjs ui/tests/packaged-channel-probe-diagnostics.test.mjs ui/public/replay-surfaces.test.mjs` → PASS (`22/22`)
  - `node --check ui/server.mjs` → PASS
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
- Current status:
  - 这轮 machine-specific timeout diagnostics 已把“交付介质差异”和“前端总预算过小”两条高概率根因显式化。
  - 后续在新机上只要回读 instance fingerprint，就能快速判断是否连到了错实例 / 旧实例；若 instance 一致但仍超时，再继续看 package-local install-state / log / diagnostics 即可。

## Session 30 (packaged-mac wrapper preflight and foreign-listener hardening)
- Date: 2026-04-23
- Goal: 吸收独立调研分支里与 packaged-mac 新机超时直接相关、且不回退当前诊断面的安全子集，把问题尽量前移到 wrapper preflight / gateway truth / bundled plugin truth。
- Review conclusion:
  - 外部调研 patch 基于较早的 `44158e4f` 测试分支，不能直接 apply 到当前 `feature/p0-packaged-mac-diagnostics @ d585c6e63`。
  - 直接套用会回退当前已加的 instance fingerprint 和 `installedWait=600000ms` 等诊断改进。
  - 因此本轮只手工移植安全子集：wrapper 预检、`PACK_ROOT` cwd、bundled plugin fail-fast、foreign listener 保护、wizard 初始页 gateway-fallback redirect。
- Changes made:
  - `platforms/mac/wrappers/01-开始部署.command`
    - 新增 quarantine 清理、runtime CPU slice 校验、Node 工具链 symlink 校验、空闲 gateway/router 端口选择；
    - 默认导出 `OPENSPARROW_PACKAGED_RUNTIME=1`、`OPENSPARROW_REQUIRE_BUNDLED_PLUGINS=1`。
  - `ui/server.mjs`
    - 新增 packaged hardening env 解析；
    - `runOc(...)` 统一以 `PACK_ROOT` 为 `cwd`；
    - `installPluginPackage(...)` 在 `REQUIRE_BUNDLED_PLUGINS=1` 时要求必须命中包内 bundled tarball，否则直接 fail-fast；
    - `startGatewayFallbackRuntime()` 不再把“目标端口已占用”直接视为成功，必须先通过 gateway health check；否则按 foreign listener 处理；
    - `buildEmptyInstallState()`、`GET /api/status`、diagnostics bundle 现额外暴露 `packagedRuntimeMode` / `requireBundledPlugins`。
  - `ui/public/index.html`
    - `init()` 现在也接受 `runtimeMode = gateway-fallback` 或 `gatewayHealthy = true` 的已安装态。
  - `docs/packaged-mac-diagnostics.md`、`docs/current-status.md`
    - 已同步 wrapper preflight、gateway port truth、bundled plugin truth。
- New / updated regression coverage:
  - `ui/tests/packaged-mac-wrapper-hardening.test.mjs`
  - `ui/tests/packaged-install-retry-guards.test.mjs`
  - `ui/tests/packaged-runtime-state-stability.test.mjs`
  - `ui/public/replay-surfaces.test.mjs`
- Validation:
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs ui/tests/packaged-runtime-state-stability.test.mjs ui/tests/packaged-instance-fingerprint.test.mjs ui/tests/dashboard-status-shell.test.mjs ui/tests/packaged-channel-probe-diagnostics.test.mjs ui/tests/packaged-mac-wrapper-hardening.test.mjs ui/public/replay-surfaces.test.mjs` → PASS (`27/27`)
  - `node --check ui/server.mjs` → PASS
  - `bash -n platforms/mac/wrappers/01-开始部署.command` → PASS
- Current status:
  - packaged-mac 这条线现在不仅能在 UI timeout 后靠 instance fingerprint 追错实例，也能在 wrapper 启动时更早识别“架构不对 / symlink 被压扁 / 端口被别的 listener 占住 / 插件 tarball 缺失”。
  - 下一步若新机仍复现 timeout，就应该优先回看 `/api/status` 与 package-local diagnostics 里的 instance / gateway truth，而不是先假设安装流程本身一定坏了。

## Session 31 (F-031/F-032 facts-only writeback after fresh combined packaged PASS)
- Date: 2026-04-23
- Goal: 仅基于 Commander 已确认的 fresh verifier truth，把 `F-031`、`F-032` 与 packaged-mac 当前 truth 同步回 docs / longrun / feature tracking；不改 runtime code，不重开实现，不扩 scope。
- Selected feature id:
  - `F-031`
  - `F-032`
- Scope:
  - 允许写面：
    - `longrun/workspaces/opensparrow-unified/feature_list.json`
    - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
    - `docs/current-status.md`
    - `docs/packaged-mac-diagnostics.md`
    - `docs/runtime-flow.md`
    - `specs/031-dashboard-model-routing-ui/spec.md`
    - `specs/032-packaged-runtime-authority-and-restart-truth/spec.md`
  - 禁止写面：
    - `ui/**`
    - `platforms/**`
    - `vendor/**`
    - 任意 Windows 文件
    - internal ids
- Fresh verifier truth used for writeback:
  - `Packet A source truth PASS`
  - `Packet B / F-031 source truth PASS`
  - `F-032 source truth PASS`
  - `combined packaged PASS`
  - fresh rebuild artifact：
    - `/tmp/f032-packaged-rebuild-botvDw/gtclaw-mac-release-arm64-20260423-141103/GTClaw-0.1.0-alpha-macOS-arm64`
    - `/tmp/f032-packaged-rebuild-botvDw/gtclaw-mac-release-arm64-20260423-141103.zip`
  - fresh packaged verification 已证明：
    - GTClaw branding PASS
    - authoritative status truth PASS
    - static fake version removed PASS
    - model-routing UI surface exists PASS
    - API config limited to compatibility lane PASS
    - authoritative model-routing load/save PASS
    - truthful save contract PASS
    - internal ids unchanged PASS
    - new files included in fresh artifact PASS
  - truthful save contract 现允许：
    - `saved`
    - `saved_degraded`
    - `rejected`
  - verifier 实际拿到：
    - `POST /api/config/api` → `HTTP 200`, `ok:true`, `persisted:true`, `saveState:"saved_degraded"`
    - `POST /api/config/model-routing` → `HTTP 200`, `ok:true`, `persisted:true`, `saveState:"saved_degraded"`
    - follow-up `GET` / reopen 都能读回 persisted truth
    - internal ids 仍是 `opensparrow-router` / `opensparrow-router/auto`
- Writeback completed:
  - `feature_list.json`
    - 新增 `F-031` 与 `F-032` completed feature entries。
    - evidence hint 统一锚到 `gtclaw-mac-release-arm64-20260423-141103` 这次 fresh combined packaged artifact。
  - `claude-progress.txt`
    - 记录本轮 facts-only writeback scope、evidence source、禁止写面与保留 debt。
  - `docs/current-status.md`
    - 把 latest packaged-mac truth 从 `2026-04-22` 状态推进到 `2026-04-23` combined packaged PASS。
    - 明确写回 F-031/F-032 已 source PASS + combined packaged PASS。
    - 明确写回 `saved | saved_degraded | rejected` 与两条 save endpoint 的 fresh packaged response truth。
  - `docs/packaged-mac-diagnostics.md`
    - 增补 `2026-04-23` fresh combined packaged truth section，记录 artifact、authoritative status、model-routing surface、truthful save contract 与 persisted read-back。
  - `docs/runtime-flow.md`
    - 更新 packaged runtime / dashboard / model-routing 的当前 frozen truth，不再把旧 `20260420-132429` blocker 叙述当 current state。
  - `specs/031-dashboard-model-routing-ui/spec.md`
    - 状态补写为 completed，并记录 F-031 source PASS + combined packaged PASS evidence。
  - `specs/032-packaged-runtime-authority-and-restart-truth/spec.md`
    - 状态补写为 completed，并记录 authoritative status truth + truthful save contract 的 packaged evidence。
- Refused writeback:
  - 未把 Windows 线写成 PASS。
  - 未改写 true `F-027` identity。
  - 未把 `opensparrow-router` / `opensparrow-router/auto` 改写成 native routing。
  - 未把 stale spec 当 authority 去修正当前 runtime truth。
  - 未把 fresh single-machine evidence 写成“跨机器正式发放已无风险”。
- Stale-doc debt retained:
  - `specs/031-dashboard-model-routing-ui/plan.md` 与 `tasks.md` 仍保留 implementation-time wording（例如“不得自行宣布最终 close”）；本轮未重写其历史执行语气。
  - `specs/032-packaged-runtime-authority-and-restart-truth/plan.md` 与 `tasks.md` 同样保留 implementation-time wording；本轮只补 status/evidence，不改 scope。
  - 更早的 historical closeout 文档（如 `F-027` / `F-030` 相关历史包线文档）继续作为 historical context 保留，不在本轮升格为当前 authority。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS（`30/30 passing`、`Remaining: 0`、`Blocked by dependencies: 0`）
  - `git diff -- docs/current-status.md docs/packaged-mac-diagnostics.md docs/runtime-flow.md specs/031-dashboard-model-routing-ui/spec.md specs/032-packaged-runtime-authority-and-restart-truth/spec.md longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（仅命中允许 write-set）
- Current status:
  - `F-031` 已完成 facts-only closeout writeback。
  - `F-032` 已完成 facts-only closeout writeback。
  - packaged-mac 当前 fresh artifact 已可作为下一台 Mac 的候选包，但仍建议先补独立新机 smoke，再决定是否对外正式发放。

## Session 32 (F-034 facts-only closeout writeback)
- Date: 2026-04-24
- Goal: 只把 `F-034` fresh verifier 已确认的 closeout truth 写回 docs / spec / longrun；不做实现，不改 code/tests，不扩 scope，不重判 authority。
- Selected feature id: `F-034`
- Scope:
  - 允许写面：
    - `docs/current-status.md`
    - `docs/packaged-mac-diagnostics.md`
    - `specs/034-packaged-single-channel-plugin-timeout-parity/spec.md`
    - `longrun/workspaces/opensparrow-unified/feature_list.json`
    - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
  - 默认不改：
    - `ui/**`
    - `specs/034-packaged-single-channel-plugin-timeout-parity/plan.md`
    - `specs/034-packaged-single-channel-plugin-timeout-parity/tasks.md`
    - `docs/runtime-flow.md`
- Fresh authority used for closeout:
  - branch/worktree：`/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics`
  - fresh packaged verifier verdict：`PASS / Ready for Closeout`
  - fresh artifact dir：
    - `/private/tmp/f034-packaged-rebuild-lT3AVc/gtclaw-mac-release-arm64-20260424-003358/GTClaw-0.1.0-alpha-macOS-arm64`
  - fresh artifact zip：
    - `/private/tmp/f034-packaged-rebuild-lT3AVc/gtclaw-mac-release-arm64-20260424-003358.zip`
  - fresh replay root：
    - `/private/tmp/f034-packaged-round5-replays-MOZFDx`
  - fresh capture root：
    - `/private/tmp/f034-packaged-round5-captures-rhqPgf`
- Frozen truth written back:
  - `F-034` 已 source PASS + fresh packaged PASS。
  - `dingtalk-only`：
    - `/api/install = HTTP 200`
    - final `status=completed`
    - final `installState=completed`
    - `blockingStep=null`
    - `blockingPlugin=null`
    - `bypass={verdict:none, used:false}`
    - `requestedChannelReadiness={dingtalk:true,wecom:true}`
    - `channelProbes.dingtalk={status:ok, ready:true}`
    - `six-surface consistency=true`
  - `wecom-only`：
    - `/api/install = HTTP 200`
    - final `status=completed`
    - final `installState=completed`
    - `blockingStep=null`
    - `blockingPlugin=null`
    - `bypass={verdict:none, used:false}`
    - `requestedChannelReadiness={dingtalk:true,wecom:true}`
    - `channelProbes.wecom={status:ok, ready:true}`
    - `six-surface consistency=true`
  - `dingtalk+wecom`：
    - `/api/install = HTTP 500`
    - final `status=error`
    - final `installState=failed`
    - `blockingStep=probe`
    - `blockingPlugin=wecom-openclaw-plugin`
    - `bypass={verdict:failed, used:false, plugin:wecom-openclaw-plugin}`
    - `requestedChannelReadiness={dingtalk:false,wecom:false}`
    - `channelProbes.dingtalk={status:warning, ready:false}`
    - `channelProbes.wecom={status:error, ready:false}`
    - `six-surface consistency=true`
    - 这是 exact frozen `F-033` probe truth，已保持
  - router invariants：
    - `providerId=opensparrow-router`
    - `modelTarget=opensparrow-router/auto`
  - `step=plugins` 没有 regression 回到 indefinite running / fake success。
  - `dingtalk-only` passing replay 观察到 `staged-shell -> critical-dist -> final-authority lag` signature，但这次没有真的跨过 `120000ms` timeout，因此 fresh packaged PASS 不是靠 timeout safe_bypass 触发的。
  - residual risks 只作 facts-only 记录：
    - `dingtalk-only` 这次 passing replay 没直接 exercise packaged timeout+grace-wait path
    - `wecom-only` 是 config-only probe summary，但在当前 acceptance 下仍是 packaged PASS
    - 不把这些 residual risks 升格成 blocker
- Writeback completed:
  - `docs/current-status.md`
    - 更新到 `2026-04-24`，补写 `F-034` source PASS + fresh packaged PASS、artifact/replay roots、three-lane frozen truth、router invariants、non-regression truth、residual risks。
    - 把 `2026-04-23` 的 f032 artifact wording降成历史上下文，避免和当前 closeout 冲突。
  - `docs/packaged-mac-diagnostics.md`
    - 新增 `2026-04-24 F-034 fresh packaged replay closeout`，完整记录 DingTalk-only / WeCom-only / combined lane matrix 和 `safe_bypass` 未触发事实。
  - `specs/034-packaged-single-channel-plugin-timeout-parity/spec.md`
    - 状态补写为 completed，并新增 closeout evidence section。
    - 旧 `Current Frozen Truth` 改名为 `Initial Frozen Truth`，保留为 historical context，不再冒充 current packaged state。
  - `feature_list.json`
    - 新增 `F-034` completed feature entry。
    - evidence hint 直接锚到 `2026-04-24` artifact/replay/capture roots。
  - `claude-progress.txt`
    - 记录本轮 facts-only closeout writeback、authority roots、frozen truth 与最小验证结果。
- Refused writeback:
  - 没有把这次 fresh packaged `dingtalk-only` 写成 safe_bypass exercised；保持 `bypass={verdict:none, used:false}`。
  - 没有把 combined lane 写成 completed 或 partial success。
  - 没有把 Round 5 之前的 `REQUEST_CHANGES` 当 current state。
  - 没有把 noisy workspace 整仓脏状态写成 `F-034` truth。
  - 没有重写 `F-033` / `F-034` identity。
  - 没有写 Windows。
  - 没有把 source harness 预期升格成 packaged authority。
- Validation:
  - `python3 -m json.tool longrun/workspaces/opensparrow-unified/feature_list.json >/dev/null` → PASS
  - `python3 longrun/scripts/progress_report.py longrun/workspaces/opensparrow-unified/feature_list.json` → PASS
  - `git diff -- docs/current-status.md docs/packaged-mac-diagnostics.md specs/034-packaged-single-channel-plugin-timeout-parity/spec.md longrun/workspaces/opensparrow-unified/feature_list.json longrun/workspaces/opensparrow-unified/claude-progress.txt` → PASS（仅命中允许 write-set）
- Current status:
  - `F-034` 已完成 facts-only closeout writeback。

## Session 33 (dashboard-dingtalk-diagnostics-timeout-readback facts-only closeout)
- Date: 2026-04-24
- Goal: 只把 `dashboard-dingtalk-diagnostics-timeout-readback` 已验证事实写回 longrun；不改实现代码，不碰 vendor / Windows / wrappers / packaging strategy，不回显 secret，不重写 `F-034` / `F-033` / `dashboard-status-recovery` / `logo-consistency` 的历史身份。
- Packet identity:
  - packet 名：`dashboard-dingtalk-diagnostics-timeout-readback`
  - 类型：小 UI/dashboard diagnostics readback patch
  - 不是 `F-034` reopen。
  - 不是 dashboard status-recovery packet reopen。
- Writeback scope:
  - 已写：`longrun/workspaces/opensparrow-unified/claude-progress.txt`
  - 未写：`longrun/workspaces/opensparrow-unified/feature_list.json`
  - 原因：本轮是小 UI/dashboard diagnostics readback packet closeout，未创建新的编号 feature identity；为避免 identity drift，不把 noisy workspace 或其他历史 packet 写成当前 packet。
- Source PASS:
  - `requestTimeout.diagnostics = 30000`
  - `/api/dingtalk/probe` 使用 `this.requestTimeout.diagnostics`，不再使用 10s status timeout。
  - `AbortError` branch 映射到 warning timeout 文案：`钉钉诊断请求超时，请稍后重试；这不代表消息发送失败。`
  - 不再把 raw `signal is aborted` 暴露给用户。
  - replay/source tests PASS:
    - `node --test ui/public/replay-surfaces.test.mjs`：`19/19`
    - `node --test ui/tests/dashboard-status-shell.test.mjs`：`9/9`
- Fresh packaged smoke PASS:
  - build root：`/private/tmp/dashboard-dingtalk-diagnostics-rebuild-oPvzTr`
  - artifact dir：`/private/tmp/dashboard-dingtalk-diagnostics-rebuild-oPvzTr/gtclaw-mac-release-arm64-20260424-213049/GTClaw-0.1.0-alpha-macOS-arm64`
  - artifact zip：`/private/tmp/dashboard-dingtalk-diagnostics-rebuild-oPvzTr/gtclaw-mac-release-arm64-20260424-213049.zip`
  - build exit code：`0`
  - fresh server port：`64627`
  - `OPENCLAW_HOME`：`/private/tmp/dashboard-dingtalk-diagnostics-smoke-home-ED6hzE/openclaw`
  - `/api/status.instance.packRoot` 指向 fresh artifact dir。
  - `GET /dashboard`：`HTTP 200`
  - `POST /api/dingtalk/probe`：`HTTP 200`，truthful no-config response `status=error`, `ready=false`, `daemon=stopped`
  - packaged UI source inspection PASS：diagnostics `30000`、`AbortError` branch、timeout copy、无 user-facing `signal is aborted`
- Browser automation evidence:
  - Playwright package unavailable。
  - 使用 headless Google Chrome + Chrome DevTools Protocol。
  - 打开 fresh server `/dashboard`。
  - intercept `/api/dingtalk/probe`，手动“刷新读回”延迟 `12001ms`。
  - `11s` 时 UI 仍 loading，无 raw `signal is aborted`，无 timeout copy。
  - final `ok/ready`，summary = `mock dingtalk ready #3`
  - screenshot：`/private/tmp/dashboard-dingtalk-diagnostics-browser-1777039138779.png`
- Risks:
  - CDP automation 代替 Playwright。
  - live backend smoke 是 no-config truthful error path，不是真实钉钉凭据 probe。
- Conclusion:
  - Source PASS + Fresh Packaged Smoke PASS
  - Closeout complete

## Session 34 (packaged-channels-late-stage-authority-closure facts-only closeout)
- Date: 2026-04-25
- Goal: 只把 `packaged-channels-late-stage-authority-closure` 已验证事实写回 spec / longrun / packaged-mac docs；不改实现代码，不碰 vendor / Windows / wrappers / packaging strategy / secrets，不重写 `F-034` / `F-033` 历史身份。
- Packet identity:
  - Packet：`packaged-channels-late-stage-authority-closure`
  - 状态：`completed`
  - 定位：新的 packaged install authority packet
  - 不是 `F-034` reopen。
  - 不是 dashboard/UI patch。
- Writeback scope:
  - 已写：
    - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
    - `longrun/workspaces/opensparrow-unified/feature_list.json`
    - `specs/packaged-channels-late-stage-authority-closure/spec.md`
    - `docs/current-status.md`
    - `docs/packaged-mac-diagnostics.md`
  - noisy workspace 只做 packet attribution；不把整仓 dirty state 写成本 packet。
- Source PASS:
  - `node --check ui/server.mjs` PASS
  - `node --test ui/tests/packaged-single-channel-plugin-timeout-parity.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-plugin-install-hang-bypass.test.mjs` PASS, `10/10`
  - `node --test ui/tests/packaged-plugin-profile-sync.test.mjs` PASS, `5/5`
  - `git diff --check` packet write-set PASS
- Source implementation truth:
  - Added bounded late-stage authority grace via `OPENSPARROW_PLUGIN_LATE_STAGE_AUTHORITY_GRACE_MS`.
  - Packaged default late-stage grace = `600000ms`.
  - Staged shell matching required before late-stage wait activates.
  - Structural readiness requires `openclaw.plugin.json`, `package.json`, `dist/index.js`, package/id match, and for channels `node_modules/@openclaw-china/dingtalk/dist/index.js`.
  - Promotion closes final shared/profile authority only after structural readiness.
  - Negative no-authority lane remains precise failed, not fake success.
- Fresh packaged PASS:
  - build exit code：`0`
  - build root：`/private/tmp/packaged-channels-late-stage-rebuild-77vgKb`
  - artifact dir：`/private/tmp/packaged-channels-late-stage-rebuild-77vgKb/gtclaw-mac-release-arm64-20260424-235728/GTClaw-0.1.0-alpha-macOS-arm64`
  - artifact zip：`/private/tmp/packaged-channels-late-stage-rebuild-77vgKb/gtclaw-mac-release-arm64-20260424-235728.zip`
  - zip SHA256：`d30c2b29023ac118c688eed3e7e109227fef84c229e6e2c6561fce11086a10d9`
  - artifact `ui/server.mjs` `node --check` PASS
  - artifact inspection PASS
- Replay method:
  - replay root：`/private/tmp/packaged-channels-late-stage-replay-UrwTeG`
  - fresh artifact started with bundled node
  - each lane used isolated `HOME`, `OPENCLAW_HOME`, profile, UI/gateway/router ports
  - controlled runtime fixture used only to force deterministic plugin timing
  - dingtalk late-stage lane used plugin timeout `250ms`, normal authority grace `300ms`, late-stage grace `1500ms`, DingTalk dependency delay `800ms`
  - explicitly not fast install replay
- Lane results:
  - dingtalk-only late-stage path：
    - `HTTP 200`
    - `status=completed`
    - `installState=completed`
    - `blockingStep=null`
    - `blockingPlugin=null`
    - `requestedChannelReadiness.dingtalk=true`
    - `channelProbes.dingtalk={status:ok,ready:true}`
  - wecom-only preservation：
    - `HTTP 200`
    - `status=completed`
    - `installState=completed`
    - no plugin indefinite running
    - six surfaces consistent
  - dingtalk+wecom combined `F-033` preservation：
    - `HTTP 500`
    - `status=error`
    - `installState=failed`
    - `blockingStep=probe`
    - `blockingPlugin=wecom-openclaw-plugin`
    - `requestedChannelReadiness={dingtalk:false,wecom:false}`
    - `channelProbes.dingtalk={status:warning,ready:false}`
    - `channelProbes.wecom={status:error,ready:false}`
- Late-stage authority evidence:
  - DingTalk install start：`2026-04-25T08:09:19.545Z`
  - staged shell created：`2026-04-25T08:09:19.546Z`
  - timeout boundary：`2026-04-25T08:09:19.795Z`
  - late DingTalk dependency created：`2026-04-25T08:09:20.373Z`
  - final shared/profile `extensions/channels` both exist with all required files
  - `install.log` includes safe bypass reason: timed out after `250ms`, footprint structurally ready, plus late-stage catch-up closed after timeout wait
- Six-surface consistency:
  - All three lanes consistent across `/api/install`, `/api/install/status`, `install-state.json`, `/api/diagnostics`, `/api/diagnostics/export`, `diagnostic-bundle.json`.
- Router invariants:
  - all lanes `providerId=opensparrow-router`
  - all lanes `modelTarget=opensparrow-router/auto`
- Risks:
  - Packaged replay used controlled runtime fixture to force timeout and late dependency timing; intentional for this packet.
  - `init.sh` still stops on missing legacy frozen dir `opensparrow_win` in isolated worktree; non-blocking.
- Conclusion:
  - Source PASS + Fresh Packaged PASS
  - Closeout complete

## Session 35 (unified-model-configuration-surface facts-only closeout)
- Date: 2026-04-26
- Goal: 只把 `unified-model-configuration-surface` 已通过 gates 的事实写回 spec / tasks / longrun / packaged-mac docs / runtime-flow；不改实现代码，不改 tests，不跑 packaged，不重写历史身份。
- Environment attribution:
  - worktree：`/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics`
  - branch：`feature/p0-packaged-mac-diagnostics`
  - HEAD：`3140ce2111d7224dbc2587490626d660cf3d2a7c`
  - noisy workspace：已有 `ui/**`、tests、untracked `vendor`、untracked `specs/unified-model-configuration-surface/` 等 dirty/untracked state；本 closeout 只做 packet attribution，不把 root dirty state 写成本 packet truth。
- Packet identity:
  - Packet：`unified-model-configuration-surface`
  - 类型：new dashboard/model-routing configuration surface packet
  - 不是 `F-031` reopen。
  - 不是 `F-027` rewrite。
  - 不是 native provider routing。
  - 不是 `F-033` / `F-034` / install packet。
- Accepted gates:
  - Worker-A source implementation complete
  - Source Verifier PASS
  - Packaged Verifier PACKAGED PASS
  - Scope / Authority Reviewer APPROVED
- Writeback scope:
  - 已写：
    - `specs/unified-model-configuration-surface/spec.md`
    - `specs/unified-model-configuration-surface/tasks.md`
    - `longrun/workspaces/opensparrow-unified/feature_list.json`
    - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
    - `docs/current-status.md`
    - `docs/packaged-mac-diagnostics.md`
    - `docs/runtime-flow.md`
  - 未改：
    - `ui/**`
    - tests
    - `vendor/**`
    - Windows
    - wrappers
    - packaging strategy
    - `dist/**`
- Source evidence:
  - `node --check ui/server.mjs` PASS
  - `node --check ui/public/dashboard-model-routing-state.mjs` PASS
  - `node --check ui/lib/model-routing-config.mjs` PASS
  - `node --test ui/tests/dashboard-model-routing-ui.test.mjs` PASS, `4/4`
  - `node --test ui/public/replay-surfaces.test.mjs` PASS, `20/20`
  - `node --test ui/tests/packaged-save-contract-truth.test.mjs` PASS, `9/9`
  - `node --test ui/tests/model-routing-runtime-dispatch.test.mjs` PASS, `1/1`
  - `node --test ui/tests/dashboard-status-shell.test.mjs` PASS, `9/9`
  - `git diff --check` allowed files PASS
- Packaged evidence:
  - Artifact：`/private/tmp/unified-model-config-rebuild-x7V8BL/gtclaw-mac-release-arm64-20260426-193709/GTClaw-0.1.0-alpha-macOS-arm64`
  - Zip：`/private/tmp/unified-model-config-rebuild-x7V8BL/gtclaw-mac-release-arm64-20260426-193709.zip`
  - SHA256：`d32c040a7cb601561137cb47eec6aa15f77fbeb3c8915e1a7f3eb8f214cf90ea`
  - UI port `19371`, gateway `19372`, router `19373`
  - isolated `HOME`：`/private/tmp/unified-model-config-home-193709`
  - isolated `OPENCLAW_HOME`：`/private/tmp/unified-model-config-openclaw-193709`
  - profile：`unified-model-config-verifier`
  - `/api/status.instance.packRoot` pointed to fresh artifact, not stale deleted `/private/tmp` artifact.
- Packaged UI truth:
  - `/dashboard` verified with Chrome CDP fallback because Playwright unavailable.
  - main nav contains `模型配置`
  - no peer main tab `API 配置（上游连接）`
  - no peer main tab `模型智能路由`
  - `单模型` mode has `Base URL` / `API Key` / `Model ID`
  - smart mode has `SIMPLE` / `MEDIUM` / `COMPLEX` / `REASONING`, each with `Base URL` / `API Key` / `Model ID`
- Single-mode packaged API truth:
  - `POST /api/config/model-routing` `HTTP 200`
  - `ok=true`
  - `mode=single`
  - `saveState=saved_degraded`
  - response did not echo synthetic key
  - GET readback:
    - `effectivePrimaryModel = openai/single-packaged-model-193709`
    - `single.apiKeyConfigured = true`
    - no plain key
- Smart-mode packaged API truth:
  - `POST /api/config/model-routing` `HTTP 200`
  - `ok=true`
  - `mode=smart`
  - `saveState=saved_degraded`
  - GET readback:
    - `effectivePrimaryModel = opensparrow-router/auto`
    - all four tiers `apiKeyConfigured=true`
    - distinct tier models read back
    - no plain key or `apiKey` property
- Runtime per-tier dispatch truth:
  - `SIMPLE -> port 19411`, model `simple-fixture-model-193709`
  - `MEDIUM -> port 19412`, model `medium-fixture-model-193709`
  - `COMPLEX -> port 19413`, model `complex-fixture-model-193709`
  - `REASONING -> port 19414`, model `reasoning-fixture-model-193709`
  - each fixture received `/v1/chat/completions`
  - Authorization was checked internally as `authOk=true`
  - no key values were printed
- Security truth:
  - Checked `/api/config/model-routing`, `/api/config`, `/api/status`, `/api/install/status`, `/api/diagnostics`, `diagnostic-bundle.json`, `ui-meta.json`.
  - no synthetic tier key leakage
  - no plain `apiKey` property in exposed/readback surfaces
  - router response headers contain tier/model only, no key
- Router invariants:
  - `providerId = opensparrow-router`
  - `modelTarget = opensparrow-router/auto`
  - preserved through single readback, smart readback, and runtime dispatch
- Residual risks:
  - `saveState=saved_degraded` is expected in isolated verifier profile because no full daemon install matrix was run.
  - dashboard initially redirects to setup until config exists; after packaged single-mode save creates config, `/dashboard` loads normally.
- Conclusion:
  - Facts-only closeout written from the four accepted gates.
  - Closeout complete.

2026-04-27 01:13 +08:00 — packaged status timeout / install redirect hotfix

- Context:
  - Fresh user validation of the prior packaged router build showed two live regressions:
    - install wizard stayed on the step-4 spinner even after backend install state said `completed`;
    - dashboard showed `状态不可用` / `PROFILE 不可用` / `端口 不可用` while chat traffic still worked.
  - Direct live evidence showed `/api/install/status` was already `status=completed`, `installState=completed`, `runtimeMode=daemon`.
  - Direct live `curl --max-time 10 /api/status` timed out; with a longer timeout it returned healthy truth after ~11.3s.
  - Root cause: `/api/status` ran heavy OpenClaw `daemon status --json` and `health --json` probes sequentially, exceeding the frontend 10s status budget. The install timeout recovery path also waited on this slow status endpoint even after terminal install status was completed.
- Source changes:
  - `ui/server.mjs`
    - Added `readDaemonRuntimeSignal()`.
    - `resolveRuntimeState()` now probes daemon status and gateway health concurrently with `Promise.all` while preserving same-profile authority checks.
  - `ui/public/index.html`
    - Added `isCompletedInstallStatus()` and `finishSuccessfulInstall()`.
    - Abort/timeout recovery now redirects when `/api/install/status` is terminal completed with `daemon` or `gateway-fallback` runtime, instead of blocking on a slow follow-up `/api/status`.
  - `ui/tests/packaged-runtime-status-authority.test.mjs`
    - Added delayed fake OpenClaw probes and a regression that fails when status probes are sequential.
  - `ui/public/replay-surfaces.test.mjs`
    - Added a regression for timed-out `/api/install` plus completed `/api/install/status` redirect.
- Source verification:
  - `node --check ui/server.mjs` PASS.
  - `node --check ui/lib/model-routing-config.mjs` PASS.
  - `node --check scripts/model-routing/lib/custom-plugin-routing.mjs` PASS.
  - `node --test ui/tests/packaged-runtime-status-authority.test.mjs` PASS, `3/3`.
  - `node --test ui/tests/packaged-runtime-state-stability.test.mjs` PASS, `3/3`.
  - `node --test ui/public/replay-surfaces.test.mjs` PASS, `21/21`.
  - `node --test ui/tests/packaged-save-contract-truth.test.mjs` PASS, `9/9`.
  - `node --test ui/tests/model-routing-runtime-dispatch.test.mjs` PASS, `2/2`.
  - `node --test ui/tests/packaged-router-model-registration-runtime-chat.test.mjs` PASS, `1/1`.
  - `node --test ui/tests/dashboard-model-routing-ui.test.mjs` PASS, `4/4`.
  - `node --test ui/tests/dashboard-status-shell.test.mjs` PASS, `9/9`.
  - `git diff --check` PASS.
- Fresh packaged artifact:
  - Build root: `/private/tmp/packaged-status-fix-build-hbQu8j`
  - Artifact: `/private/tmp/packaged-status-fix-build-hbQu8j/gtclaw-mac-release-arm64-20260427-010231/GTClaw-0.1.0-alpha-macOS-arm64`
  - Zip: `/private/tmp/packaged-status-fix-build-hbQu8j/gtclaw-mac-release-arm64-20260427-010231.zip`
  - SHA256: `6a1b62360dca1ea1bb6b7eb4f3feda72b43a741b8b998708d8eb901fd2b3df63`
  - `unzip -tq` PASS.
- Fresh packaged smoke:
  - Isolated `HOME`: `/private/tmp/packaged-status-fix-home-RVFFfj`
  - Isolated `OPENCLAW_HOME`: `/private/tmp/packaged-status-fix-openclaw-1r7p5f`
  - Profile: `packaged-status-fix`
  - UI/Gateway/Router ports: `20180 / 20181 / 20182`
  - No-config `/api/status` returned in `3.27s`, below the 10s UI timeout, with `installed=false`, `daemon=not_installed`, `runtimeMode=stopped`, and fresh `instance.packRoot`.
  - No-channel packaged install returned `HTTP 200`, `ok=true`, `installState=completed`, `runtimeMode=daemon` in `52.47s`.
  - Post-install `/api/install/status` returned `status=completed`, `installState=completed`, `summary=安装完成，可导出诊断信息`, `blockingStep=null`, `blockingPlugin=null`.
  - Post-install `/api/status` returned in `2.11s`, below the 10s UI timeout, with `installed=true`, `daemon=running`, `daemonReported=running`, `runtimeMode=daemon`, `gatewayHealthy=true`, `rpcHealthy=true`, and fresh `instance.packRoot`.
  - Smoke daemon and UI listener were cleaned up after verification.
- Residual scope:
  - This smoke did not exercise real DingTalk/WeCom credentials or a manual chat E2E on the fresh artifact.
  - It specifically closes the observed spinner/dashboard-status blocker path and preserves the existing model-router source regressions.

2026-04-27 03:06 +08:00 — packaged router latest-turn routing hotfix

- Context:
  - Live channel testing showed Feishu and WeCom dynamic routing could keep selecting the reasoning tier after an earlier reasoning-style prompt, even when the latest user message was a simple Chinese translation prompt.
  - Current `/api/config/model-routing` smart configuration truth is SIMPLE `gpt-5.4-mini`, MEDIUM `gpt-5.4-mini`, COMPLEX `gpt-5.4`, and REASONING `gpt-5.5`.
  - `gpt-4o-mini` is present only as single-mode/default configuration in this profile, not as a smart-routing tier target.
  - DingTalk shares the same normal `resolveAgentRoute` / buffered dispatch path as Feishu and WeCom, so the same latest-turn classification bug applies there.
- Root cause:
  - `extractPromptFromMessages()` concatenated all historical user messages in the OpenAI-compatible request.
  - Feishu, WeCom, and DingTalk default channel dispatches can reuse the same `agent:main:main` conversation/session, so one earlier REASONING prompt contaminated later simple prompts across channel traffic.
  - A direct old-helper probe reproduced the failure: the simple latest prompt routed SIMPLE when sent alone, but routed REASONING when a prior `solve this logic proof` user message existed in history.
- Source changes:
  - `scripts/model-routing/lib/custom-plugin-routing.mjs`
    - `extractPromptFromMessages()` now scans from the end and returns the latest non-empty user text, including array text blocks.
  - `ui/tests/model-routing-runtime-dispatch.test.mjs`
    - Added a regression where prior reasoning history followed by `你好，回复 OK。把这句话翻译成英文：今天下午三点开会。` must route SIMPLE.
    - Kept 8192-token requests covered so the packaged embedded-agent token budget does not force all prompts into REASONING.
- Verification:
  - Red phase reproduced the failure before the production change: expected SIMPLE but got REASONING.
  - `node --check scripts/model-routing/lib/custom-plugin-routing.mjs` PASS.
  - `node --check ui/server.mjs` PASS.
  - `node --test ui/tests/model-routing-runtime-dispatch.test.mjs` PASS, `1/1`.
  - `node --test ui/tests/packaged-router-model-registration-runtime-chat.test.mjs` PASS, `1/1`.
  - `node --test ui/tests/packaged-save-contract-truth.test.mjs` PASS, `9/9`.
  - `node --test ui/tests/dashboard-model-routing-ui.test.mjs` PASS, `4/4`.
  - `scripts/build-usb-pack.sh --platform mac` PASS.
  - Packaged helper check confirmed the fresh package extracts only the latest simple prompt and does not include the earlier reasoning prompt.
  - Fresh zip `/private/tmp/packaged-router-latest-turn-20260427-030615.zip` passed `unzip -tq`, SHA256 `a07190e8cb27c81f86c8c91ba4acab01ae34026edfcd7178282354af7aff26cb`.
  - Follow-up pure backend channel simulation added to `ui/tests/model-routing-runtime-dispatch.test.mjs` and passed with `2/2` tests:
    - Feishu-shaped prompts routed SIMPLE, MEDIUM, COMPLEX, and REASONING to matching fixture models.
    - WeCom-shaped prompts routed SIMPLE, MEDIUM, COMPLEX, and REASONING to matching fixture models.
    - DingTalk-shaped prompts routed SIMPLE, MEDIUM, COMPLEX, and REASONING to matching fixture models.
    - SIMPLE cases included a prior `solve this logic proof` user message and still routed SIMPLE.
  - Fresh packaged backend simulation started the package root `ui/server.mjs` with isolated state and proved Feishu/WeCom/DingTalk channel-shaped prompts each routed all four tiers to the expected fixture model:
    - Package root `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
    - Isolated `OPENCLAW_HOME` `/var/folders/tj/p85q6mtd5gz377mgk2ndvqrm0000gn/T/packaged-channel-router-home-aiwbik`
    - UI/router ports `49770 / 49771`
  - Fresh packaged natural-language backend simulation repeated the check with realistic user tasks:
    - SIMPLE translation task routed to `SIMPLE / gpt-5.4-mini` on Feishu, WeCom, and DingTalk.
    - MEDIUM meeting-minutes action-item task routed to `MEDIUM / gpt-5.4-mini` on Feishu, WeCom, and DingTalk.
    - COMPLEX TypeScript debug task routed to `COMPLEX / gpt-5.4` on Feishu, WeCom, and DingTalk.
    - REASONING logic-proof task routed to `REASONING / gpt-5.5` on Feishu, WeCom, and DingTalk.
    - Isolated `OPENCLAW_HOME` `/var/folders/tj/p85q6mtd5gz377mgk2ndvqrm0000gn/T/natural-routing-home-0CEaHA`
    - UI/router ports `50102 / 50103`
- Fresh packaged artifact:
  - Package root: `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
  - Finder entry point: `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha/01-开始部署.command`
  - Zip: `/private/tmp/packaged-router-latest-turn-20260427-030615.zip`
- Residual scope:
  - This closes the source/package helper cause for Feishu, WeCom, and normal DingTalk dispatch.
  - Real Feishu/WeCom/DingTalk channel E2E still needs to be rerun against the fresh package; old running packages still contain the historical-user concatenation bug.

2026-04-27 08:55 +08:00 — packaged mac wrapper packRoot / UI port hotfix

- Context:
  - User launched Finder path `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha/01-开始部署.command` and still saw DingTalk install failure:
    `plugin install failed before requested channel dingtalk was ready`
    plus packaged-mode error saying bundled archive for `@openclaw-china/channels` was missing under `plugins/`.
  - Direct evidence showed the generated package did contain bundled plugin archives:
    - `plugins/openclaw-china-channels-2026.4.24.tgz`
    - `plugins/wecom-wecom-openclaw-plugin-2026.4.22.tgz`
  - Direct `curl /api/status` on `127.0.0.1:19000` reported PID `51407` with `instance.packRoot` equal to the worktree root, not `dist/usb-pack/opensparrow-0.1.0-alpha`.
- Root cause:
  - `platforms/mac/wrappers/01-开始部署.command` did not set `OPENSPARROW_UI_PORT`, so a stale/source UI already listening on `19000` could remain the browser-visible service.
  - `resolve_pack_root()` checked ancestor paths before the script directory. When a generated package was run from inside repo `dist/`, root `01-开始部署.command` could resolve `packRoot` back to the source checkout (`../../..`) before considering its own package directory.
  - The stale/source `packRoot` has no generated `plugins/` archive directory, so packaged install failed with a false missing-bundled-plugin error.
- Source changes:
  - `platforms/mac/wrappers/01-开始部署.command`
    - `resolve_pack_root()` now prefers `$script_dir`, then `$script_dir/..`, then source-checkout fallback `$script_dir/../../..`.
    - Adds `OPENSPARROW_UI_PORT="${OPENSPARROW_UI_PORT:-$(resolve_free_port 19000)}"`.
    - Prints `UI: <port>` beside Gateway/Router before launching.
  - `ui/tests/packaged-mac-wrapper-hardening.test.mjs`
    - Adds regression coverage for packaged script-directory precedence.
    - Adds regression coverage for UI free-port selection and printed UI port.
  - Current generated package copies were hotfixed for immediate Finder retest:
    - `dist/usb-pack/opensparrow-0.1.0-alpha/01-开始部署.command`
    - `dist/usb-pack/opensparrow-0.1.0-alpha/mac/01-开始部署.command`
- Verification:
  - Red phase:
    - wrapper test failed before the UI-port fix because `OPENSPARROW_UI_PORT` was absent.
    - wrapper test failed before the packRoot fix because candidate order was `"$script_dir/.."`, `"$script_dir/../../.."`, `"$script_dir"`.
  - Green/source:
    - `node --test ui/tests/packaged-mac-wrapper-hardening.test.mjs` PASS, `5/5`.
    - `bash -n platforms/mac/wrappers/01-开始部署.command` PASS.
    - `bash -n dist/usb-pack/opensparrow-0.1.0-alpha/01-开始部署.command` PASS.
    - `bash -n dist/usb-pack/opensparrow-0.1.0-alpha/mac/01-开始部署.command` PASS.
    - `git diff --check` PASS.
  - Live collision smoke:
    - Existing stale/source UI on `19000` was left running.
    - Launching the generated package root command with isolated `HOME` / `OPENCLAW_HOME` and `OPENSPARROW_AUTO_OPEN=0` selected:
      - Gateway `18929`
      - Router `18413`
      - UI `19001`
    - Wrapper printed runtimeRoot:
      `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha/vendor/mac-openclaw`
    - `/api/status.instance.packRoot` returned:
      `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
  - Fresh zip:
    - `/private/tmp/packaged-router-wrapper-root-ui-port-hotfix-20260427-085516.zip`
    - SHA256 `e0107fda9ea58ed2acd8826b0dac525536c8553fb3fa09c74f4bd21a535158d2`
    - `unzip -tq` PASS.
  - Workspace init:
    - `./longrun/workspaces/opensparrow-unified/init.sh` still exits on known isolated-worktree legacy freeze residual: missing frozen dir `opensparrow_win`.
    - Before that residual, it passes required file checks and shell syntax checks including `platforms/mac/wrappers/*.command`.
- User-facing result:
  - The Finder path under `dist/usb-pack/opensparrow-0.1.0-alpha` is now hotfixed in place.
  - If old `localhost:19000` tabs remain open, use the new port printed by the command window, for example `UI: 19001`.
  - The old source server PID `51407` on `19000` can be stopped separately, but was intentionally left running during smoke to prove collision handling.

2026-04-27 11:32 +08:00 — packaged mac entry contract hardening closeout

- Packet:
  - `packaged-mac-entry-contract-hardening`
  - Closeout writer only; facts-only docs / longrun / memory update.
  - This closeout does not rewrite `F-027`, DingTalk, WeCom, router, wrapper hotfix, or packaged status histories.
- Accepted gates:
  - Spec Review: APPROVED
  - Batch Review: APPROVED
  - Batch Verify: PASS
- Source verification facts:
  - Source checks all PASS, including wrapper `bash -n`, build script `bash -n`, server/helper `node --check`, related `node --test`, and `git diff --check`.
  - Fresh package was rebuilt by `scripts/build-usb-pack.sh --platform mac`.
- Fresh package evidence:
  - Fresh package root: `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
  - package root stat: `Apr 27 10:45:47 2026`
  - root launcher and `mac/01` stat: `Apr 27 10:45:43 2026`
  - root launcher SHA matched source wrapper; `mac/01` is an independent handoff SHA.
  - package root launcher PASS: printed UI `19001`, Mode `packaged runtime hardening`.
  - stale `localhost:19000` existed with PID `11496`; its `packRoot` was source/worktree root, so verification used only printed `19001`.
  - `/api/status.instance.packRoot` exactly matched the fresh package root.
- Entry contract:
  - `platforms/mac/wrappers/*.command` are source template / developer debug surfaces, not user packaged install entries.
  - Package root `01-开始部署.command` is the only official packaged first-click path.
  - Package `mac/01-开始部署.command` is compatibility / handoff path, has compatibility / handoff wording, hands off to root launcher, and does not directly run `ui/server.mjs`.
  - Packaged install verification must use the launcher-printed UI port and must not trust `localhost:19000` by default.
- Bundled plugin readiness:
  - `bundledPlugins.required=true`
  - `bundledPlugins.ready=true`
  - `bundledPlugins.missing=[]`
  - DingTalk archive ready: `plugins/openclaw-china-channels-2026.4.24.tgz`
  - WeCom archive ready: `plugins/wecom-wecom-openclaw-plugin-2026.4.22.tgz`
- Missing DingTalk archive negative:
  - Isolated fixture deleted the DingTalk archive.
  - Result: `ready=false`, `missing` included `@openclaw-china/channels`.
  - `/api/install` returned `HTTP 500`.
  - Error wording pointed to wrong `packRoot` / incomplete package, root `01-开始部署.command`, or regenerating / obtaining a complete package.
  - Error wording had no online install, ClawHub, or 在线安装 fallback wording.
- Docs / longrun updates:
  - `docs/usb-pack/INSTALL.md`
  - `docs/usb-pack/SOP.md`
  - `docs/packaged-mac-diagnostics.md`
  - `docs/runbooks/release-process.md`
  - `longrun/workspaces/opensparrow-unified/feature_list.json`
  - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
- Noisy workspace attribution:
  - Existing dirty/untracked implementation, vendor, UI, and longrun files were present before this closeout.
  - This packet only attributes the docs/longrun closeout write-set above.

2026-04-27 15:08 +08:00 — packaged runtime profile config authority hardening closeout

- Packet:
  - `packaged-runtime-profile-config-authority-hardening`
  - Closer Gate only; facts-only docs / longrun / memory update.
  - This closeout does not modify implementation, tests, spec truth, `dist/**`, `vendor/**`, wrappers, build script, UI public files, model-routing files, or runtime code.
- Accepted gates:
  - Spec Review: APPROVED
  - Worker: DONE
  - Batch Review: APPROVED
  - Batch Verify: PASS
- Source verification PASS:
  - `node --check ui/server.mjs` PASS
  - `node --test ui/tests/packaged-runtime-profile-config-authority.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-runtime-status-authority.test.mjs` PASS, `6/6`
  - `node --test ui/tests/packaged-runtime-state-stability.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-channel-probe-diagnostics.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs` PASS, `6/6`
  - `git diff --check` PASS
- Fresh package verification PASS:
  - build command: `scripts/build-usb-pack.sh --platform mac`
  - fresh package root: `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
  - build removed previous staging dir and printed `USB pack built successfully`
  - packaged `ui/server.mjs` and root launcher matched source via `diff -q`
  - package root stat: `2026-04-27 14:05:26 +0800`
  - root launcher stat: `2026-04-27 14:05:23 +0800`
  - bundled plugin tgz stats: `2026-04-27 14:04:34 / 14:04:35 +0800`
  - build script did not generate zip
- Launch evidence:
  - baseline isolated `HOME`: `/private/tmp/packaged-runtime-authority-verify-20260427-140622-baseline/home`
  - baseline isolated `OPENCLAW_HOME`: `/private/tmp/packaged-runtime-authority-verify-20260427-140622-baseline/openclaw`
  - launched via package root `01-开始部署.command`
  - printed UI ports: baseline `54486`, DingTalk controlled replay `54490`, foreign-port negative `55752`
  - gateway/router ports: baseline `54487/54488`, DingTalk replay `54489/54491`, foreign negative `55753/55754`
  - launched servers and foreign listener stopped; validation temp dirs and fake runtime removed
- Status authority evidence:
  - `/api/status.instance.packRoot` matched the fresh package root exactly.
  - profile: `gtclaw-portable`
  - profileDir: isolated `.openclaw-gtclaw-portable`
  - configPath: isolated `.openclaw-gtclaw-portable/openclaw.json`
  - gatewayPort readback: baseline `54487`, DingTalk replay `54489`, foreign negative `55753`
  - classifications: baseline `gateway_unhealthy`, DingTalk replay `authoritative_ready`, foreign negative `foreign_gateway_port`
  - `/api/status.statusAuthority.daemonStatusUsesProfileConfig=true`
  - `/api/status.statusAuthority.healthUsesProfileConfig=true`
  - controlled env log confirmed daemon status, health, and channels status `--probe` received the same server-side current-profile config authority.
  - `config_path_token_mismatch` covered by source test `packaged-runtime-status-authority`.
- DingTalk readiness replay PASS:
  - controlled fresh-package install persisted DingTalk config.
  - `enabled=true`
  - credential-present and metadata-present readback verified without printing credential values.
  - wrong config would fail health/probe in controlled runtime.
  - env log showed daemon status, health, and channels status `--probe` all used current profile config authority.
  - `/api/install` returned `HTTP 200`
  - `installState=completed`
  - `requestedChannelReadiness.dingtalk=true`
  - direct `/api/dingtalk/probe` returned `ready=true`
  - classification: `authoritative_ready`
  - no real DingTalk credential used or printed
- Stale / foreign port negative PASS:
  - external HTTP listener occupied gateway port `55753`.
  - fresh package launched with isolated state and same gateway port.
  - `/api/status` returned `installed=false`.
  - `runtimeMode=port-occupied`
  - `gatewayPortBusy=true`
  - classification: `foreign_gateway_port`
  - daemon was not reported as authoritative running.
  - `gatewayHealthy=false`
- Secret safety PASS:
  - inspected `/api/status`, `/api/install/status`, `/api/diagnostics`, `/api/diagnostics/export`, `diagnostic-bundle.json`, `install-state.json`, `install.log`, and install response summary.
  - no synthetic secret values found.
  - no token/API key/auth profile secret/channel credential printed.
  - server-side current-profile config env authority was recorded only as implementation truth; no user-facing config-env action instruction was added.
- Closeout write-set:
  - `docs/packaged-mac-diagnostics.md`
  - `longrun/workspaces/opensparrow-unified/feature_list.json`
  - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
- Noisy workspace attribution:
  - Pre-existing dirty/untracked docs, longrun, implementation, tests, vendor, `dist`-adjacent, and spec surfaces existed before this closeout.
  - This packet attributes only the closeout write-set above.

2026-05-02 15:44 +08:00 — packaged channel smart routing authority hardening facts-only closeout

- Packet:
  - `packaged-channel-smart-routing-authority-hardening`
  - Closer Gate only; facts-only docs / longrun / memory update.
  - This closeout does not modify implementation, tests, `ui/**`, `scripts/**`, `platforms/**`, `vendor/**`, `dist/**`, credential/auth files, or unrelated docs/specs/longrun.
- Accepted gates:
  - Spec Review: APPROVED
  - Worker Rounds 8-16: DONE
  - Batch Review: APPROVED
  - Batch Verify: PASS
- Fresh package root:
  - `dist/usb-pack/opensparrow-0.1.0-alpha`
- Active instance:
  - profile: `gtclaw-portable`
  - configPath: packaged `.gtclaw-state/.openclaw-gtclaw-portable/openclaw.json`
  - UI / router / gateway: `19000 / 18412 / 18929`
  - runtimeOwnership: `gatewayOwner=current`, `daemonOwner=current`, `routerOwner=current`, `liveChannelOwner=current_only`
- Smart authority:
  - `effectivePrimaryModel=opensparrow-router/auto`
  - `singleModelMode=false`
  - `SIMPLE -> gpt-4o`
  - `MEDIUM -> gpt-5.4-nano`
  - `COMPLEX -> gpt-5.4`
  - `REASONING -> gpt-5.5`
- Feishu live external inbound PASS:
  - Feishu side actual reply observed.
  - Evidence recorded here is redacted only.
  - `routerInvocation.invokedSinceStart=true`
  - `inputSource=sanitized-current-user-text`
  - input length and hash were recorded in verifier evidence; raw text is not copied.
  - `selectedTier=SIMPLE`
  - `outboundTier=SIMPLE`
  - `outboundModel=gpt-4o`
- Carry-forward Batch Verify evidence:
  - `MEDIUM -> gpt-5.4-nano`
  - `COMPLEX -> gpt-5.4`
  - `REASONING -> gpt-5.5`
  - post-history `SIMPLE -> gpt-4o`
  - single-model regression PASS
  - secret safety PASS
  - Round 13 vendor marker cleanup PASS
- Residual risk:
  - DingTalk / WeCom live external inbound was not rerun in the final live continuation.
  - This closeout does not claim DingTalk / WeCom live PASS.
  - DingTalk / WeCom coverage is limited to diagnostics/readiness/current-owner coverage plus supplemental package-path routing evidence.
- Security:
  - No raw Feishu event envelope, raw user text, sender/chat/message IDs, tokens, secrets, bearer tokens, API keys, or raw JSON metadata were written.
- Closeout write-set:
  - `docs/packaged-mac-diagnostics.md`
  - `docs/current-status.md`
  - `longrun/workspaces/opensparrow-unified/feature_list.json`
  - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
  - `specs/packaged-channel-smart-routing-authority-hardening/spec.md`
  - `specs/packaged-channel-smart-routing-authority-hardening/tasks.md`
- Noisy workspace attribution:
  - Pre-existing Worker-owned dirty/untracked `ui/**`, `scripts/**`, and packet spec files were present before this closeout.
  - This packet attributes only the closeout write-set above.

2026-04-27 17:45 +08:00 — latest usable packaged mac snapshot

- User manual validation:
  - User reran the package-root `01-开始部署.command` after the install-gate authority consumption closeout.
  - The previously observed DingTalk install failure no longer reproduced in the manual path.
  - User confirmed the current package is normal.
- Latest usable package root:
  - `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
- Latest usable source state:
  - branch: `feature/p0-packaged-mac-diagnostics`
  - pre-commit HEAD before recording this snapshot: `b714f8cf247e535843b8a51471ab17d78b03c457`
  - This snapshot is intended to be preserved and pushed as the current latest usable packaged mac baseline.
- Operational note:
  - Continue using the package-root launcher and the launcher-printed UI port.
  - Do not use a stale browser tab or default `localhost:19000` as packaged evidence unless `/api/status.instance.packRoot` matches the package root.

2026-04-27 17:06 +08:00 — packaged install gate authority consumption hardening closeout

- Packet:
  - `packaged-install-gate-authority-consumption-hardening`
  - Closer Gate only; facts-only docs / longrun / memory update.
  - This closeout does not modify implementation, tests, spec truth, `dist/**`, `vendor/**`, wrappers, build script, UI public files, model-routing files, or runtime code.
- Accepted gates:
  - Spec Review: APPROVED
  - Worker: DONE
  - Batch Review: APPROVED
  - Batch Verify: PASS
- Root cause:
  - install gate consumed a stale/transient probe snapshot as terminal requested-channel failure.
  - The same live instance later reached `authoritative_ready`, but install-state had already persisted early `daemon=unknown` / `ready=false`.
  - This packet fixes install gate authority consumption; it is not a reopen of the previous profile config propagation packet.
- Source verification PASS:
  - `node --check ui/server.mjs` PASS
  - `node --test ui/tests/packaged-install-gate-authority-consumption.test.mjs` PASS, `5/5`
  - `node --test ui/tests/packaged-install-retry-guards.test.mjs` PASS, `6/6`
  - `node --test ui/tests/packaged-channel-probe-diagnostics.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-runtime-state-stability.test.mjs` PASS, `3/3`
  - `node --test ui/tests/packaged-runtime-profile-config-authority.test.mjs` PASS, `3/3`
  - `git diff --check` PASS
- Fresh package verification PASS:
  - build command: `scripts/build-usb-pack.sh --platform mac`
  - fresh package root: `/Users/eduardogan/.config/superpowers/worktrees/opensparrow/feature-p0-packaged-mac-diagnostics/dist/usb-pack/opensparrow-0.1.0-alpha`
  - no zip generated by build script
  - package root mtime: `Apr 27 16:24:49 2026`
  - root launcher mtime: `Apr 27 16:24:44 2026`
  - source/package `ui/server.mjs` match SHA256: `13ff72e8425ceb1b9a22d045c6e66fab50fc77a5d49f81778959127c121f6bfd`
  - build output included previous staging dir removal and `USB pack built successfully`
  - package plugins tgz mtime matched this build
- Launch evidence:
  - launched fresh package root `01-开始部署.command`
  - used printed UI port `19003`, not default `19000`
  - isolated `HOME`: `/var/folders/.../opensparrow-packaged-gate-transient-2ad1Hp/home`
  - isolated `OPENCLAW_HOME`: `/var/folders/.../opensparrow-packaged-gate-transient-2ad1Hp/openclaw`
  - success replay gateway/router ports: `18931/18414`
  - foreign negative gateway/router ports: `18929/18414`
  - cleanup: launcher terminated; temp dirs deleted; foreign listener PID `64983` killed; no LISTEN residue on `19003/18929/18931/18414`
- DingTalk install gate replay PASS:
  - method: controlled fixture with safe synthetic credentials and controlled fake runtime
  - path: real fresh package launcher + packaged `/api/install`
  - `/api/install` HTTP status: `200`
  - `installState=completed`
  - `requestedChannelReadiness.dingtalk=true`
  - `channelProbes.dingtalk.status=ok`
  - `channelProbes.dingtalk.ready=true`
  - `channelProbes.dingtalk.daemon=running`
  - `channelProbes.dingtalk.authorityClassification=authoritative_ready`
  - `channelProbes.dingtalk` warnings/errors `0/0`
  - `statusAuthority.classification=authoritative_ready`
  - event log observed transient sequence: daemon `unknown -> running`, health `gateway_unhealthy -> ok`
  - final install state completed with latest probe/authority evidence
  - no real DingTalk credential used or printed
- Status authority:
  - `/api/status.instance.packRoot` matched fresh package root.
  - profile: `gtclaw-portable`
  - profileDir: isolated packaged profile under transient replay `OPENCLAW_HOME`
  - configPath: isolated packaged profile `openclaw.json`
  - gatewayPort: `18931`
  - `statusAuthority.classification=authoritative_ready`
  - `daemonStatusUsesProfileConfig=true`
  - `healthUsesProfileConfig=true`
- Negative verification PASS:
  - foreign port: external Node listener occupied gateway port; packaged `/api/install` returned `500`; `installState=failed`; DingTalk readiness `false`; classification `foreign_gateway_port`; runtime `port-occupied`; `gatewayHealthy=false`.
  - gateway unhealthy: packaged `/api/install` returned `500`; classification `gateway_unhealthy`.
  - config path token mismatch: packaged `/api/install` returned `500`; classification `config_path_token_mismatch`.
  - missing DingTalk credential remains source-covered as `400` input validation.
- Secret safety PASS:
  - inspected `/api/status`, `/api/install/status`, `/api/diagnostics`, `/api/diagnostics/export`, `install-state.json`, `diagnostic-bundle.json`, `install.log`, and install response summary.
  - secret scan hits `[]`.
  - no token/API key/auth profile secret/channel credential printed.
  - closeout records only server/internal authority convergence facts; no user-facing config-env action instruction was added.
- Closeout write-set:
  - `docs/packaged-mac-diagnostics.md`
  - `longrun/workspaces/opensparrow-unified/feature_list.json`
  - `longrun/workspaces/opensparrow-unified/claude-progress.txt`
- Noisy workspace attribution:
  - Pre-existing dirty/untracked docs, longrun, implementation, tests, vendor, `dist`-adjacent, and spec surfaces existed before this closeout.
  - This packet attributes only the closeout write-set above.
