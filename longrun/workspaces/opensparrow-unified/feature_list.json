[
  {
    "id": "F-001",
    "priority": 1,
    "category": "infrastructure",
    "component": "root-persistence",
    "description": "The opensparrow root contains a usable Longrun + Spec-Kit persistence workflow and a dedicated unified workspace.",
    "depends_on": [],
    "acceptance_criteria": [
      "`AGENTS.md`, `.specify/`, `.codex/`, `docs/`, `longrun/`, and `scripts/codex` exist in the repo root.",
      "`longrun/workspaces/opensparrow-unified/` exists with app spec, feature list, progress log, and init script.",
      "`docs/项目持久化说明.md` points future sessions to the unified workspace."
    ],
    "steps": [
      "Import the persistence skeleton into the root.",
      "Create a unified workspace for continued execution.",
      "Update persistence docs to point at the new workspace."
    ],
    "evidence_hint": "Root tree + unified workspace files + docs updated",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-002",
    "priority": 1,
    "category": "migration",
    "component": "openclaw-native-import",
    "description": "openclawNative source-of-truth assets are imported into canonical `vendor/` and `platforms/*/companion/` directories and its legacy workspace is adapted.",
    "depends_on": [
      "F-001"
    ],
    "acceptance_criteria": [
      "`vendor/{linux-openclaw,mac-openclaw,windows-openclaw}/` exist in the root.",
      "`platforms/{linux,mac,windows}/companion/` contain the migrated source files.",
      "`longrun/workspaces/openclaw-native/` points to the new root paths."
    ],
    "steps": [
      "Copy vendor bundles into `vendor/`.",
      "Copy companion source files into `platforms/*/companion/`.",
      "Adapt the imported openclaw-native workspace metadata and init script."
    ],
    "evidence_hint": "Canonical structure + workspace diff",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-003",
    "priority": 1,
    "category": "migration",
    "component": "portable-source-import",
    "description": "The OpenSparrow USB/Feishu source-of-truth assets are imported into the root and the portable legacy workspace remains usable.",
    "depends_on": [
      "F-001"
    ],
    "acceptance_criteria": [
      "`specs/002-*` through `specs/005-*`, `scripts/openclaw-usb/`, `skills/`, `research/`, `ui/`, and `docs/runbooks/` exist in the root.",
      "`longrun/workspaces/openclaw-usb-portable/` exists in the root with its execution docs preserved.",
      "The portable workspace metadata no longer points to the old root as the active repo root."
    ],
    "steps": [
      "Copy portable source-of-truth assets from `feishu-source/`.",
      "Copy UI, runbooks, and docs from `usb-pack/` source directories.",
      "Update portable workspace metadata to the new root."
    ],
    "evidence_hint": "Root specs/scripts/ui/docs + portable workspace updated",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-004",
    "priority": 1,
    "category": "planning",
    "component": "multi-platform-scheme",
    "description": "A merged multi-platform unification scheme based on the latest Claude/Codex research is recorded in docs, spec, and longrun.",
    "depends_on": [
      "F-001",
      "F-002",
      "F-003"
    ],
    "acceptance_criteria": [
      "A root-level solution document exists under `docs/`.",
      "`specs/006-opensparrow-root-unification/` contains `spec.md`, `plan.md`, and `tasks.md`.",
      "The unified workspace progress log references the chosen approach and next steps."
    ],
    "steps": [
      "Summarize the merged risk conclusions and execution method.",
      "Write the new feature spec, plan, and tasks.",
      "Record the current execution status in longrun."
    ],
    "evidence_hint": "Docs/specs/longrun all updated",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-005",
    "priority": 1,
    "category": "infrastructure",
    "component": "git-bootstrap",
    "description": "The root repo is bootstrapped with `.gitattributes`, `.gitignore`, and root guidance to support a clean first Git history.",
    "depends_on": [
      "F-001",
      "F-004"
    ],
    "acceptance_criteria": [
      "`.gitattributes` exists in the root with LF/CRLF/binary rules.",
      "`.gitignore` exists in the root and freezes legacy directories and generated outputs.",
      "`AGENTS.md` and `.specify/memory/constitution.md` reflect the new root structure."
    ],
    "steps": [
      "Write root line-ending and ignore policies.",
      "Write merged agent and constitution guidance.",
      "Initialize git in the root."
    ],
    "evidence_hint": "Root config files + git init",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-006",
    "priority": 1,
    "category": "validation",
    "component": "workspace-validation",
    "description": "The unified workspace, imported legacy workspaces, and shell entrypoints all validate under the new root layout.",
    "depends_on": [
      "F-002",
      "F-003",
      "F-005"
    ],
    "acceptance_criteria": [
      "`./longrun/workspaces/opensparrow-unified/init.sh` succeeds.",
      "Imported legacy workspace init scripts succeed or clearly report only expected optional warnings.",
      "Touched shell and `.command` scripts pass `bash -n`."
    ],
    "steps": [
      "Run unified workspace init.",
      "Run legacy workspace init scripts.",
      "Run shell syntax checks and record evidence."
    ],
    "evidence_hint": "Init output + syntax check output",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-007",
    "priority": 2,
    "category": "next-phase",
    "component": "legacy-archive-container-baseline",
    "description": "After the root unification stabilizes, the repo completes logical legacy archive freeze and lands the first Docker baseline under `deploy/docker/`.",
    "depends_on": [
      "F-006"
    ],
    "acceptance_criteria": [
      "`docs/legacy-archive-freeze-20260323.md` and `scripts/verify-legacy-freeze.sh` define and validate the frozen legacy boundary.",
      "`deploy/docker/` contains Docker baseline assets, including `Dockerfile`, `docker-compose.yml`, `.env.example`, and helper scripts.",
      "Compose/bootstrap/status flow is documented in runbook/SOP and at least the config + `/api/status` path is runnable.",
      "Legacy native delivery paths remain explicitly preserved in docs and runbooks."
    ],
    "steps": [
      "Freeze legacy directories as logical archive and add verification.",
      "Implement Docker baseline assets and shared runtime entrypoints.",
      "Validate container status flow and sync longrun/docs."
    ],
    "evidence_hint": "Archive docs + verify script + Docker assets + docs/docker-SOP.md + status validation",
    "risk": "high",
    "passes": true
  },
  {
    "id": "F-008",
    "priority": 2,
    "category": "build-export",
    "component": "dist-export-closure",
    "description": "The USB build/export pipeline writes generated outputs under `dist/` and packages only canonical root assets from `platforms/`, `vendor/`, `scripts/`, `ui/`, and root docs.",
    "depends_on": [
      "F-007"
    ],
    "acceptance_criteria": [
      "`build-delivery-pack.sh` writes the staging pack to `dist/usb-pack/openclaw-usb-pack/`.",
      "Mac and Windows handoff scripts write outputs to `dist/handoff/`.",
      "Packaged wrappers, docs, and runtime come from canonical root paths rather than frozen directories, missing workspace templates, or host-global runtime state.",
      "Affected docs, spec, feature list, and progress logs are synchronized."
    ],
    "steps": [
      "Redirect staging and handoff outputs to root `dist/`.",
      "Switch packaging sources to canonical root wrappers/docs/scripts and vendor runtime bundles.",
      "Validate shell syntax and rebuild the staging pack before syncing handoff records."
    ],
    "evidence_hint": "Spec-008 + dist tree + bash -n output + synced handoff logs",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-009",
    "priority": 2,
    "category": "reliability",
    "component": "ui-install-reset-hardening",
    "description": "The UI-first deploy flow correctly distinguishes daemon state from stale config, returns to the setup wizard after factory reset, and provides a canonical macOS UI launcher.",
    "depends_on": [
      "F-003",
      "F-006",
      "F-008"
    ],
    "acceptance_criteria": [
      "`/api/status` reports `installed=true` only when the profile config exists and daemon is actually running.",
      "Direct visits to `/dashboard` return to `/setup?force=1` after a full reset removes the profile config.",
      "`platforms/mac/wrappers/01-开始部署.command` launches the UI service directly without prompting for CLI credentials.",
      "Runbook and unified progress log capture the status/reset/entrypoint contracts."
    ],
    "steps": [
      "Refactor runtime status probing to separate daemon state, fallback health, and raw port occupancy.",
      "Guard dashboard routing with reset-aware status checks and force-setup handling.",
      "Add a canonical macOS UI-first launcher and sync runbook/progress evidence."
    ],
    "evidence_hint": "Spec-003 + runbook F-005 + status/reset syntax checks + unified progress entry",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-010",
    "priority": 2,
    "category": "infrastructure",
    "component": "ci-cd-pipeline",
    "description": "GitHub Actions CI pipeline covering shell lint, Node syntax check, Docker compose validation, and repo health checks.",
    "depends_on": [
      "F-007"
    ],
    "acceptance_criteria": [
      "`.github/workflows/ci.yml` exists and defines shell-lint, node-check, docker-validate, and repo-health jobs.",
      "All jobs pass on current main branch code.",
      "Spec and runbook documentation is complete."
    ],
    "steps": [
      "Create spec-009 documentation.",
      "Implement CI workflow with four jobs.",
      "Validate locally and sync longrun records."
    ],
    "evidence_hint": "CI workflow file + local act dry-run or syntax validation",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-011",
    "priority": 2,
    "category": "infrastructure",
    "component": "vendor-source-verification",
    "description": "Vendor runtime source inventory document and verification script for supply chain traceability.",
    "depends_on": [
      "F-002"
    ],
    "acceptance_criteria": [
      "`docs/vendor-source-inventory.md` documents version, architecture, and origin for each vendor platform bundle.",
      "`scripts/verify-vendor.sh` checks vendor directory presence, binary versions, and key file existence.",
      "Script passes on current vendor state."
    ],
    "steps": [
      "Detect vendor binary versions and architectures.",
      "Create source inventory document.",
      "Create verification script.",
      "Validate and sync longrun records."
    ],
    "evidence_hint": "Inventory doc + verify script output",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-012",
    "priority": 1,
    "category": "maintenance",
    "component": "openclaw-runtime-upgrade",
    "description": "Upgrade OpenClaw runtime to v2026.3.23 across all platforms with breaking change adaptations.",
    "depends_on": [
      "F-002",
      "F-011"
    ],
    "acceptance_criteria": [
      "All three vendor platforms upgraded to v2026.3.23.",
      "gateway.auth.mode explicitly set when both token and password exist.",
      "DEFAULT_MODEL extracted to environment variable.",
      "All syntax checks and regression tests pass."
    ],
    "steps": [
      "Upgrade vendor binaries for Mac/Linux/Windows.",
      "Adapt gateway.auth.mode logic in ui/server.mjs.",
      "Extract DEFAULT_MODEL to env var in ui/server.mjs and one-click-deploy.ps1.",
      "Verify ClawHub-first plugin install.",
      "Update docs and longrun records."
    ],
    "evidence_hint": "Vendor version output + code changes + runbook + validation results",
    "risk": "high",
    "passes": true
  },
  {
    "id": "F-013",
    "priority": 1,
    "category": "validation",
    "component": "windows-env-verification",
    "description": "Windows environment verification on UTM ARM VM: UI accessibility, gateway stability, daemon status consistency, plugin install retry.",
    "depends_on": [
      "F-012"
    ],
    "acceptance_criteria": [
      "USB mode starts successfully on Windows (<30s).",
      "Dashboard status matches actual gateway process state.",
      "Plugin install includes 3x exponential backoff retry on 429.",
      "30-minute stability test passes without crash.",
      "Playwright automated test suite passes."
    ],
    "steps": [
      "Deploy OpenSparrow on Windows VM via USB mode.",
      "Fix daemon status inconsistency (gateway-fallback vs actual).",
      "Add plugin install retry with exponential backoff.",
      "Run Playwright E2E test suite.",
      "30-minute stability soak test."
    ],
    "evidence_hint": "Test report + Playwright screenshots + fix commits",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-014",
    "priority": 2,
    "category": "channel",
    "component": "wecom-channel-integration",
    "description": "WeCom (企业微信) channel integration hardening for the bot-first long-connection mainline, with real Bot ID + Secret evidence and at least one real message roundtrip.",
    "depends_on": [
      "F-012"
    ],
    "acceptance_criteria": [
      "WeCom plugin is installable via bundled/offline archive or an existing plugin path accepted by the current runtime.",
      "UI setup and Dashboard support bot-first `Bot ID + Secret` WeCom configuration for the long-connection mainline.",
      "`ui/server.mjs`, shared helpers, and probe handling support the WeCom bot-first long-connection contract reflected in the frozen spec/runbook surface.",
      "Commander-accepted fresh operator evidence confirms real `Bot ID + Secret` enablement, successful probe readiness, and at least one real message roundtrip with reply visibility on the WeCom side.",
      "Callback / self-built app enhanced paths remain optional scenario-specific chains and are not the default passing gate for this closeout."
    ],
    "steps": [
      "Keep the WeCom integration on the bot-first long-connection mainline with `Bot ID + Secret` as the minimum real entry.",
      "Support the same bot-first contract across install UI, Dashboard, server handling, and probe/readiness surfaces.",
      "Retain helper and server validation coverage for the frozen bot-first truth surface.",
      "Close the feature only from Commander-accepted fresh operator evidence covering real enablement and at least one real message roundtrip.",
      "Leave callback / self-built app enhanced-chain verification to separate follow-up work when business scenarios actually require it."
    ],
    "evidence_hint": "Helper tests + server check + fresh probe evidence + real WeCom roundtrip evidence",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-015",
    "priority": 1,
    "category": "maintenance",
    "component": "skill-store-source-dist-sync",
    "description": "Skill Store correctly reads nested `skills/My_Skills/<category>/<skill>/` directories in both source and rebuilt dist packs, and the dashboard reflects installed counts and category metadata from the API.",
    "depends_on": [
      "F-003",
      "F-008"
    ],
    "acceptance_criteria": [
      "`ui/server.mjs` lists nested industry skills from `skills/My_Skills/<category>/<skill>/SKILL.md` and reports labeled categories.",
      "`ui/public/dashboard.html` uses the backend `installedCount` field and builds the category dropdown from API data.",
      "A rebuilt `dist/usb-pack/opensparrow-<version>/ui/server.mjs` returns `total > 0` from `/api/skills/list?page=1&pageSize=3`.",
      "Install/uninstall smoke test toggles both `installed` and `installedCount` as expected in an isolated HOME directory."
    ],
    "steps": [
      "Fix Skill Store API metadata and installed-count semantics.",
      "Align dashboard state with the API payload.",
      "Rebuild the USB pack and validate source/dist API responses plus install/uninstall smoke."
    ],
    "evidence_hint": "Source/dist API probe + isolated install/uninstall smoke + release-process note",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-016",
    "priority": 1,
    "category": "maintenance",
    "component": "windows-home-path-portability",
    "description": "Windows one-click deployment no longer assumes historical profiles live under a hardcoded `C:\\Users` root; it resolves candidate home directories from environment variables and Windows user profile APIs.",
    "depends_on": [
      "F-008",
      "F-013"
    ],
    "acceptance_criteria": [
      "`platforms/windows/wrappers/one-click-deploy.ps1` does not hardcode `C:\\Users` when locating existing `.openclaw-<profile>` state.",
      "Historical profile detection still prefers the current user's home when config already exists, and can fall back to discovered Windows user homes for elevated-launch scenarios.",
      "Affected Windows delivery docs and unified longrun records mention the new discovery strategy."
    ],
    "steps": [
      "Replace hardcoded Windows user-root scanning with environment-driven candidate homes.",
      "Add Windows profile API fallback for non-standard home locations.",
      "Sync Windows delivery docs and unified progress records."
    ],
    "evidence_hint": "one-click-deploy.ps1 diff + docs/usb-pack/windows-native-delivery.md note + longrun entry",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-017",
    "priority": 2,
    "category": "ui",
    "component": "public-static-asset-hardening",
    "description": "Unify OpenSparrow UI public asset routing so dashboard images and future nested static files load consistently and safely.",
    "depends_on": [
      "F-015"
    ],
    "acceptance_criteria": [
      "`ui/public/dashboard.html` references `logo.png` instead of an oversized inline data URL.",
      "`ui/server.mjs` serves public assets through a shared MIME/extension contract with path traversal protection.",
      "Static GET/HEAD requests for `logo.png` return `200` and direct file-open validation shows the image renders correctly."
    ],
    "steps": [
      "Extract dashboard logo into a standalone public PNG file.",
      "Unify public asset MIME and route handling in the UI server.",
      "Add safe public path resolution with traversal checks.",
      "Validate HTTP and direct-file rendering paths."
    ],
    "evidence_hint": "`curl -I /logo.png` + direct `dashboard.html` browser check",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-018",
    "priority": 1,
    "category": "maintenance",
    "component": "openai-compatible-provider-hardening",
    "description": "Custom OpenAI-compatible provider setup now writes provider-level api metadata, auto-corrects `/v1` when the raw custom path chat route is unreachable, and keeps Dashboard API saves aligned with the active runtime model.",
    "depends_on": [
      "F-012"
    ],
    "acceptance_criteria": [
      "`ui/server.mjs` writes `models.providers.openai.api = \"openai-completions\"` during install and Dashboard API saves.",
      "`POST /api/config/api` follows provider config writes with `models set`, preventing model drift between the form and runtime.",
      "Custom non-root OpenAI-compatible URLs get a conservative `/v1` fallback probe and surface compatibility warnings in install/Dashboard success responses.",
      "Shared Bash / PowerShell install scripts also write provider-level `api` metadata."
    ],
    "steps": [
      "Extract and test OpenAI provider config helpers.",
      "Wire install/save flows to provider probing, provider-level api writes, and models set synchronization.",
      "Expose compatibility warnings in the dashboard and sync shared installer scripts."
    ],
    "evidence_hint": "helper unit tests + isolated /api/config/api smoke + dashboard/script diffs",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-019",
    "priority": 1,
    "category": "planning",
    "component": "commander-orchestration-governance",
    "description": "Record a long-term Commander operating model with a fixed 5-agent roster, optional 1-3 temporary specialists, and explicit dispatch / review / handoff rules for the unified repo.",
    "depends_on": [
      "F-004"
    ],
    "acceptance_criteria": [
      "`specs/013-commander-orchestration-governance/` contains `spec.md`, `plan.md`, and `tasks.md`.",
      "`docs/runbooks/F-019-commander-orchestration-governance.md` records the fixed roster, dispatch patterns, temporary expansion triggers, and conflict-avoidance rules.",
      "`longrun/workspaces/opensparrow-unified/feature_list.json` and `claude-progress.txt` both mention the Commander governance baseline and note that this round did not modify runtime code."
    ],
    "steps": [
      "Write the governance spec, implementation plan, and task breakdown.",
      "Create a Commander runbook for role cards, dispatch modes, and escalation rules.",
      "Sync the new governance baseline into unified longrun records and verify the persistence workflow still runs."
    ],
    "evidence_hint": "new spec suite + runbook + progress_report output + unified init pass",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-020",
    "priority": 1,
    "category": "packaging",
    "component": "mac-arm64-installer-hardening",
    "description": "Harden the macOS arm64 UI package so bundled superpowers copy recursively, DingTalk/WeCom install from bundled plugin archives instead of ClawHub-first resolution, plugin-backed channel config is skipped unless the plugin is ready, package-local Sparrow/OpenClaw state never ships in release artifacts, retry/reinstall uses idempotent plugin replacement semantics, install timeout follows `/api/install/status` terminal authority, and mac packaged release cuts fail fast on runtime CPU architecture mismatch.",
    "depends_on": [
      "F-012",
      "F-019"
    ],
    "acceptance_criteria": [
      "`ui/install-helpers.mjs` plus `ui/tests/install-helpers.test.mjs` cover recursive skill copy and bundled plugin archive resolution.",
      "`ui/server.mjs` prefers packaged `.tgz` plugin archives, skips DingTalk/WeCom channel config when plugin install fails, and uses a longer plugin install timeout suitable for WeCom local archive installs.",
      "A fresh packaged mac artifact exists at `dist/handoff/gtclaw-mac-release-arm64-20260422-174834.zip`, its bundled `vendor/mac-openclaw/bin/node` includes an `arm64` slice, and the export script refuses to ship an artifact whose runtime node lacks the expected CPU slice.",
      "Smoke evidence shows local `.tgz` plugin install works for DingTalk and WeCom, and package launch still reaches the UI listening state."
    ],
    "steps": [
      "Add pure install helpers and a focused node:test regression for recursive skill copy and plugin archive discovery.",
      "Wire UI install and channel-update flows to local plugin archives, plugin-ready gating, corrected OpenClaw entry preference, and a longer plugin install timeout.",
      "Rebuild the mac arm64 UI package, bundle local DingTalk/WeCom plugin archives, validate launch, harden runtime architecture truth, and retain only the latest good ZIP."
    ],
    "evidence_hint": "Fresh artifact lineage `gtclaw-mac-release-arm64-20260422-193047` + `node --test ui/tests/install-helpers.test.mjs ui/tests/packaged-mac-runtime-arch.test.mjs ui/tests/packaged-install-retry-guards.test.mjs` + package-local first/second real `/api/install` both `completed` + zip integrity/state-strip checks",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-021",
    "priority": 1,
    "category": "maintenance",
    "component": "codespec-template-ingestion",
    "description": "Mirror curated codeSPEC cleanup templates into the unified repo, keep the source project untouched, and clarify the boundary between imported references and local authority surfaces.",
    "depends_on": [
      "F-006",
      "F-019"
    ],
    "acceptance_criteria": [
      "`specs/015-codespec-template-ingestion/` contains `spec.md`, `plan.md`, and `tasks.md`.",
      "`scripts/import-codespec-template.sh` plus `scripts/tests/import-codespec-template.test.mjs` prove the import is whitelist-based and does not overwrite local reference docs.",
      "`docs/reference/codeSPEC-template/` contains local scope docs plus an upstream `UnifiedFramework` snapshot imported from `codeSPEC` without modifying the source repo.",
      "`longrun/README.md`, `longrun/CHECKLIST.md`, and `longrun/METHOD.zh-CN.md` clearly position longrun as project memory rather than feature or execution authority.",
      "`README.md`, `longrun/workspaces/opensparrow-unified/feature_list.json`, and `longrun/workspaces/opensparrow-unified/claude-progress.txt` all mention the new reference surface and its authority boundary."
    ],
    "steps": [
      "Write the migration spec, plan, and tasks for curated codeSPEC template ingestion.",
      "Add a tested import script that rebuilds a local reference snapshot from a whitelist of `UnifiedFramework` files.",
      "Create reference docs and runbook entries that explain imported scope, skipped assets, and why current repo authority stays local.",
      "Sync the longrun boundary notes and unified project-memory records after the import succeeds."
    ],
    "evidence_hint": "`node --test scripts/tests/import-codespec-template.test.mjs` + `bash -n scripts/import-codespec-template.sh` + import manifest + unified init/progress report",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-022",
    "priority": 1,
    "category": "governance",
    "component": "codespec-framework-adaptation",
    "description": "Adapt codeSPEC into OpenSparrow's active development framework by wiring local governance docs, longrun scaffolds, authority order, and curated upstream reference sync into one usable底座.",
    "depends_on": [
      "F-019",
      "F-021"
    ],
    "acceptance_criteria": [
      "`docs/governance/` contains an active local governance surface: framework stack, project onboarding SOP, dispatch templates, and quick reference.",
      "`AGENTS.md`, `.specify/memory/constitution.md`, `docs/项目持久化说明.md`, and `README.md` all point to the new framework entry and share the same authority order.",
      "`longrun/templates/initializer_prompt.template.md` and `longrun/templates/coding_prompt.template.md` clearly position longrun as project memory scaffolding instead of feature or execution authority.",
      "`scripts/import-codespec-template.sh` plus `scripts/tests/import-codespec-template.test.mjs` mirror the expanded curated whitelist (`UnifiedFramework` + `AgentTeam`) without modifying the source repo or overwriting local reference docs.",
      "`claude-progress.txt` records that the framework底座 is ready and future product work should build on this adapted stack."
    ],
    "steps": [
      "Create active local governance docs that adapt codeSPEC's UnifiedFramework and AgentTeam concepts to OpenSparrow paths and rules.",
      "Wire the new governance stack into AGENTS, constitution, project persistence docs, README, and longrun prompt scaffolds.",
      "Expand curated reference import coverage to include AgentTeam onboarding/dispatch templates and the execution bridge document.",
      "Validate the updated stack with import tests, real reference sync, unified init, and progress reporting."
    ],
    "evidence_hint": "`docs/governance/` + `node --test scripts/tests/import-codespec-template.test.mjs` + import manifest + unified init/progress report",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-023",
    "priority": 1,
    "category": "governance",
    "component": "commander-mode-hardening",
    "description": "Harden Commander Mode boundaries across the constitution, plan template, and coding prompt scaffold so commander activation stays explicit, execution ownership stays serialized, and closeout remains non-authoritative.",
    "depends_on": [
      "F-019",
      "F-022"
    ],
    "acceptance_criteria": [
      "`.specify/memory/constitution.md` records explicit Commander Mode activation, role boundaries, commander contract, ownership serialization, and non-authoritative closeout rules.",
      "`.specify/templates/plan-template.md` makes packetization the default dispatch scaffold with required packet fields, independent reviewer/verifier roles, stop rules, and separate integration-packet wording.",
      "`longrun/templates/coding_prompt.template.md` states that commander mode does not make the main thread the default implementation owner, that longrun closeout remains non-authoritative, and that `passes: true` still requires traceable acceptance evidence.",
      "Cross-surface wording for `PKT-017-A/B/C/D` is backed by existing `Batch Reviewer: APPROVED` and `Batch Verifier: PASS` evidence and no further integration is required for this round."
    ],
    "steps": [
      "Harden the constitution wording for explicit commander activation, role boundaries, commander contract, and serialized ownership.",
      "Upgrade the plan template so packet fields, ownership guards, reviewer/verifier separation, and stop rules are dispatch defaults.",
      "Harden the coding prompt scaffold so continuation notes stay non-authoritative and `passes: true` remains evidence-gated.",
      "Align the cross-surface wording and close the round only from existing reviewed and verified evidence."
    ],
    "evidence_hint": "`Batch Reviewer: APPROVED` + `Batch Verifier: PASS` for `PKT-017-A/B/C/D` + key anchors in `.specify/memory/constitution.md`, `.specify/templates/plan-template.md`, and `longrun/templates/coding_prompt.template.md`",
    "risk": "low",
    "passes": true
  },
  {
    "id": "F-024",
    "priority": 1,
    "category": "channel",
    "component": "unified-channel-contract-baseline",
    "description": "Freeze the Feishu / DingTalk / WeCom baseline canonical contract so server canonical handling, shared browser helpers, minimal docs wording sync, and install/dashboard/read-back closure all follow one baseline without claiming full replay parity.",
    "depends_on": [
      "F-012",
      "F-014"
    ],
    "acceptance_criteria": [
      "`specs/024-unified-channel-contract-baseline/spec.md`, `plan.md`, `tasks.md`, and `docs/runbooks/F-024-unified-channel-contract-baseline.md` freeze the baseline truth table and mapping closure, including Feishu `appId + appSecret`, DingTalk `clientId + clientSecret` with optional `corpId`, and WeCom `botId + secret` with nested persistence.",
      "`PKT-024-B` lands server-side canonical normalize / validate / persist / read-back handling so `/api/install` and `/api/config/channels` share one requiredness model, DingTalk `corpId` stays optional metadata, WeCom remains bot-first with nested persistence, and Feishu has a first-class canonical boundary.",
      "`PKT-024-C` lands shared frontend helper baseline for Feishu / DingTalk / WeCom so Install and Dashboard use the same normalize / validate / payload-build contract and DingTalk `corpId` is no longer a browser install blocker.",
      "`PKT-024-D` syncs only the directly affected live-spec / tutorial / SOP / runbook wording to the frozen baseline, including the three-card Dashboard surface and the explicit `F-024` versus `F-025` boundary.",
      "`PKT-024-E` provides install / dashboard / read-back baseline closure evidence and the Commander acceptance gate passes for the baseline only, explicitly leaving real E2E, Windows replay fidelity, and broader docs parity to `F-025`."
    ],
    "steps": [
      "Freeze the shared core / delta / alias truth table and install/dashboard/server/read-back mapping closure for the baseline contract.",
      "Converge server canonical handling for Feishu, DingTalk, and WeCom without flattening WeCom persistence or re-promoting DingTalk `corpId` to required.",
      "Switch Install and Dashboard to shared channel helpers for normalize / validate / payload build while keeping the current surface structure.",
      "Sync only the directly affected live-spec, tutorial, SOP, and runbook wording to the frozen baseline and document the `F-024` versus `F-025` boundary.",
      "Close the round from verification-only install / dashboard / read-back evidence plus Commander acceptance PASS, not from longrun self-inference."
    ],
    "evidence_hint": "Commander acceptance gate PASS + existing `PKT-024-A/B/C/D/E` evidence for baseline truth table, canonical handling, shared helpers, wording sync, and closure",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-026",
    "priority": 1,
    "category": "reliability",
    "component": "mac-first-platform-parity",
    "description": "Verified closeout for the mac-first direct user-path parity surface across the primary launcher, secondary handoff wrappers, and authoritative Dashboard gateway-port binding, limited to `MAC-DIV-001/002/003/006` and explicitly excluding companion `MAC-DIV-004/005`, packaging/runtime blocker `MAC-DIV-007`, and any change to `F-025-B`'s blocked Windows-specific status.",
    "depends_on": [
      "F-003",
      "F-020"
    ],
    "acceptance_criteria": [
      "`specs/026-mac-first-platform-parity/` plus `mac-truth-inventory.md` freeze `F-026` as a mac-first feature and confine the verified closeout scope to `MAC-DIV-001/002/003/006` without re-opening `F-003`, true `F-014 = wecom-channel-integration`, or `F-025-B`.",
      "`platforms/mac/wrappers/01-开始部署.command` now follows the same runtime-root precedence as `ui/server.mjs`, checking `runtime/` before falling back to `vendor/mac-openclaw` on the mac direct user path.",
      "`platforms/mac/wrappers/run-openclaw-usb.command` and `platforms/mac/wrappers/harden-openclaw-usb.command` are closed as UI-first / canonical UI-Dashboard handoffs instead of direct legacy credential or hardening bridges.",
      "`ui/server.mjs` returns authoritative `gatewayPort` from `/api/status`, and `ui/public/dashboard.html` displays that authoritative port instead of hardcoding `18889`.",
      "Fresh macOS evidence shows syntax checks passing for the three wrappers, launcher/handoff entry at `http://localhost:19031`, `http://localhost:19032`, and `http://localhost:19033`, matching `/api/status.gatewayPort` values `18897/18898/18899`, and Safari dashboard display matching authoritative `gatewayPort` rather than default `18889`.",
      "Closeout wording explicitly keeps `MAC-DIV-004/005` unresolved in this round, retains `MAC-DIV-007` as a packaging/runtime blocker outside this closeout, and leaves `F-025-B` blocked on Windows-specific evidence."
    ],
    "steps": [
      "Freeze the mac-first truth surface and admissible closeout scope for `MAC-DIV-001/002/003/006` without widening into companion, packaging, or Windows fidelity work.",
      "Align the primary mac launcher with server runtime-root precedence and convert the secondary wrappers into UI-first / canonical UI-Dashboard handoff surfaces.",
      "Make `/api/status.gatewayPort` the authoritative Dashboard service-port source instead of a hardcoded `18889` display.",
      "Close the feature only from fresh macOS syntax, launcher, status, and browser evidence on the direct user-path surface.",
      "Retain companion `MAC-DIV-004/005`, packaging/runtime blocker `MAC-DIV-007`, and `F-025-B`'s Windows-specific blocked status outside this verified closeout."
    ],
    "evidence_hint": "Fresh macOS `bash -n` on the three wrappers + direct launcher/handoff runs at `19031/19032/19033` + `/api/status.gatewayPort` parity + Safari dashboard port evidence",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-027",
    "priority": 1,
    "category": "packaging",
    "component": "packaged-mac-ui-first-release-readiness",
    "description": "Closed as `packaged-mac-ui-first-release-readiness` for tonight's Mac-only UI-first packaged support surface: the only verified official first-click path is root `01-开始部署.command`, official packaged support is limited to Feishu / DingTalk, `mac/run-openclaw-usb.command` and `mac/harden-openclaw-usb.command` are advanced compatibility / handoff only, packaged UI / Dashboard reads authoritative `/api/status.gatewayPort` instead of hardcoding `18889`, companion stays outside tonight's support surface, WeCom is de-scoped from tonight's outward promise without marking true `F-014` as failed, and `F-025-B` remains blocked on Windows-specific evidence.",
    "depends_on": [
      "F-020",
      "F-026"
    ],
    "acceptance_criteria": [
      "`specs/027-mac-ui-first-release-readiness/` plus `packaged-mac-release-surface-inventory.md` freeze `F-027` as a new packaged Mac-only feature, not a reopening of `F-026`, not a rewrite of true `F-014`, and not any change to `F-025-B = blocked on Windows-specific evidence`.",
      "The Commander-accepted `PKT-027-A3` PASS gate is tied to one packaged verification artifact only: `dist/handoff/opensparrow-mac-ui-full-arm64-20260415-235340/opensparrow-0.1.0-alpha-mac-ui-arm64`, with root `01-开始部署.command` as tonight's only official first-click path.",
      "Tonight's packaged official support surface is Mac-only UI-first with Feishu / DingTalk, while `mac/run-openclaw-usb.command` and `mac/harden-openclaw-usb.command` are recorded only as advanced compatibility / handoff surfaces rather than the main install or hardening path.",
      "The packaged UI / Dashboard surface is closed on authoritative `/api/status.gatewayPort`, and the packaged dashboard no longer hardcodes `18889` for the service port display.",
      "Tonight's outward promise explicitly de-scopes packaged WeCom readiness without converting true `F-014` into a failure, keeps companion outside the official support surface, and preserves residual Windows files plus the known WeCom plugin/runtime mismatch as residual notes rather than upgraded blockers for this closeout."
    ],
    "steps": [
      "Freeze `F-027` as packaged Mac UI-first release readiness scoped only to tonight's packaged support surface.",
      "Anchor the closeout to the single Commander-accepted verification artifact and root `01-开始部署.command` as the only official first-click path.",
      "Record the official packaged support promise as Mac-only UI-first with Feishu / DingTalk, while demoting `mac/run-openclaw-usb.command` and `mac/harden-openclaw-usb.command` to advanced compatibility / handoff only.",
      "Carry forward the packaged `/api/status.gatewayPort` authority and the dashboard de-hardcoding of `18889` as part of the packaged release-ready surface.",
      "Preserve WeCom de-scope, companion exclusion, residual Windows files, the known WeCom plugin/runtime mismatch note, and `F-025-B`'s blocked Windows-specific status without reopening `F-026` or rewriting true `F-014`."
    ],
    "evidence_hint": "Commander-accepted `PKT-027-A3 = PASS` on `dist/handoff/opensparrow-mac-ui-full-arm64-20260415-235340/opensparrow-0.1.0-alpha-mac-ui-arm64` + packaged first-click / wrapper-role / gatewayPort / outward-promise evidence",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-028",
    "priority": 1,
    "category": "tooling",
    "component": "mem0-project-memory-integration",
    "description": "Integrate Mem0 as the project's durable memory layer for Codex-first usage, codify memory operating rules, curate durable project knowledge, and import the initial OpenSparrow memory batch without touching the F-027 Mac UI-first implementation path.",
    "depends_on": [
      "F-022",
      "F-023"
    ],
    "acceptance_criteria": [
      "`specs/028-mem0-project-memory-integration/` contains `spec.md`, `plan.md`, and `tasks.md`.",
      "`AGENTS.md` contains a `Memory Operating Rules` section that matches the existing authority order and longrun subordinate-memory boundary.",
      "`docs/memory-setup.md` and `memory/mem0_import.json` explain the memory operating model, A/B/C mapping, namespace contract, and re-import/verification workflow.",
      "Local Codex configuration exposes the Mem0 MCP with auth injected from environment variables rather than repo secrets.",
      "Mem0 connectivity, list/search/get/update, and the initial OpenSparrow memory import are all verified with fresh evidence."
    ],
    "steps": [
      "Freeze the sidecar scope for Mem0 project memory integration in spec/plan/tasks.",
      "Add repo-level memory operating rules and curate durable project memories into a JSON import batch.",
      "Connect Mem0 to Codex via local MCP configuration and install the official mem0 CLI.",
      "Run system-test writes, import the initial OpenSparrow memory batch, and verify CLI plus agent-side retrieval."
    ],
    "evidence_hint": "Mem0 MCP config + `mem0 status` + project-scoped list/search/get/update + imported memory batch + `codex exec` log showing Mem0 MCP retrieval",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-030",
    "priority": 1,
    "category": "packaging",
    "component": "packaged-dingtalk-wecom-support-closure",
    "description": "Close the packaged DingTalk / WeCom support promise on the latest macOS artifact lineage with same-session real install evidence, authoritative daemon runtime, consistent install/status/diagnostics/package-local facts, DingTalk probe/readiness closure, and a packaged WeCom route migrated to the official plugin without lowering true `F-014`'s bot-first floor.",
    "depends_on": [
      "F-014",
      "F-026",
      "F-027"
    ],
    "acceptance_criteria": [
      "`specs/030-packaged-dingtalk-wecom-support-closure/{spec,plan,tasks}.md` freeze `F-030` as a standalone packaged support closure feature, keep `F-025-B` blocked on Windows-specific evidence, preserve true `F-014 = bot-first / Bot ID + Secret / callback 非默认 gate`, and require per-channel fresh packaged evidence rather than promise carryover.",
      "Latest packaged lineage evidence shows DingTalk real `/api/install` completes with `runtimeMode=daemon`, `channelProbes.dingtalk.status=ok`, `ready=true`, and consistent facts across `/api/install`, `/api/install/status`, `/api/diagnostics`, `/api/diagnostics/export`, `install-state.json`, and `diagnostic-bundle.json`.",
      "Latest packaged lineage evidence shows WeCom real `/api/install` completes with `runtimeMode=daemon`, `channelProbes.wecom.status=ok`, `ready=true`, and consistent facts across `/api/install`, `/api/install/status`, `/api/diagnostics`, `/api/diagnostics/export`, `install-state.json`, and `diagnostic-bundle.json`.",
      "The packaged WeCom authoritative route is the official plugin `@wecom/wecom-openclaw-plugin` with bundled archive `wecom-wecom-openclaw-plugin-2026.4.22.tgz`, explicit plugin-entry enablement, and no reproduction of the old `@sunnoy/wecom` install-time blocker or `unknown channel id: wecom` follow-on error.",
      "Facts-only closeout keeps `F-025-B` blocked, keeps `F-026` / `F-027` frozen, and treats the preserved handoff bundle as audit/reference evidence rather than a new authority source."
    ],
    "steps": [
      "Freeze `F-030` as its own packaged DingTalk / WeCom support closure feature with per-channel gates, inadmissible evidence rules, and role separation.",
      "Close DingTalk packaged implementation gaps by aligning plugin availability, profile-extension discovery, runtime-state stabilization, and diagnostics/package-local truth surfaces.",
      "Close WeCom packaged implementation gaps by migrating the packaged route to the official plugin, gating channel config on plugin readiness, and preserving the bot-first mainline without promoting callback to a default gate.",
      "Run same-session fresh packaged verification for DingTalk and WeCom on the latest artifact lineage, capturing `/api/install`, status, diagnostics/export, and package-local evidence under isolated homes.",
      "Sync the resulting facts to active docs and longrun without reopening Windows, officialization, `F-026`, `F-027`, or true `F-014`."
    ],
    "evidence_hint": "Fresh packaged artifact lineage `gtclaw-mac-release-arm64-20260422-150245` + preserved Desktop bundle `opensparrow-gptpro-handoff-20260422-1518-dingtalk-wecom-pass.zip` + same-session DingTalk/WeCom install/status/diagnostics/package-local evidence",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-031",
    "priority": 1,
    "category": "dashboard",
    "component": "dashboard-model-routing-ui",
    "description": "Close the packaged Mac dashboard model-routing follow-up by exposing a dedicated authoritative model-routing UI surface distinct from the API compatibility lane, preserving `opensparrow-router` / `opensparrow-router/auto`, and proving packaged read/write parity on the 2026-04-23 fresh combined artifact.",
    "depends_on": [
      "F-027",
      "F-030"
    ],
    "acceptance_criteria": [
      "`specs/031-dashboard-model-routing-ui/{spec,plan,tasks}.md` freeze the Packet B follow-up without rewriting true `F-027` identity or native routing authority.",
      "Dashboard has a dedicated model-routing surface distinct from the legacy `API 配置` form, and the packaged artifact still shows GTClaw branding.",
      "`GET /api/config/model-routing` and `POST /api/config/model-routing` are the dashboard's authoritative load/save surface, while `POST /api/config/api` remains the upstream connection / compatibility lane only.",
      "Fresh combined packaged verification confirms follow-up `GET` / reopen reads back persisted model-routing truth and preserves internal ids `opensparrow-router` and `opensparrow-router/auto`."
    ],
    "steps": [
      "Freeze `F-031` as the Packet B dashboard model-routing UI follow-up in `spec / plan / tasks` without reopening Packet A, true `F-027`, or native routing authority.",
      "Land the dedicated dashboard model-routing surface and source-level verification while preserving the compatibility-lane boundary and router invariants.",
      "Close the feature only after fresh combined packaged verification confirms branding parity, authoritative model-routing load/save, persisted read-back, and unchanged internal ids."
    ],
    "evidence_hint": "`Packet B / F-031 source truth PASS` + fresh combined packaged artifact `gtclaw-mac-release-arm64-20260423-141103` showing GTClaw branding, dedicated model-routing UI, compatibility-lane-only API config, authoritative `/api/config/model-routing` load/save, persisted read-back, and unchanged internal ids",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-032",
    "priority": 1,
    "category": "runtime",
    "component": "packaged-runtime-authority-and-restart-truth",
    "description": "Close the packaged runtime authority follow-up by making `/api/status` authoritative, returning truthful degraded save contracts for `/api/config/api` and `/api/config/model-routing`, and proving the combined packaged artifact keeps persisted truth without changing router identities.",
    "depends_on": [
      "F-030",
      "F-031"
    ],
    "acceptance_criteria": [
      "`specs/032-packaged-runtime-authority-and-restart-truth/{spec,plan,tasks}.md` freeze the backend/runtime follow-up without reopening Packet A shell work, Packet B UI shape, Windows, or true `F-027` / `F-031` identity.",
      "Fresh packaged verification confirms `/api/status` no longer reports false-positive running truth and now serves as the authoritative packaged runtime status surface.",
      "Truthful save contract now allows `saved`, `saved_degraded`, and `rejected`, and both `POST /api/config/api` plus `POST /api/config/model-routing` can return caller-consumable `2xx` degraded truth when persistence succeeds but restart remains degraded.",
      "Fresh combined packaged verification observed `HTTP 200`, `ok:true`, `persisted:true`, `saveState:\"saved_degraded\"` on both save endpoints, with follow-up `GET` / reopen reading back persisted truth while preserving `opensparrow-router` and `opensparrow-router/auto`."
    ],
    "steps": [
      "Freeze `F-032` as the packaged runtime authority and truthful save-contract follow-up in `spec / plan / tasks`, limited to backend/runtime truth.",
      "Land source-level `/api/status` authority truth and save-contract behavior without reopening UI shape or changing router identities.",
      "Close the feature only after fresh combined packaged verification confirms authoritative status truth, truthful degraded save responses, persisted read-back, and no regression to Packet A / Packet B packaged surfaces."
    ],
    "evidence_hint": "`F-032 source truth PASS` + fresh combined packaged artifact `gtclaw-mac-release-arm64-20260423-141103` with `/api/status` authority PASS and both save endpoints returning `HTTP 200`, `ok:true`, `persisted:true`, `saveState:\"saved_degraded\"`, followed by persisted read-back and unchanged internal ids",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "F-034",
    "priority": 1,
    "category": "packaging",
    "component": "packaged-single-channel-plugin-timeout-parity",
    "description": "Close the packaged single-channel plugin timeout parity packet from fresh packaged replay evidence by proving DingTalk-only and WeCom-only now complete truthfully on the same artifact, while the combined DingTalk+WeCom lane preserves the exact frozen F-033 probe failure contract without regressing to indefinite running, fake success, or router-id drift.",
    "depends_on": [
      "F-030",
      "F-032"
    ],
    "acceptance_criteria": [
      "`specs/034-packaged-single-channel-plugin-timeout-parity/{spec,plan,tasks}.md` freeze F-034 as its own packet and close it only from the 2026-04-24 fresh packaged replay, not from stale historical PASS.",
      "Fresh packaged replay on artifact `gtclaw-mac-release-arm64-20260424-003358` shows `dingtalk-only` with `/api/install = HTTP 200`, final `status=completed`, final `installState=completed`, `blockingStep=null`, `blockingPlugin=null`, `bypass={verdict:none, used:false}`, `requestedChannelReadiness={dingtalk:true,wecom:true}`, `channelProbes.dingtalk={status:ok, ready:true}`, and `six-surface consistency=true`.",
      "Fresh packaged replay on the same artifact shows `wecom-only` with `/api/install = HTTP 200`, final `status=completed`, final `installState=completed`, `blockingStep=null`, `blockingPlugin=null`, `bypass={verdict:none, used:false}`, `requestedChannelReadiness={dingtalk:true,wecom:true}`, `channelProbes.wecom={status:ok, ready:true}`, and `six-surface consistency=true`.",
      "Fresh packaged replay on the same artifact preserves the exact frozen F-033 combined contract: `dingtalk+wecom` returns `/api/install = HTTP 500`, final `status=error`, final `installState=failed`, `blockingStep=probe`, `blockingPlugin=wecom-openclaw-plugin`, `bypass={verdict:failed, used:false, plugin:wecom-openclaw-plugin}`, `requestedChannelReadiness={dingtalk:false,wecom:false}`, `channelProbes.dingtalk={status:warning, ready:false}`, `channelProbes.wecom={status:error, ready:false}`, and `six-surface consistency=true`.",
      "Router invariants remain `providerId=opensparrow-router` and `modelTarget=opensparrow-router/auto`, `step=plugins` does not regress to indefinite running or fake success, and the passing DingTalk-only replay shows the staged-shell -> critical-dist -> final-authority lag signature without actually crossing the 120000ms timeout or exercising timeout safe_bypass."
    ],
    "steps": [
      "Freeze F-034 as the packaged single-channel plugin timeout parity packet, keeping its identity separate from F-033 and requiring closeout to come only from fresh packaged replay.",
      "Verify the fresh packaged single-channel matrix on one artifact: DingTalk-only PASS, WeCom-only PASS, and combined DingTalk+WeCom preserving the exact frozen F-033 probe truth across six surfaces.",
      "Close the packet only after syncing the verified matrix, router invariants, non-regression truth, and residual facts-only risks into docs, spec status, and longrun tracking."
    ],
    "evidence_hint": "`F-034 source PASS` + fresh packaged verifier `PASS / Ready for Closeout` on artifact dir `/private/tmp/f034-packaged-rebuild-lT3AVc/gtclaw-mac-release-arm64-20260424-003358/GTClaw-0.1.0-alpha-macOS-arm64`, zip `/private/tmp/f034-packaged-rebuild-lT3AVc/gtclaw-mac-release-arm64-20260424-003358.zip`, replay root `/private/tmp/f034-packaged-round5-replays-MOZFDx`, and capture root `/private/tmp/f034-packaged-round5-captures-rhqPgf`",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "packaged-channels-late-stage-authority-closure",
    "priority": 1,
    "category": "packaging",
    "component": "packaged-install-authority",
    "description": "Close the packaged channels late-stage authority packet by adding bounded post-timeout staged-shell authority closure for channels installs, proving DingTalk late dependency catch-up reaches final shared/profile authority, preserving WeCom-only success and exact F-033 combined failure truth, and keeping F-034 / F-033 identities intact.",
    "depends_on": [
      "F-034"
    ],
    "acceptance_criteria": [
      "`specs/packaged-channels-late-stage-authority-closure/{spec,plan,tasks}.md` freezes this as a new packaged install authority packet, not an F-034 reopen or dashboard/UI patch.",
      "Source PASS confirms `OPENSPARROW_PLUGIN_LATE_STAGE_AUTHORITY_GRACE_MS`, packaged default late-stage grace `600000ms`, staged shell matching before late-stage wait, structural readiness checks for `openclaw.plugin.json`, `package.json`, `dist/index.js`, package/id match, and channels `node_modules/@openclaw-china/dingtalk/dist/index.js`.",
      "Source tests pass: `node --check ui/server.mjs`, `node --test ui/tests/packaged-single-channel-plugin-timeout-parity.test.mjs` (`3/3`), `node --test ui/tests/packaged-plugin-install-hang-bypass.test.mjs` (`10/10`), `node --test ui/tests/packaged-plugin-profile-sync.test.mjs` (`5/5`), and `git diff --check` on the packet write-set.",
      "Fresh packaged verifier PASS on artifact `gtclaw-mac-release-arm64-20260424-235728` proves the DingTalk late-stage lane crosses timeout, receives the late dependency, closes final shared/profile `extensions/channels`, returns `HTTP 200`, `status=completed`, `installState=completed`, `blockingStep=null`, `blockingPlugin=null`, `requestedChannelReadiness.dingtalk=true`, and `channelProbes.dingtalk={status:ok,ready:true}`.",
      "Fresh packaged verifier preserves WeCom-only success, exact F-033 combined DingTalk+WeCom probe failure truth, six-surface consistency across install/status/state/diagnostics/export/bundle, and router invariants `providerId=opensparrow-router` plus `modelTarget=opensparrow-router/auto`."
    ],
    "steps": [
      "Freeze this packet as backend/runtime packaged install authority closure with no vendor, Windows, wrapper, packaging-strategy, dashboard/UI, or secret scope.",
      "Implement bounded late-stage staged-shell authority closure that promotes only structurally ready footprints and keeps no-authority failures precise.",
      "Run source syntax/tests and packet write-set diff checks.",
      "Run fresh packaged replay on a new artifact using controlled timing to exercise the timeout plus late dependency path rather than a fast install replay.",
      "Close out the verified facts into spec, longrun, feature tracking, and packaged-mac status docs without rewriting F-034 or F-033 history."
    ],
    "evidence_hint": "Source PASS + fresh packaged verifier PASS on build root `/private/tmp/packaged-channels-late-stage-rebuild-77vgKb`, artifact dir `/private/tmp/packaged-channels-late-stage-rebuild-77vgKb/gtclaw-mac-release-arm64-20260424-235728/GTClaw-0.1.0-alpha-macOS-arm64`, zip `/private/tmp/packaged-channels-late-stage-rebuild-77vgKb/gtclaw-mac-release-arm64-20260424-235728.zip`, SHA256 `d30c2b29023ac118c688eed3e7e109227fef84c229e6e2c6561fce11086a10d9`, replay root `/private/tmp/packaged-channels-late-stage-replay-UrwTeG`, and DingTalk late-stage evidence from `2026-04-25T08:09:19.545Z` through `2026-04-25T08:09:20.373Z`",
    "risk": "medium",
    "passes": true
  },
  {
    "id": "unified-model-configuration-surface",
    "priority": 1,
    "category": "dashboard",
    "component": "unified-model-configuration-surface",
    "description": "Close the new dashboard/model-routing configuration surface packet by replacing the split model/API main tabs with one `模型配置` surface containing `单模型` and `模型智能路由` modes, saving single and per-tier smart model configuration through `/api/config/model-routing`, masking all keys in readback and diagnostics surfaces, proving packaged per-tier runtime dispatch, and preserving `opensparrow-router` / `opensparrow-router/auto` without reopening F-031 or rewriting F-027.",
    "depends_on": [
      "F-031",
      "F-032"
    ],
    "acceptance_criteria": [
      "`specs/unified-model-configuration-surface/{spec,plan,tasks}.md` freezes this as a new dashboard/model-routing configuration surface packet, not an F-031 reopen, F-027 rewrite, native provider routing change, or F-033/F-034/install packet.",
      "Source verifier PASS confirms `node --check ui/server.mjs`, `node --check ui/public/dashboard-model-routing-state.mjs`, `node --check ui/lib/model-routing-config.mjs`, dashboard model-routing UI tests `4/4`, replay surfaces `20/20`, packaged save contract truth `9/9`, model-routing runtime dispatch `1/1`, dashboard status shell `9/9`, and `git diff --check` on allowed files.",
      "Fresh packaged verifier PASS on artifact `gtclaw-mac-release-arm64-20260426-193709` confirms `/api/status.instance.packRoot` points to the fresh artifact, `/dashboard` shows main nav `模型配置`, no peer main tab `API 配置（上游连接）`, no peer main tab `模型智能路由`, single mode has `Base URL` / `API Key` / `Model ID`, and smart mode has all four tiers with `Base URL` / `API Key` / `Model ID`.",
      "Packaged API verification confirms single-mode `POST /api/config/model-routing` returns `HTTP 200`, `ok=true`, `mode=single`, `saveState=saved_degraded`, does not echo the synthetic key, and GET readback returns `effectivePrimaryModel = openai/single-packaged-model-193709`, `single.apiKeyConfigured = true`, and no plain key.",
      "Packaged API verification confirms smart-mode `POST /api/config/model-routing` returns `HTTP 200`, `ok=true`, `mode=smart`, `saveState=saved_degraded`, and GET readback returns `effectivePrimaryModel = opensparrow-router/auto`, all four tiers `apiKeyConfigured=true`, distinct tier models, and no plain key or `apiKey` property.",
      "Runtime dispatch verification confirms SIMPLE, MEDIUM, COMPLEX, and REASONING route to distinct fixture ports/models, authorization is checked internally as `authOk=true` without printing key values, security surfaces contain no synthetic tier key leakage or plain `apiKey`, router response headers contain tier/model only, and router invariants remain `providerId=opensparrow-router` plus `modelTarget=opensparrow-router/auto`."
    ],
    "steps": [
      "Freeze and implement the unified dashboard `模型配置` surface without changing F-031/F-027/F-033/F-034 identities or entering install/vendor/Windows/wrapper/packaging scope.",
      "Run source verifier checks for syntax, dashboard UI contract, replay surfaces, save contract truth, runtime dispatch, dashboard status shell, and diff hygiene.",
      "Run fresh packaged verification on an isolated artifact/profile using Chrome CDP fallback when Playwright is unavailable.",
      "Verify single-mode and smart-mode packaged save/readback truth, masked key behavior, and per-tier runtime dispatch with controlled fixtures.",
      "Close out only after Worker-A implementation complete, Source Verifier PASS, Packaged Verifier PACKAGED PASS, and Scope / Authority Reviewer APPROVED."
    ],
    "evidence_hint": "Accepted gates: Worker-A source implementation complete + Source Verifier PASS + Packaged Verifier PACKAGED PASS + Scope / Authority Reviewer APPROVED. Packaged artifact `/private/tmp/unified-model-config-rebuild-x7V8BL/gtclaw-mac-release-arm64-20260426-193709/GTClaw-0.1.0-alpha-macOS-arm64`, zip `/private/tmp/unified-model-config-rebuild-x7V8BL/gtclaw-mac-release-arm64-20260426-193709.zip`, SHA256 `d32c040a7cb601561137cb47eec6aa15f77fbeb3c8915e1a7f3eb8f214cf90ea`, UI/gateway/router ports `19371/19372/19373`, isolated `HOME` `/private/tmp/unified-model-config-home-193709`, isolated `OPENCLAW_HOME` `/private/tmp/unified-model-config-openclaw-193709`, profile `unified-model-config-verifier`, and controlled runtime fixtures SIMPLE `19411`, MEDIUM `19412`, COMPLEX `19413`, REASONING `19414`",
    "risk": "medium",
    "passes": true
  }
]
